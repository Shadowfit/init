spring:
  application:
    name: shadowfit
  profiles:
    active: prod

  # 시간대 — 한국 전용 서비스 (협의 안건 #16, 갱신 15)
  # LocalDateTime 직렬화 형식 — § 12.5 결정과 정합 ("2026-05-28T10:00:03", timezone 마커 없음)
  jackson:
    time-zone: Asia/Seoul
    serialization:
      write-dates-as-timestamps: false

  # [1] 데이터베이스 및 JPA 설정 (spring 하위로 정렬)
  datasource:
    # rewriteBatchedStatements=true: JDBC 드라이버가 batch INSERT 를 multi-row SQL 한 방으로 재작성.
    # 🔴 2026-08-18 실측 정정 (#221) — «개선» 이 아니다. 레벨 20세션·ABBA 8판 대조:
    #      true   RPS 923.7  p50 88  p95 201  p99 486
    #      false  RPS 964.8  p50 95  p95 131  p99 261
    #    처리량 차이는 이상치 하나에 끌려 중앙값끼리는 942 ↔ 965 지만, **p99 는 4판 전부 일관되게
    #    나쁘다**(444~595 ↔ 209~338). 팔은 Com_insert 30,000 ↔ 150,000 으로 확정했다.
    #    ⚠️ 값을 아직 안 바꾼다 — 이 판은 레벨 20 한 지점이고, 끄는 결정은 별도 판단이다.
    #    결과: loadtest/results/session-spread-aws-2026-08-17/
    url: jdbc:mysql://${DB_HOST:shadowfit-mysql}:${DB_PORT:3306}/${DB_NAME:shadowfit}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&rewriteBatchedStatements=true
    username: ${DB_USERNAME:shadowfit}
    # 기본값 없음(의도) — docker-compose.prod.yml 은 이미 ${DB_PASSWORD:?운영 배포에는 필수}로
    # 막아뒀지만, 그건 compose 레이어의 방어라 jar 를 직접 띄우는 등 그 레이어를 안 거치면
    # 뚫린다. 여기 기본값이 있으면 그 경로에서 비번 없이도 'shadowfit'으로 조용히 접속을
    # 시도했다 — 실제 로컬/배포 흐름(.env.example 의 DB_PASSWORD=CHANGE_ME)도 이 기본값에
    # 기대지 않으므로 죽은 안전망이었다. 없으면 플레이스홀더 해석 실패로 기동 자체가 죽는다.
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      # 근거 (2026-08-09 4차 실측, docs/decisions/commit-count-and-mysql-metrics.md §0-1-ㄹ,
      # 원본 loadtest/results/commit-count-2026-08-09/ §2-3):
      #
      #   다세션 페이로드 · 기본 내구성 · c=100 에서
      #     pool  2 →  69%   pool  5 →  69%   pool 10 → 100%   pool 20 → 100% (plateau)
      #
      # **10 부터 plateau 고 5 는 부족하다.** 15 는 그 위의 여유값이다.
      #
      # 🔴 이 값이 말할 수 있는 것은 «5 는 부족하다» 까지다. «15 가 최적» 은 아니다 —
      #    10~20 사이를 좁히는 실험은 하지 않았고, 실익이 낮아 계획도 없다.
      #
      # 이력 (값은 15 로 유지됐지만 근거는 세 번 갈렸다):
      #   1차 2026-07-25  "10 은 47% 타임아웃, 15부터 안정" → **폐기.** «배치당 25행» 쓰던 코드
      #                   기준이고, 같은 PR #53 이 넣은 다운샘플(R=5)이 그 전제를 지웠다
      #   2차 2026-08-08  "절벽이 아예 없다"                → **조건부.** 부하가 단일 세션
      #                   하나로 몰리는 페이로드였고, 그 조건에서는 fsync 가 천장이라 풀이 가려졌다
      #   3차 2026-08-08  "천장은 커밋 fsync"                → **조건부.** 위와 같은 페이로드
      #   4차 2026-08-09  위 표. 다세션으로 재니 절벽이 다시 나타났다 (이슈 #166)
      #
      # ⚠️ 여기 있던 «병목이 백엔드 CPU 로 옮겨갔다» 는 **철회된 주장이라 지웠다** — 근거로 들었던
      #    수치가 수집된 적 없는 지표였다(pool-cliff-vs-concurrency.md §0-1). 인용하지 말 것.
      maximum-pool-size: 15

  # [1-1] Flyway — 스키마 마이그레이션 (이슈 #115, schema-migration-tracking.md §8)
  #
  # 무엇이 달라졌나: 예전엔 docker initdb 가 schema.sql 을 한 번 실행하는 게 전부라
  # "이 DB 가 어디까지 갔는지" 아무 데도 안 남았다. 이제 Flyway 가 적용 이력을
  # flyway_schema_history 테이블에 기록하고, 미적용분이 있으면 부팅 시 적용한다.
  #
  # 이력 확인: SELECT * FROM flyway_schema_history  또는  GET /actuator/flyway
  flyway:
    enabled: true
    locations: classpath:db/migration

    # ⚠️ baseline-version 이 2 인 이유 (§3-1 baseline 함정)
    #   기존 DB 는 이미 V1(스키마)·V2(마스터 시드)에 해당하는 상태다. 여기에 Flyway 를
    #   처음 붙이면 baseline-on-migrate 가 "이 버전까지는 이미 됐다"고 도장을 찍는데,
    #   그 값이 1 이면 V2 가 실제로 실행돼 시드가 중복 INSERT 된다.
    #   2 로 찍어야 둘 다 건너뛰고 V3 부터 적용된다. 신규 설치는 빈 DB 라
    #   baseline 자체가 안 걸리고 V1→V2 가 정상 실행된다.
    baseline-on-migrate: true
    baseline-version: 2
    baseline-description: "existing schema before flyway"

    # 이미 적용된 파일이 바뀌면 부팅을 실패시킨다(기본값). 끄지 말 것 —
    # "누가 마이그레이션을 사후 수정했다"를 잡아주는 게 이 도구의 핵심 중 하나다.
    validate-on-migrate: true

  jpa:
     open-in-view: false # 2026-07-15: lazy 필드 접근이 뷰 렌더링 단계까지 조용히 숨겨지는 것 방지, 코드 작을 때 끔
     hibernate:
       # Flyway 가 스키마를 소유한다. Hibernate 는 만들지도 검증하지도 않는다.
       ddl-auto: none
     properties:
       hibernate:
         jdbc:
           # 🔴 2026-08-18 정정 (#211). 예전 주석은 «saveAll 을 batch 로 묶음 (부하테스트 §7.5 개선)»
           #    이었는데 **둘 다 사실이 아니다.**
           #
           #    ① **INSERT 는 이 설정으로 안 묶인다.** 이 프로젝트의 엔티티는 **11개 전부
           #       `GenerationType.IDENTITY`** 다. IDENTITY 는 행마다 생성된 키를 받아와야 해서
           #       Hibernate 가 insert 를 batch 로 못 묶는다 — 설정값과 무관하게 **꺼져 있다.**
           #    ② **§7.5 의 개선은 이 설정이 낸 것이 아니다.** 그 개선은 pose 적재를 JPA 에서
           #       `JdbcTemplate.batchUpdate` 로 옮긴 것이고(PoseDataService), 그 경로는 Hibernate 를
           #       아예 안 탄다. 즉 이 줄은 그 성과의 공을 잘못 가져가고 있었다.
           #
           #    ⚠️ 그렇다고 죽은 설정은 아니다 — **UPDATE·DELETE 배치에는 그대로 걸린다**
           #       (생성 키를 받을 필요가 없어서다). 지금 남은 `saveAll` 은 레퍼런스 저장
           #       한 곳뿐이고(PoseDataService), 그것도 INSERT 라 배치가 안 된다.
           #    📌 값을 바꾸려면 «무엇이 실제로 묶이는지» 부터 재고할 것. 지금 근거로는 25 를
           #       유지할 이유도 바꿀 이유도 없다.
           batch_size: 25
         # 위와 같은 이유로 order_inserts 는 지금 효과가 없다(묶일 insert 자체가 없다).
         # order_updates 는 유효하다.
         order_inserts: true
         order_updates: true

  # [1.5] 로컬 캐시 (Caffeine) — feedbackTemplates: 쓰기 경로 없는 카탈로그성 데이터, 1시간은
  # 히트율 손해 목적이 아니라 미래에 관리자 CRUD 생기고 @CacheEvict 깜빡해도 자동 복구되는 안전판.
  # exercises: 쓰기 경로 있음(AdminExerciseService.updateThresholds) — @CacheEvict로 무효화,
  # findByIdCached(읽기 전용)만 캐시 타고 findById(쓰기 경로)는 캐시 미적용 유지.
  # exerciseReferences: 쓰기 경로(saveReferencePoses, 신규 운동 등록 시만)가 조회 메서드를
  # 재사용하지 않아 detached 엔티티 함정 없음 — findByExerciseId에 직접 @Cacheable, evict만 병행.
  cache:
    type: caffeine
    cache-names: feedbackTemplates,exercises,exerciseReferences # 시작 시 미리 등록 — 안 하면 Micrometer 가 지연생성 캐시를 못 잡음
    caffeine:
      spec: expireAfterWrite=1h,maximumSize=500,recordStats

# [2] gRPC 설정 (최상단 레벨)
grpc:
  port: 6565
  server:
    port: 6565
    reflection-enabled: true
  client:
    fastapi-client: # @GrpcClient("fastapi-client")와 일치
      # 주소는 compose 가 주는 환경변수에서 온다 (#285). 이 줄이 하드코딩이던 동안
      # compose 의 AI_SERVER_HOST·AI_SERVER_GRPC_PORT 는 **아무도 안 읽는 죽은 키**였고,
      # 값이 우연히 같아서 증상이 없었다. 컨테이너명이나 포트를 바꾸면 옛 주소로 계속
      # 붙어 UNAVAILABLE → 서킷브레이커(aiServer) 열림 → 「AI 장애」로 오독됐을 자리다.
      # 기본값은 도커 네트워크 기준이라 로컬 bootRun 은 AI_SERVER_HOST=localhost 를 준다
      # (위 datasource url 의 ${DB_HOST:shadowfit-mysql} 과 같은 방식).
      address: 'static://${AI_SERVER_HOST:shadowfit-ai}:${AI_SERVER_GRPC_PORT:8585}'
      negotiation-type: plaintext

# AI 채널 풀 크기 — entrypoint.sh 가 띄우는 AI 워커 프로세스 수와 같아야 한다.
# docker-compose 의 AI_CHANNEL_POOL_SIZE(=AI_WORKER_COUNT 와 같은 소스)를 그대로 받는다
# (docs/decisions/ai-channel-pool-hardening.md).
ai:
  channel-pool-size: ${AI_CHANNEL_POOL_SIZE:3}
  # gRPC vs WebClient 실측 비교(docs/decisions/grpc-webclient-empirical-comparison.md §8).
  # grpc(기본) | webclient — AiAnalysisClient 구현체 스위치.
  client-type: ${AI_CLIENT_TYPE:grpc}
  webclient:
    # gRPC 채널 풀(8585-8587, Spring이 직접 연결)과 달리 ai-nginx(8000)를 거친다 — 프론트의
    # POST /pose 가 이미 쓰는 X-AI-Worker 헤더 기반 라우팅을 그대로 재사용한다(§8.3). 로컬
    # bootRun 은 AI_NGINX_HOST=localhost 로 override(호스트에 8000 포트가 매핑돼 있다).
    base-url: 'http://${AI_NGINX_HOST:ai-nginx}:${AI_NGINX_PORT:8000}'

# [3] Swagger 및 기타 설정 (최상단 레벨)
#
# 기본은 꺼짐이다 — SPRING_PROFILES_ACTIVE 로는 dev/prod 를 못 가른다(docker-compose.yml 의
# dev 기동도 SPRING_PROFILES_ACTIVE: prod 를 쓴다). 그래서 별도 env var(SPRINGDOC_ENABLED)로
# 연다. security.whitelist 의 /v3/api-docs·/swagger-ui 항목은 그대로 둔다 — 꺼져 있으면
# springdoc 이 그 경로에 아무 핸들러도 안 등록해 permitAll 이어도 노출될 게 없다.
springdoc:
  api-docs:
    path: /v3/api-docs
    enabled: ${SPRINGDOC_ENABLED:false}
  swagger-ui:
    path: /swagger-ui.html
    enabled: ${SPRINGDOC_ENABLED:false}

security:
  whitelist:
    - "/"
    - "/error"
    - "/v3/api-docs/**"
    - "/swagger-ui/**"
    - "/swagger-ui.html"
    - "/webjars/**"
    - "/member/login"
    - "/member/signup"
    # ⚠️ permitAll 이 이 엔드포인트의 **전제**다 (이슈 #135).
    #
    # 재발급은 access token 이 만료된 뒤에 불리는 것이 정상 경로라, 인증을 요구하면 «만료돼서
    # 부르는 API 가 만료 때문에 막히는» 상태가 된다. 그래서 필터를 안 태운다.
    #
    # 그 대신 신원 근거가 요청자(SecurityContext)가 아니라 **refresh token 의 서명**이다 —
    # 서버가 서명한 것이라 위조가 불가능하고, subject 가 곧 소유자다.
    # (decisions/token-lifecycle.md §4-2 — «요청자 기준으로 확인한다» 는 처음 안이 틀렸던 자리다)
    - "/member/reissue"
    - "/ws/**"
    - "/actuator/health"
    - "/actuator/health/**"
    # ⚠️ 2026-08-08 — 이 줄이 필요하다는 것은 **실측으로 알았다.**
    #
    # 관리 포트를 9090 으로 분리하면(management.server.port) 시큐리티가 거기엔 안
    # 걸릴 것으로 봤는데 **틀렸다.** 실제로는 필터체인이 두 포트에 공유돼서,
    # 분리만 해두면 Prometheus 가 9090 을 긁을 때 401 을 받는다(검증 로그: 9090
    # /actuator/prometheus → 401, /actuator/health → 200 ← 이 whitelist 때문).
    #
    # 그래서 permitAll 이 필요하다. 지표가 공개되지 않는 근거는 **인증이 아니라
    # 포트 경계**다 — 액추에이터는 9090 에서만 서비스되고, prod compose 는 그
    # 포트를 호스트에 매핑하지 않는다. 8080 에 같은 경로를 쳐도 핸들러가 없다.
    # 🔴 따라서 9090 을 어딘가에 노출하는 순간 이 whitelist 가 그대로 뚫린다.
    - "/actuator/prometheus"

  # [보안-2] 인증 경로 시도 제한 (이슈 #394)
  #
  # 위 whitelist 의 permitAll 셋(/member/login · /member/signup · /member/reissue)은
  # **인증 없이 부를 수 있다는 것이 곧 제한이 필요한 이유**다. 그전까지 시도를 세는 곳이
  # 앱에도 인프라에도 없었다 — 리버스 프록시가 아직 없어서(reverse-proxy-and-tls.md 미결정)
  # 「프록시로 막으면 된다」가 무기한 열려 있던 자리다.
  #
  # 키가 둘인 이유: IP 는 «한 곳에서 여러 계정», 계정은 «여러 곳에서 한 계정» 을 막는다.
  # 로그인 브루트포스의 실제 모양이 후자라 IP 하나만으로는 반쪽이다.
  #
  # ══════════════════════════════════════════════════════════════════════════════
  # 🔴 아래 숫자는 임의값이 아니다 — 가정 넷에서 유도된다
  # ══════════════════════════════════════════════════════════════════════════════
  #
  #   가정 A  DAU              1,000    load-test-strategy.md §4.2 «성공 시나리오»
  #                                     ⚠️ 같은 문서 §4.1 의 «냉정한 기본값» 은 DAU 30~50 이다.
  #                                        큰 쪽을 쓰는 것은 한도를 **넉넉하게** 잡는 방향이라
  #                                        오탐(정상 사용자 차단)이 덜 난다
  #   가정 B  피크 1시간 집중도   18%     ai-receive-path-scaling.md ㄹ-1 — «피크 동시 67» 에서
  #                                     역산해 복원한 값. 이 저장소가 이미 쓰고 있는 계수를
  #                                     그대로 쓴다 (새 가정을 만들지 않는다)
  #   가정 C  1인 1일 로그인      1회     🔶 **여기서 새로 못박는다.** refresh 수명이 7일이라
  #                                     (jwt.refresh_expiration_time) 실제로는 이보다 훨씬 적다.
  #                                     상한 쪽으로 잡은 값이다
  #   가정 D  한 IP 뒤 사용자     20명    🔶 **여기서 새로 못박는다.** 모바일 캐리어 NAT·학교·
  #                                     회사 공유. 재본 적 없다
  #
  #   ── 유도 ──────────────────────────────────────────────────────────────────
  #
  #   서비스 전체 피크 로그인 = DAU × (C) × (B) ÷ 60분
  #                          = 1,000 × 1 × 0.18 ÷ 60 = **3회/분**
  #
  #   account-failures-per-window = 3
  #     앵커: **계정 하나가 «서비스 전체 피크» 보다 많이 로그인할 이유가 없다.**
  #     그 위는 정의상 비정상이다. 그리고 이건 «실패» 만 세므로(성공하면 초기화)
  #     정상 사용자가 닿는 값이 아니다 — 비밀번호를 분당 3번 틀려야 걸린다.
  #
  #   ip-per-window = 3 × (D) = **60회/분**
  #     한 IP 뒤에 20명이 있어도 각자 «전체 피크» 만큼 쓸 수 있게 둔다. 계정 한도를
  #     IP 에 그대로 걸면 NAT 뒤 사용자들이 서로의 한도를 잡아먹는다.
  #
  # ══════════════════════════════════════════════════════════════════════════════
  #
  # ⚠️ **이 값들은 «측정» 이 아니라 «가정 + 유도» 다.** 실사용 로그가 생기면 C·D 를 실제
  #    분포로 갈아끼우고 여기 숫자를 다시 내는 것이 맞다. 지금 이 저장소에 그 로그는 없다
  #    (slo-baseline.md §6 — 운영 SLO 를 못 세우는 것과 같은 이유).
  #
  # ⚠️ **실패 방향**: 값이 작으면 정상 사용자가 429 를 받는다(불편, 창이 지나면 풀림).
  #    크면 공격이 더 오래 시도한다(무제한은 아님). 둘 다 «죽음» 은 아니다.
  #
  # 🔴 IP 키는 remote addr 이다. 프록시를 세우는 날 FORWARD_HEADERS_STRATEGY 를 같이 켜지
  #    않으면 **IP 한도가 전역 한도로 붕괴한다** — AuthRateLimitFilter 주석 참조. 기동 로그가
  #    그 조합을 한 번 짚는다.
  rate-limit:
    enabled: ${AUTH_RATE_LIMIT_ENABLED:true}
    window-seconds: 60
    ip-per-window: 60
    account-failures-per-window: 3
    # 이 장치 자체가 메모리 고갈 통로가 되지 않게 하는 상한. 넘으면 오래된 키부터 버린다.
    max-keys: 100000

# [5-b] 종료 정책 — #208 조치 후보 1 (2026-08-26/27 실측 후 채택).
# server.shutdown 이 없어 기본 IMMEDIATE였고, SIGTERM 후 ~2초는 정상 처리되다 그 뒤로는
# 커넥션만 받고 응답 없이 끊기는 구간이 실측됐다(#208 코멘트, curl 「Empty reply」).
# graceful 로 켠 뒤 진짜 in-flight 요청(2MB 느린 업로드, SIGTERM이 41% 진행 시점에 옴)으로
# 재검증하니 HTTP 200으로 완료됐다 — 효과 확인.
# spring.lifecycle.timeout-per-shutdown-phase 는 값을 안 정한다 — 이 값이 얼마여야
# 하는지 실측 근거가 없어서([[feedback_no_arbitrary_threshold_values]]), Spring Boot
# 기본값(30초)을 그대로 쓴다. 이 값으로도 위 재검증이 통과했다.
server:
  shutdown: graceful
  tomcat:
    # 기본값(false)이면 Micrometer의 TomcatMetrics가 tomcat.threads.*·tomcat.global.*를
    # 못 낸다 — 그 값들이 Tomcat의 ThreadPool MBean(JMX)에서 나오는데 MBean 등록 자체가
    # 꺼져 있어서다(tomcat.sessions.*는 Manager 객체를 직접 참조해서 이 설정과 무관하게
    # 이미 나오고 있었다 — 그 차이로 원인을 좁혔다,
    # docs/decisions/group-websocket-capacity-deep-dive.md §1-1/§1-5, 2026-08-30).
    mbeanregistry:
      enabled: true

# [6] Actuator — 캐시 히트율·서킷브레이커 상태 관측용
management:
  # ⚠️ 2026-08-08: 관리 엔드포인트를 앱 포트(8080)에서 분리했다.
  #
  # 이유는 /actuator/prometheus 다. Prometheus 가 긁어가려면 인증 없이 닿아야 하는데,
  # 8080 은 외부 노출이라 whitelist 에 넣으면 지표가 인터넷에 공개된다 — 엔드포인트별
  # 응답시간·에러율·회원 수 추이가 전부 보인다.
  #
  # 그래서 포트를 나누고 compose 에서 9090 을 **매핑하지 않는다.** 같은 도커 네트워크
  # 안의 Prometheus 만 shadowfit-backend:9090 으로 닿는다. AI 서버 gRPC 8585 를
  # expose 만 하고 ports 에 안 쓰는 것과 같은 방식이다.
  #
  # 🔴 부작용: /actuator/health 도 같이 9090 으로 옮겨간다. 컨테이너 healthcheck 와
  #    verify 스킬이 8080 을 치고 있어 함께 고쳤다(docker-compose.prod.yml,
  #    .claude/skills/verify/SKILL.md). 둘 다 안쪽에서 부르므로 매핑 없이 닿는다.
  server:
    port: 9090
  endpoints:
    web:
      exposure:
        # flyway: 이 서버가 어느 버전까지 적용했는지 HTTP 로 확인한다 (GET /actuator/flyway).
        # 배포 후 "스키마가 코드보다 뒤처졌나"를 DB 에 붙지 않고 볼 수 있어 CD 와 맞물린다.
        # prometheus: 스크레이프 대상. 위 포트 분리로 외부에서는 닿지 않는다.
        include: health,caches,metrics,circuitbreakers,circuitbreakerevents,flyway,prometheus
  endpoint:
    health:
      show-details: always
  health:
    circuitbreakers:
      enabled: true

# [7] Resilience4j — Spring→AI(FastAPI) gRPC 호출(StartAnalysis/ExtractReferenceData/StopAnalysis)
# 서킷브레이커. AI 장애 시 백엔드가 계속 호출을 밀어넣는 대신 빠르게 실패해서 graceful하게
# 버티도록(load-test-strategy.md §9.2 OPEN 항목, production-signal-checklist.md §2-3 재확인).
#
# 🔴 "instances.aiServer" 하나였던 것을 "configs.default"로 바꿨다(#556,
# docs/decisions/circuit-breaker-worker-aggregation.md). ExerciseAnalysisService 가 이제
# "aiServer-0"·"aiServer-1"·"aiServer-2"(워커별)를 요청한다 — 이름을 여기 하드코딩하지
# 않고 default 설정으로 두면, AI_WORKER_COUNT 가 바뀌어도(ai-channel-pool-hardening.md
# §2-2 가 지적한 하드코딩 이원화와 같은 함정) 이 설정을 안 늘려도 새 워커가 같은 값을 받는다.
resilience4j:
  circuitbreaker:
    configs:
      default:
        registerHealthIndicator: true
        slidingWindowType: COUNT_BASED
        slidingWindowSize: 10
        minimumNumberOfCalls: 5
        failureRateThreshold: 50
        waitDurationInOpenState: 10s
        permittedNumberOfCallsInHalfOpenState: 3
        automaticTransitionFromOpenToHalfOpenEnabled: true

jwt:
  secret: ${JWT_SECRET}
  # 30분. 24시간에서 줄였다 (이슈 #137, decisions/token-lifecycle.md §3-0-2).
  #
  # ⚠️ **측정값이 아니라 약속(threshold)이다.** 이 프로젝트엔 세션 길이 분포도 SLO 도 사용
  # 로그도 없어 유도할 근거가 없다. 이 값의 의미는 하나뿐이다 — 블랙리스트를 없앤 대가로
  # **로그아웃 후 access 가 살아 있는 최대 구간**이 곧 이 값이다.
  #
  # 줄일 수 있게 된 전제: 프론트에 재발급 호출이 붙었다(api.ts reissueOnce, #135). 그게 없던
  # 동안은 수명을 줄이면 그 주기로 사용자가 로그아웃돼서 24시간을 쓸 수밖에 없었다.
  expiration_time: ${JWT_EXPIRATION_TIME:1800}
  refresh_expiration_time: ${JWT_REFRESH_EXPIRATION_TIME:604800}

internal:
  api:
    token: ${INTERNAL_API_TOKEN}

# [5] 운동 세션 타임아웃 설정
exercise:
  session:
    timeout:
      # [주 기준] 마지막 활동(rep 완성) 이후 이만큼 지나면 걷어간다.
      # session-liveness-vs-elapsed-time.md ㄷ안 — 생존 판정을 경과 시간에서 마지막 활동으로 옮겼다.
      # 하한 근거: 세트 사이 휴식이 90초까지 관측됐다(이슈 #91 재현). 그동안은 rep 이 안 나와
      # 갱신이 멈추므로 그보다 넉넉해야 한다. 10분이면 여유가 크고, 이탈한 세션도 종전 45분이
      # 아니라 10분 만에 정리된다.
      idle-minutes: 10
      # [폴백 전용] 아직 rep 이 하나도 없는 세션(자세 잡기·준비 중)에만 쓰는 기존 식의 버퍼.
      # 예: 스쿼트 예상시간 15분 + 30분 버퍼 = 45분. 첫 rep 이 들어오는 순간 위 idle 기준으로
      # 넘어간다. 준비 구간에 짧은 유휴 임계를 걸면 시작하자마자 걷어가게 되어 남겨둔다.
      default-buffer-minutes: 30
      # 스케줄러 체크 간격(분) - 매 1분마다 체크
      check-interval-minutes: 1

# [5.5] 회원 탈퇴 가드 (withdrawal-with-active-session.md, 이슈 #87)
member:
  withdrawal:
    # 이 시간 동안 pose_data 유입이 없으면 그 IN_PROGRESS 세션은 죽은 것(앱 종료·네트워크 단절)으로
    # 보고 탈퇴를 허용한다. 상태값만 보면 좀비 세션 때문에 운동 중이 아닌 사용자가 최대 ~45분간
    # 탈퇴하지 못한다.
    #
    # 값의 근거는 배치 간격(rep 단위 ~3~4초)이 아니라 **세트 간 휴식**이다. 휴식 중에는 rep 이
    # 완성되지 않아 AI 가 콜백을 안 보내므로 Spring 이 보기엔 죽은 세션과 구분되지 않는다 —
    # 이 프로젝트는 이미 같은 이유로 ET-C(AI timeout 자동 종료)를 거부한 적이 있다(tts-design.md
    # §2.A ET-C: "세트 사이 휴식 시간(30~90초)도 frame 안 오는 구간이라 휴식과 종료 구분 불가").
    #
    # restTimeSec = max(90 - (level-1)*5, 30) → 최대 90초(초급자, 12-persona-difficulty.md:90).
    # 여기에 2배 여유를 둬 180초. 짧게 잡으면 쉬는 중인 사용자를 죽은 것으로 오판해 "정말 운동
    # 중인데 탈퇴가 통과"하는데, 그건 막으려던 결함 그 자체라 반대 방향 오판(죽은 세션 때문에
    # 3분 더 기다림)보다 훨씬 나쁘다.
    active-workout-idle-seconds: 180

# [5.6] 트레이너 SSE 하트비트 (trainer-live-monitoring.md §8 세션5)
coaching:
  trainer-stream:
    # half-open TCP 좀비 연결(네트워크 단절 등으로 클라이언트가 FIN 없이 사라진 경우)을 다음
    # broadcast 없이도 능동적으로 잡아내기 위한 주기. 리버스 프록시/ALB idle timeout 을 피하려는
    # 값이 아니다 — 이 프로젝트엔 아직 그런 프록시가 없다(reverse-proxy-and-tls.md, 미결정).
    # 30초는 실측이 아니라 SSE 하트비트 업계 관행값(2026-08-30 사용자 확인) — 프록시가 정해지거나
    # §7~8 캐파시티 실측이 나오면 재검토.
    heartbeat-interval-seconds: 30

# [6] pose_data 파티션 TTL 설정 (report-read-path.md §9-B, 2026-07-24 확정)
pose-data:
  # 저장 전 다운샘플 윈도우 크기(R). 창 하나에서 대표 프레임 1개만 남긴다.
  # 🔴 **튜닝 손잡이가 아니다.** R=5 는 실측으로 고른 하한이고(R 5→1 은 배치 고정비용 비중이
  #    커져 행당 효율이 오히려 악화된다, pose-ingest-downsampling.md §5-1(4)), 그 결정은 그대로다.
  #    설정으로 뺀 이유는 **측정**이다 — 정본 「다운샘플 1.7배」가 단일 핫세션 조건에서 나온 값이라
  #    다세션에서 팔을 바꿔 재려면 재빌드가 필요했고, 그 재빌드가 실험을 막고 있었다(P2).
  #    값을 바꾸는 것은 측정 조건이지 채택이 아니다.
  downsample-window: 5
  # E2E 상호작용 실험용 측정 손잡이(load-test-strategy.md §3.3.2) — 콜백 처리에 인위 지연을
  # 얹어 "Spring이 느려지면 AI worker까지 막히는가"를 실측하기 위한 것. 기본 0=off, 운영 동작 불변.
  inject-delay-ms: ${POSE_INJECT_DELAY_MS:0}
  partition:
    # 이번 달 + 이 개월 수만큼은 보존(버퍼), 그보다 오래된 파티션은 드롭
    retention-buffer-months: 1
    # pfuture가 실데이터를 안 떠안도록 이번 달 기준 몇 개월 앞까지 미리 파티션을 만들어둘지
    lookahead-months: 2
    # 파티션 점검 주기 - 월 단위 하우스키핑이라 매일 1회(새벽 4시)면 충분
    check-cron: "0 0 4 * * *"
  # 고아 행 관측 (이슈 #87) — 세기만 하고 지우지 않는다. 수정안 선택에 필요한 발생 빈도를 모으는 용도
  orphan-monitor:
    # anti-join 범위. 파티션 TTL(이번 달 + 지난 1개월)이 살려두는 범위와 맞춘다 — 그보다 오래된
    # 행은 곧 통째로 드롭되므로 세지 않는다. created_at 하한이 파티션 pruning 도 걸어준다
    lookback-months: 2
    # 파티션 드롭(4시)이 끝난 뒤에 세야 곧 사라질 행까지 세어 값이 부풀지 않는다
    check-cron: "0 30 4 * * *"

# [4] 로깅 설정 (최상단 레벨)
logging:
  level:
    root: INFO
    com.shadowfit: DEBUG
    io.github.lognet.grpc: DEBUG
    net.devh: DEBUG
    org.springframework.jdbc.datasource.init: DEBUG # SQL 초기화 로그 확인용