# ==================== OS ====================
.DS_Store
Thumbs.db
Desktop.ini
*.swp
*.swo
*~

# ==================== IDE ====================
.idea/
.vscode/
*.iml
*.iws
*.ipr
*.code-workspace

# ==================== 환경 변수 / 비밀 정보 ====================
.env
.env.*
!.env.example
#
# 🔴 application-prod.yml 은 **더 이상 무시하지 않는다** (2026-08-22).
#
# 초기 커밋부터 여기 있었는데, 그건 «prod 설정 파일에는 비밀이 들어간다» 는 일반적 관성이지
# 이 저장소를 보고 내린 판단이 아니었다. 이 저장소는 비밀을 전부 환경변수로 받는다
# (application.yml 의 ${JWT_SECRET}·${DB_USERNAME} …). 그리고 무시된 파일은 이미지에도
# 안 들어가므로, 무시한 채로는 **배포된 앱이 그 설정을 영영 못 본다.**
#
# ⚠️ 그러니 **비밀은 여기 적지 말 것.** 값이 필요하면 .env 로 받고 yml 에서는 ${...} 로 참조한다.

# ==================== Frontend (React Native / Expo) ====================
frontend/node_modules/
frontend/.expo/
frontend/dist/
frontend/web-build/
frontend/expo-env.d.ts
frontend/.metro-health-check*
frontend/npm-debug.*
frontend/yarn-debug.*
frontend/yarn-error.*
frontend/*.tsbuildinfo
frontend/ios/
frontend/android/
frontend/.kotlin/
frontend/*.orig.*

# ==================== Backend (Spring Boot / Gradle) ====================
backend/build/
backend/.gradle/
backend/bin/
backend/out/
backend/HELP.md
backend/.apt_generated
backend/.classpath
backend/.factorypath
backend/.project
backend/.settings/
backend/.springBeans
backend/.sts4-cache

# ==================== Python 바이트코드 (저장소 전체) ====================
# ai-server 밖에도 파이썬이 산다 — loadtest 의 rig·seed 스크립트가 그렇다.
# 아래 ai-server 절의 규칙은 그 경로를 안 덮어서 rig 을 돌릴 때마다 __pycache__ 가 뜬다.
__pycache__/
*.pyc

# ==================== AI Server (Python / FastAPI) ====================
.venv/
venv/
ai-server/venv/
ai-server/.venv/
ai-server/__pycache__/
ai-server/**/__pycache__/
ai-server/*.pyc
ai-server/**/*.pyc
ai-server/*.egg-info/
ai-server/dist/
ai-server/build/
ai-server/.mypy_cache/
ai-server/.pytest_cache/
ai-server/.ruff_cache/
ai-server/*.log
ai-server/uvicorn.*.log

# ==================== 미디어 / 분석 산출물 ====================
*.mp4
*.avi
*.mov
*.mkv
*.webm
/git_local

# ==================== Java ====================
*.class
*.jar
# Gradle 래퍼 jar 은 반드시 커밋한다 — 없으면 clone 후 ./gradlew 가 바로 죽는다
# ("Unable to access jarfile ...", 2026-07-28 CI 최초 실행에서 발각).
# 아래 경로는 래퍼가 backend/ 에서 저장소 루트로 옮겨진 뒤에도 갱신되지 않아
# 실제로는 아무것도 예외 처리하지 못하고 있었다.
!gradle/wrapper/gradle-wrapper.jar
!backend/gradle/wrapper/gradle-wrapper.jar
*.war
*.ear
hs_err_pid*
replay_pid*

# ==================== 빌드 산출물 ====================
*.apk
*.ipa
*.aab

# ==================== Docker ====================
mysql_data/

# ==================== Signing Keys / Certificates ====================
*.jks
*.p8
*.p12
*.key
*.pem
*.mobileprovision

# ==================== Claude Code / Skills ====================
# skills/ 만 추적한다. verify 스킬은 세션마다 읽히는 «검증 레시피» 인데, 추적되지 않으면
# 코드가 고쳐져도 문서가 조용히 낡는다 — 2026-08-11 에 이미 고쳐진 결함 4개를 «살아 있다» 고
# 안내하고 있었다(sex 오타·ADMIN 자기지정·validation 핸들러·Flyway 시드). 이력이 있어야
# «언제부터 틀렸나» 를 물을 수 있다.
#
# ⚠️ 디렉터리를 통째로 제외하면 그 안을 다시 살릴 수 없다(git 이 아예 안 내려간다).
#    그래서 `.claude/` 가 아니라 `.claude/*` 로 두고 skills/ 만 예외로 판다.
.claude/*
!.claude/skills/
!.claude/settings.json
!.claude/hooks/

# ==================== 루트 생성 파일 ====================
/package-lock.json
/skills-lock.json

# Local setting file (Secret keys)
src/main/resources/application-local.properties

# MySQL 데이터 폴더 제외 (매우 중요)
/mysql/data/

# 환경 변수 파일 제외 (보안)
.env

# 빌드 결과물 제외
/build/
/out/
.gradle/

# 프론트 소스는 추적. node_modules/.expo/빌드물은 frontend/.gitignore 가 제외함.
.agents/
.backup-presync/

# loadtest 시딩 산출물 (gen_pose_template.py 출력, ~1.3MB)
loadtest/seed/template.sql

# 개인 지원 서류 — 이 저장소는 PUBLIC 이다.
# 지원서·경력기술서는 특정 회사에 낸 문서이고 개인 신상이 들어간다. 공개 이력에
# 박히면 지우기 어렵고(포크·캐시), 회사명이 붙은 파일이 커밋 로그에 남는 것 자체가
# 의도치 않은 노출이다. 로컬에는 그대로 두되 추적만 하지 않는다.
#
# 📌 여기 걸린 파일들은 애초에 커밋된 적이 없어 히스토리 정리가 필요 없었다.
#    새로 쓸 때도 이 이름 규칙(application-*, career-statement-*)을 지키면 자동으로 빠진다.
#    포트폴리오 본체(one-pager, db-deep-dive, realmysql-experiments 등)는 계속 추적한다.
docs/portfolio/application-*
docs/portfolio/career-statement-*

# ==================== 런타임 데이터 ====================
# 관리자가 올린 기준 영상(mp4) — docker-compose 가 /data/reference-videos 로 bind mount 한다
backend/data/
data/
