# P6 從 R9 — 팔 D(관측 스택) 첫 투입 + 캡 재확인 (2026-09-08)

측정일: 2026-09-07 UTC 17:00 ~ 21:48 (약 4시간 48분, preflight+rehearsal+본판+collect) · RUN_ID `ec2-20260907-170040`
대상 박스: **`c7i.4xlarge`**(16 vCPU / 31GB, Round 태그 `coresidency-R9-2026-09-08-v3`) · 부하기: **`c7i.xlarge`** · ap-northeast-2
커밋: `8ff7ad74`(#687 로 로그인 레이트리밋 손잡이 추가 직후) — #686(캡 리허설 FAIL)·#687(로그인 429) 두 버그 다 이 커밋에서 고쳐진 뒤 첫 무인 실행
설계: [`../../../docs/decisions/ai-coresidency-capacity.md`](../../../docs/decisions/ai-coresidency-capacity.md) §3-2 · H5(팔 D)는 2026-08-23 从 R9 로 등록만 돼 있었고 이번이 첫 실측
격자: `ARMS='A B C D'` × `LEVELS='20 40 60 80 90 100 120 160'` × `REPEATS=3` + 앵커 4판(B@80) + §T 로더 코어 팔 12판(taskset 2코어 ↔ 전체) = 본판 91행

> 팔 정의 — **A** 캡 없음·ghz 부하 없음(유휴 동거 기준선) · **B** A+ghz 부하(캡 없음, prod 현행과 유사) ·
> **C** B + CPU 캡(AI 8vCPU·backend 4·mysql 4) · **D** C + 관측 스택(cadvisor·node-exporter) 동거.

---

## 0. 한 줄

**rig 버그(#686·#687) 없이 처음으로 깨끗하게 돌았다.** 캡 대가(H3)는 08-17 라운드보다
더 큰 폭으로 재확인됐고, 오래 미결이던 **관측 스택 비용(H5/Q5)은 캡 자체의 대가에 묻힐 만큼
작다 — C와 D 차이가 캡 유무(A/B vs C/D)의 차이보다 한 자릿수 이상 작다.**

---

## 1. rig 정상성 확인 — #686·#687 재발 없음

- `caps.tsv` 51행 전부 `verdict=OK`(STALE 0건) — #686(캡 리허설 FAIL) 재발 안 함.
- `coresidency.tsv` 91행 전부 `setup_fail=0`·`nolease=0`·`nopose=0` — #687(로그인 429로 계정 준비 중단) 재발 안 함.
- `detect_pct`는 91행 전부 92.7% 이상(대부분 94~100%) — 검출 자체가 깨진 행은 없다.

## 2. 캡 대가(H3 재확인) — 120세션부터 처리량이 245 rps 근방에 눌러앉는다

r1·r2·r3 3판 평균(`coresidency/coresidency.tsv`, discard/anc/§T 판 제외):

| 세션 | A rps | B rps | C rps | D rps | A p99 | B p99 | C p99 | D p99 |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 20 | 59.9 | 59.9 | 60.0 | 60.0 | 33.3 | 33.6 | 34.7 | 34.4 |
| 80 | 239.9 | 239.9 | 239.9 | 239.8 | 130.7 | 139.1 | 172.0 | 149.7 |
| **90** | 269.7 | 269.8 | **246.0** | **247.8** | 167.5 | 165.8 | **625.3** | **620.3** |
| **120** | 359.7 | 359.7 | **245.6** | **246.1** | 286.3 | 286.4 | **933.3** | **963.9** |
| **160** | 444.2 | 449.2 | **238.6** | **244.7** | 650.4 | 618.0 | **1275.3** | **1185.3** |

- 90세션부터 캡이 걸린 C·D가 목표 RPS(3×세션)를 못 채우기 시작하고(A·B는 160세션까지 목표치를
  거의 그대로 유지), **120세션 이상에서는 C·D 처리량이 아예 245~250 rps 근방에 눌러앉는다** —
  08-17 라운드(캡 대가 −9.7%, 312 rps)보다 훨씬 큰 폭. 레벨 격자는 08-17과 같지만(20~160)
  ⚠️ 캡 값 자체가 08-17과 같은 조건인지(AI vCPU 값 등) 두 라운드 `.env`를 직접 대조 안 했다 —
  절대값 비교는 caveat 필요, "캡=대가" 방향은 동일.
- p99는 캡 걸린 쪽이 90세션부터 600ms대로 튄다 — A·B는 160세션에서도 600ms 안팎인데, C·D는
  90세션(!)에서 이미 그 수준이다.

## 3. H5(관측 스택 비용, Q5) — C와 D가 구분 안 된다, 사실상 닫힘

| 세션 | C rps | D rps | rps 차이 | C p99 | D p99 | p99 차이 |
|---:|---:|---:|---:|---:|---:|---:|
| 90 | 246.0 | 247.8 | +0.7% | 625.3 | 620.3 | −0.8% |
| 100 | 246.6 | 245.6 | −0.4% | 834.3 | 778.1 | −6.7% |
| 120 | 245.6 | 246.1 | +0.2% | 933.3 | 963.9 | +3.3% |
| 160 | 238.6 | 244.7 | +2.6% | 1275.3 | 1185.3 | −7.1% |

- rps 차이는 전부 ±3% 안. p99는 최대 ±7% 정도 갈리지만 **방향이 세션마다 뒤집힌다**(D가 나을
  때도, C가 나을 때도 있음) — 세션 수를 올릴수록 D가 일관되게 나빠지는 패턴은 없다. 반복 3판
  범위에서 나올 수 있는 흔들림인지 진짜 작은 비용인지는 이 판 수로는 못 가른다
  ([[feedback_measure_design_needs_repeats]]).
- **결론: 관측 스택(cadvisor·node-exporter) 동거 비용은, 있다 해도 캡 자체의 대가(§2, −45%대)에
  비하면 무시할 수 있는 크기다.** `ai-coresidency-capacity.md` §6의 H5 "🔴 여전히 미측정"을
  "작다(±7% 안, 방향 불일치)"로 갱신 가능 — "0"이라 단정하기보다 "캡 대가에 묻힐 정도"로 적는다.

## 4. 「옆(Spring·MySQL)은 이번에도 안 아팠다」— side.tsv 요약

- MySQL `Threads_running`은 전 라운드 348개 관측점 중 332개가 2, 나머지도 3~4 — 부하 수준(20~160
  세션)·팔(A~D)과 무관하게 거의 항상 유휴. CPU 지표는 mysql exporter가 안 걷어서(이번 라운드
  한계) 쿼리·락 카운터로만 확인 가능.
- Spring `process_cpu_usage`(가용 코어 대비 비율, mid phase 3판 평균)는 160세션 기준
  **A 2.07% · B 2.05% · C 8.28% · D 0.85%** — 어느 팔이든 10% 미만. 캡을 걸어 backend 코어
  할당 자체가 줄어든 C에서 상대적으로 올라가 보이지만 절대적으로는 여전히 여유가 넘친다.
  원자료: `coresidency/side_process_cpu_summary.tsv`(원본 33MB `side.tsv`는 S3에만 있음:
  `s3://shadowfit-measure-055447613012/shadowfit/ec2-20260907-170040/coresidency/side.tsv`).
- → 08-17의 "옆은 애초에 안 아팠다"가 이번 라운드에서도 재확인됐다. **캡이 지킬 대상이 여전히
  안 보인다.**

## 5. §T(로더 코어 제한) — 여전히 로더가 병목 아님

`lc2`(taskset 2코어) vs `lcF`(무제한) 3쌍, 120·160세션 팔 B:

| 레벨 | lc2 평균 rps | lcF 평균 rps | 차이 |
|---:|---:|---:|---:|
| 120 | 359.5 | 359.6 | 0.0% |
| 160 | 444.6 | 444.5 | 0.0% |

방향도 안 갈리고 폭도 0% 수준 — 08-17 판정("로더는 병목 아님") 그대로 유지.

---

## 미검증 / 다음

- **CPU 캡을 몇으로 걸지는 이 라운드로 못 정한다.** 캡(AI 8vCPU)만으로 이미 −45%가 났고, 옆은
  캡 없이도 안 아팠다는 게 재확인됐을 뿐 — "그래서 몇으로 걸어야 하는가"는 **실측이 아니라
  이 호스트가 실제로 다른 워크로드와 동거하는지(사실 확인) + 동거한다면 그 워크로드가 뭘
  요구하는지(정책 결정)**가 먼저 있어야 답이 나온다. 지금은 둘 다 미확인.
- 08-17 대비 캡 대가 폭이 커진 이유(캡 조건 차이 vs 진짜 격차 확대)는 두 라운드의 `.env`/캡
  값을 직접 대조해야 갈린다 — 미수행.
- 대상 박스가 자동 종료 안 됨(별건) — 부하기는 collect 후 정상 종료했으나 대상 박스
  `i-01bfa1c0618f6fc0c`는 `run_all.sh` collect 완료(21:48 UTC) 후에도 계속 running으로 남아
  약 2시간 24분 초과 과금(2026-09-08 00:11 UTC 수동 terminate). 원인 미조사 — 별도 이슈 등록 검토.

관련: [[project_pending_aws_pool_sizing_reverify]] · [`../../../docs/decisions/load-test-glossary.md`](../../../docs/decisions/load-test-glossary.md)
