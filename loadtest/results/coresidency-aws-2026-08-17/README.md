# P6 2라운드 — H3 이 반증됐다. 캡은 지킬 것이 없는데 10% 를 낸다 (2026-08-17)

측정일: 2026-08-17 UTC 05:04~08:37 (약 3시간 33분) · RUN_ID `ec2-20260817-050448`
대상 박스: **`c7i.4xlarge`** (16 vCPU / 물리 8코어 / 31GB) · 부하기: **`c7i.xlarge`** (4 vCPU) · ap-northeast-2
설계: [`../../../docs/decisions/ai-coresidency-capacity.md`](../../../docs/decisions/ai-coresidency-capacity.md) ·
조건 원본: [`MANIFEST.txt`](MANIFEST.txt) · 커밋 `d05951a` (main)
격자: `ARMS='A B C'` × `LEVELS='20 40 60 80 90 100 120 160'` × `REPEATS=3` · `DUR=90s`
      + 앵커 4판 + §T 부하기 코어 팔 12판 = **본 측정 72판 · 총 85판**

> 팔 정의 — **A** 옆이 유휴 / **B** 옆이 일함(ghz 부하) / **C** B + **CPU 캡**(#212).
> A·B 는 구성이 같고 **갈리는 것은 ghz 부하 하나**다(#222 로 2026-08-16 재정의).

---

---

> # ⚠️ 2026-09-14 단서 — 아래 「346 RPS」(천장 환산 105세션)에는 박스 보정값이 없다
>
> 이 라운드는 박스 보정(축 0 `calibration` 단계, 2026-08-26 상설화) **이전**이다. 축 B 실측
> ([결과](../nonrepro-axisB-2026-09-14/README.md))이 같은 `c7i.4xlarge` 를 **stop→start 만 해도 처리량이 284.8~344.6 rps(+21.0%)로
> 갈린다**(부팅 안 CV ≤0.33% · 부하기·커밋·격자 고정)는 것을 보였고, 그 뒤 인용 규칙 ㉠
> ([§8](../../../docs/decisions/round-to-round-nonreproducibility.md#8--채택--인용-규칙--2026-09-14-사용자-결정))이 채택됐다.
>
> **→ 「346 RPS」(천장 환산 105세션)은 보정값 없음 — 라운드 간 ±10% 폭(축 B 실측) 안에서만 읽을 것.** 값이 틀린 게
> 아니다. 다른 라운드의 절대값과 나란히 놓고 «같다/다르다» 를 말할 근거(처리량비÷보정비)가 없을 뿐이고,
> 이 문서 안의 팔·레벨 간 비교(같은 부팅)는 이 단서와 무관하다.

## 0. 한 줄

**H3(「캡이 옆을 지키는가」)은 반증됐다.** 옆은 애초에 아프지 않았고, 캡을 걸어도 옆이
좋아지지 않으며, 캡의 대가는 **AI 처리량 −10% · AI 지연 3.1배**다.

---

## 1. 천장 — 동거 천장은 약 346 RPS, 캡을 걸면 312 RPS

| 세션 | A (유휴) | B (옆 일함) | C (B+캡) |
|---:|---:|---:|---:|
| 20 | 60.0 | 60.0 | 60.0 |
| 60 | 179.9 | 179.9 | 179.9 |
| 100 | 299.7 | 299.8 | 299.8 |
| **120** | **344.5** | **345.5** | **311.7** |
| **160** | **348.1** | **346.1** | **312.6** |

- 100세션까지는 세 팔 모두 **목표 RPS(3×세션)를 정확히** 낸다 — 천장이 아니다
- 120세션부터 갈린다. **A ≈ B ≈ 346** / **C ≈ 312**
- **옆이 일하는 것(A→B)은 천장을 거의 안 깎는다**: 348.1 → 346.1 (**−0.6%**)
- **캡은 깎는다**(B→C): 346.1 → 312.6 (**−9.7%**)

## 2. H3 — 캡이 «옆» 을 지키는가 → **아니다**

«옆» 은 ghz 가 때리는 Spring gRPC 경로다. #254 로 이번 라운드에 **처음으로** 이 지연을 걷었다
(08-16 두 라운드는 판정할 열 자체가 없어 H3 을 못 닫았다).

| 세션 | B p50 | C p50 | B p95 | C p95 | B p99 | C p99 | fail |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 20 | 8.8 | 9.0 | 10.2 | 10.5 | 11.1 | 11.3 | 0 |
| 60 | 8.7 | 8.5 | 10.0 | 10.3 | 11.2 | 11.7 | 0 |
| 100 | 8.2 | 8.5 | 9.8 | 10.2 | 10.9 | 11.7 | 0 |
| 120 | 8.3 | 8.5 | 9.5 | 9.9 | 10.7 | 12.5 | 0 |
| 160 | 8.3 | 8.6 | 9.6 | 10.2 | 10.7 | **12.7** | 0 |

**셋이 한꺼번에 읽힌다:**

1. **옆은 처음부터 안 아팠다.** AI 세션이 20→160 으로 **8배**가 되는 동안 옆의 p50 은
   8.2~8.8ms 로 **평평**하고 실패는 0이다. 지켜야 할 피해가 관측되지 않았다
2. **캡을 걸어도 옆이 좋아지지 않는다.** 오히려 전 구간에서 C 가 **미세하게 나쁘다**
   (160세션 p99 10.7 → 12.7)
3. **대가만 확실하다.** §1 의 처리량 −9.7% 에 더해, 100세션(둘 다 목표 RPS 달성 = 천장 아래)에서
   **AI 경로 p50 36.6 → 114.5ms (3.1배)** · p99 109.1 → 233.3ms

→ **H3 반증.** [#212](https://github.com/Shadowfit/init/issues/212)(컨테이너 CPU 한도 부재)의
전제 — 「캡이 없어서 서로 코어를 뺏는다」 — 가 **이 부하 수준에서는 성립하지 않는다.**

### 2-0. 캡은 실제로 물렸다 — 그리고 깎은 양이 아주 작다

`caps.tsv` 가 팔 C 의 적용값을 남겼다(#253 의 단언이 통과했다):

| 컨테이너 | NanoCpus | = |
|---|---:|---:|
| `shadowfit-ai` | 8,000,000,000 | **8 vCPU** |
| `shadowfit-backend` | 4,000,000,000 | 4 vCPU |
| `shadowfit-mysql` | 4,000,000,000 | 4 vCPU |

160세션에서 AI 컨테이너가 실제로 쓴 CPU(`stats_*`, 3판):

| 팔 | 평균 | **피크** |
|---|---:|---:|
| B (캡 없음) | 644.5 / 673.2 / 695.4 % | **944.6 / 927.5 / 947.0 %** (≈ **9.3~9.5 vCPU**) |
| C (캡 8) | 584.8 / 600.9 / 604.2 % | **829.0 / 830.4 / 829.6 %** (≈ **8.3 vCPU** — 캡에 딱 붙었다) |

🔴 **깎인 것은 약 1.2 vCPU 인데 처리량은 9.7% 빠졌다.** AI 는 캡 없이도 9.5 vCPU 에서 멈추지만,
그 위쪽 1.2 를 뺏으면 **거의 그대로 처리량이 준다** — 이 구간에서 AI 는 CPU-bound 다.

### 2-2. R6 과 붙이면 그림이 닫힌다

같은 날 같은 박스에서 [R6](../ai-scaling-aws-2026-08-17/README.md) 을 태웠다. **순수 추론만**
돌리면 이 박스는 **15.2 vCPU** 를 쓴다(16워커, CPU 피크 1517.6%). 그런데 **서비스 경로를 태우면
캡 없이도 9.5 에서 멈춘다**(위 표).

> **즉 AI 는 하드웨어가 모자라서 9.5 인 게 아니다.** R6 이 GIL·풀 크기·하드웨어를 전부 지웠으므로,
> 남은 것은 «추론 밖 · 동시성 하에서만 드러나는 직렬화» 다. **두 실험이 같은 자리를 가리킨다.**
> 무엇인지는 **아직 아무도 안 쟀다.**

### 2-3. §T — 「부하기가 천장」이 반증됐다. 346 은 **서버** 천장이다

08-16 라운드는 *"부하기가 천장"* 으로 끝났고, 08-17 리허설 회수 때도
*"「부하기가 병목이 아니다」는 아직 못 하는 말이다"* 로 남겨 뒀다(커밋 `cd5d9c6`).
**이제 할 수 있는 말이 됐다.**

부하기 코어를 **2개 ↔ 전체(4)** 로 흔든 12판(팔 B · 3쌍 · 레벨 120·160):

| 세션 | 부하기 2코어 | 부하기 전체 | 차이 |
|---:|---|---|---:|
| 120 | 345.4 / 345.6 / 346.1 | 346.2 / 346.8 / 347.0 | **0.3%** |
| 160 | 343.3 / 343.8 / 345.3 | 344.5 / 346.1 / 346.2 | **0.4%** |

지연도 같다(160세션 p50 456.8 ↔ 457.1). **부하기 코어를 절반 뺏어도 처리량이 안 변한다.**

확인 사살 — 부하기 자신의 CPU(`loader_*`, 160세션):

| 팔 | 평균 | 피크 | 상한 |
|---|---:|---:|---:|
| 2코어 | 47.0% | 102.5% | 200% |
| 전체 | 54.8% | 143.2% | 400% |

**2코어로 조여도 상한의 절반만 쓴다.** 포화 근처도 아니다.

→ **346 RPS 는 대상 박스(서버)의 천장이다.** 그리고 서버는 그 천장에서 **9.5 vCPU** 밖에 안 쓴다
(§2-0). 16코어 박스에서 **6코어가 논다.**

### 2-1. 🔴 이 판정의 범위를 좁혀서 읽을 것

- **옆의 부하가 약하다.** ghz 목표가 **19 RPS** 이고 달성도 19.0, 실패 0이다. 즉 «옆이 일한다» 는
  조건이 **옆을 아프게 할 만큼 세지 않았을 수 있다.** 「옆이 안 아팠다」는 **이 부하에서**의 사실이다
- **캡 값을 바꿔가며 재지 않았다.** 판정은 `AI=8 vCPU` **한 점**에서 나온 것이다(§2-0).
  캡을 9~10 으로 느슨하게 잡으면 −9.7% 가 줄어들 여지가 있고, 그건 안 재봤다.
  ⚠️ `AI_CPUS` 는 `MANIFEST.txt` 에 **없다** — 원본은 `coresidency/caps.tsv` 다
- **A 는 «AI 단독» 이 아니다**(#222). Spring·MySQL 이 같이 떠 있고 유휴일 뿐이라,
  A↔B 차이는 «서비스 경로 오버헤드» 가 아니라 «옆이 일하는 비용» 하나다

---

## 3. 회수 범위 — ghz JSON 3.9GB 는 S3 에 둔다

원본: **`s3://shadowfit-measure-055447613012/shadowfit/ec2-20260817-050448`** (794객체 · 4.2GB)

| | 크기 | 처리 |
|---|---:|---|
| `ghz_*.json` (152개) | **3.9GB** | 🔴 **S3 에만.** 파일당 51.7MB(ghz 전건 기록) |
| `req_*.tsv` (208개) · `side*` (2개) | **91MB** | 🔴 **S3 에만.** 요청 단위 원시 로그 |
| 판정에 쓰는 표 전부 | 약 4MB | **이 디렉터리** |

여기 있는 것: `coresidency.tsv` · `ghz.tsv` · `caps.tsv` · `stats_*` · `loader_*` · `probe_*` ·
`req_*_summary.tsv` · `MANIFEST.txt` · `phases.tsv` · `run_all.log`.

⚠️ **이전 라운드는 `req_*` 를 커밋했다**(그땐 전체가 841KB 였다). 이번엔 같은 파일이 63MB 라
관례를 깼다 — **판정에 쓰는 표는 전부 여기 있고**, 요청 단위 원시가 필요하면 위 S3 URI 를 볼 것.
🔴 **버킷이 지워지면 그 원시는 사라진다.**

---

## 4. 정직하게 비어 있는 것

- **자기 프로브(`probe_*`)를 안 읽었다.** 대상 박스에서 1세션을 따로 돌린 계열인데 이 문서는
  안 봤다. 회수는 돼 있다
- **앵커 4판을 안 읽었다** — 라운드 내 시간 추세를 보는 판이다
- **절대값 비재현(+17.7%)** 은 이 라운드로도 안 풀렸다. 재고표의 AI 4순위 항목 그대로다

---

## 5. 이 결과가 움직이는 것

- [`../../../docs/decisions/experiment-inventory.md`](../../../docs/decisions/experiment-inventory.md)
  Spring 1순위 **H3 닫힘(반증)**
- [#212](https://github.com/Shadowfit/init/issues/212) — 전제가 흔들린다. **닫자는 게 아니라**,
  「캡이 필요하다」의 근거를 이 라운드가 지웠으므로 다시 세워야 한다는 뜻이다
- [#254](https://github.com/Shadowfit/init/issues/254) — 지연 열이 실제로 판정을 냈다. **닫힘**
- [#250](https://github.com/Shadowfit/init/issues/250) · [#251](https://github.com/Shadowfit/init/issues/251)
  — §T 가 **「부하기가 천장」을 반증**했다(§2-3). 08-16 의 판정이 뒤집힌다
- [`./coresidency/ghz.tsv`](coresidency/ghz.tsv) — 옆의 부하가 19 RPS 였다는 사실이
  다음 라운드 설계의 입력이다(더 세게 때려야 H3 을 «세게» 반증할 수 있다)
- 🔴 **새 항목** — 「346 RPS 천장에서 서버가 9.5 vCPU 만 쓰는 이유」. 오늘 후보 다섯 중
  **넷이 지워졌다**(GIL · 하드웨어 · 검출기 풀 · **부하기**). 남은 것은 **AI 프로세스 안의
  동시성 상한**(FastAPI sync 엔드포인트의 anyio 워커 풀 기본 40 — 08-16 의 «40 in-flight» 와
  숫자가 같다)과 **락**([#162](https://github.com/Shadowfit/init/issues/162)) 둘이다.
  ⚠️ 둘 다 **아직 하나도 안 쟀다** — 이 줄은 가설이지 판정이 아니다
