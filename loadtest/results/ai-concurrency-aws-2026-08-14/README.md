# AI 동시 세션 — 물리 8코어 실측 (2026-08-14)

선행: [`../ai-concurrency-2026-08-09/`](../ai-concurrency-2026-08-09/README.md)(i3-6100, 물리 2코어) ·
[`../ai-recalibrate-2026-08-11/`](../ai-recalibrate-2026-08-11/)(c7i.2xlarge, 물리 4코어) ·
설계 [`../../../docs/decisions/load-test-strategy.md`](../../../docs/decisions/load-test-strategy.md) §1

**08-11 정정이 열어둔 질문을 닫는다** — 그 문서는 *「물리 4코어의 상한 65.5 인데 가정 피크는
67 이라 상한조차 못 미친다. 실제 배치는 물리 8코어급을 봐야 한다」* 로 끝났다. 그 8코어다.

rig: `loadtest/measure_ai_concurrency.py` (수정 없음 — 스윕이 적응형이라 코어 수에 맞춰 늘어난다)

---

> # ⚠️ 2026-09-14 단서 — 아래 「156세션」에는 박스 보정값이 없다
>
> 이 라운드는 박스 보정(축 0 `calibration` 단계, 2026-08-26 상설화) **이전**이다. 축 B 실측
> ([결과](../nonrepro-axisB-2026-09-14/README.md))이 같은 `c7i.4xlarge` 를 **stop→start 만 해도 처리량이 284.8~344.6 rps(+21.0%)로
> 갈린다**(부팅 안 CV ≤0.33% · 부하기·커밋·격자 고정)는 것을 보였고, 그 뒤 인용 규칙 ㉠
> ([§8](../../../docs/decisions/round-to-round-nonreproducibility.md#8--채택--인용-규칙--2026-09-14-사용자-결정))이 채택됐다.
>
> **→ 「156세션」은 보정값 없음 — 라운드 간 ±10% 폭(축 B 실측) 안에서만 읽을 것.** 값이 틀린 게
> 아니다. 다른 라운드의 절대값과 나란히 놓고 «같다/다르다» 를 말할 근거(처리량비÷보정비)가 없을 뿐이고,
> 이 문서 안의 팔·레벨 간 비교(같은 부팅)는 이 단서와 무관하다.

## 0. 판정

**물리 8코어에서 동시 156세션. 가정 피크 67 을 넘어선다 — 상한 미달이 해소됐다.**

> 🔴 **인용 조건 (2026-08-23 추가).** 이 줄과 아래 표는 «**2.3배 여유**» 로 적혀 있었다.
> **156 은 이 라운드가 잰 값으로 맞다** — 단 이 rig 은 검출기를 in-process 로 부르고
> (`measure_ai_concurrency.py`) **HTTP 도 동거도 없다.** 배포 구성은 MySQL·Spring 이 같은
> 호스트에 살고 프레임은 HTTP 로 온다. 그 조건의 실측 천장은 **89~105세션**(P6 1·2라운드)이라
> **여유는 1.33~1.57배**다. 「2.3배」를 배포 여유로 인용하면 안 된다 — [#385](https://github.com/Shadowfit/init/issues/385) ·
> 계산 [`ai-receive-path-scaling.md` §3-ㄹ-1](../../../docs/decisions/ai-receive-path-scaling.md).

| | i3-6100 (08-09) | c7i.2xlarge (08-11) | **c7i.4xlarge (08-14)** |
|---|---|---|---|
| 물리 코어 | 2 | 4 | **8** |
| 프레임당 추론 | 103.4 ms† | 17.6 ms | **15.0 ms** |
| 포화 처리량 | 18.3 fps | — | **468.3 fps** |
| **동시 세션(3fps)** | **6** | **65.5** | **156.1** |
| 가정 피크 67 대비 | 11배 부족 | **상한조차 미달** | **2.3배** ⚠️ 단독 박스 기준 · 배포 구성은 **1.33~1.57배**([#385](https://github.com/Shadowfit/init/issues/385)) |

† 08-09 은 Docker Desktop 경유라 절대값 인용 금지 조건이 붙어 있다.

**코어 2배(4→8)에 세션 2.38배(65.5→156.1).** 약간 초선형인데 **기전은 규명하지 않았다** —
아래 [C] 가 보여주듯 HT 구간의 기여가 코어가 많을수록 커지는 것으로 보이지만, 인스턴스 크기에
따른 터보·L3 차이도 섞여 있어 가르지 않았다. **「선형 이상으로 붙는다」까지만 말한다.**

## 1. [C] 스레드 스케일 — 로컬의 「1.73배 천장」은 알고리즘이 아니었다

| 스레드 | 총 처리량 | 판별 최소~최대 | 스레드당 평균 | 1스레드 대비 |
|---|---|---|---|---|
| 1 | 55.7 fps | 55.7~55.9 | 14.8 ms | 1.00x |
| 2 | 104.8 | 102.6~107.6 | 15.6 | 1.88x |
| **4** | **184.4** | 184.2~184.6 | 17.5 | **3.31x** |
| **8** | **325.9** | 320.4~326.4 | 19.9 | **5.85x** ← 물리 코어 |
| 16 | 451.8 | 450.7~454.7 | 28.3 | 8.11x ← HT |
| 32 | 468.3 | 464.0~468.9 | 55.1 | 8.41x ← 포화 |

🔴 **같은 4스레드가 로컬에선 1.73x, 여기선 3.31x 다.** 로컬 i3-6100 은 **물리 2코어에 4스레드**를
밀어넣은 초과 구독 상태였다 — 그 1.73배는 **알고리즘의 천장이 아니라 그 박스의 코어 수**였다.
08-09 문서가 「물리 2코어 + HT 가 그대로 드러난다」로 읽은 것이 맞았고, 이제 대조군이 생겼다.

- **물리 코어 구간(1→8)**: 5.85x — 완전 선형(8x)은 아니고 **73% 효율**
- **HT 구간(8→16)**: +38.6%. 로컬과 달리 **HT 가 실제로 값을 한다**
- **초과 구독(16→32)**: +3.7%. 포화. 스레드당 지연만 28.3 → 55.1ms 로 나빠진다

→ **운영 스레드 수는 16(=논리 코어)가 적정**이다. 32 는 처리량을 거의 안 늘리면서 지연을 2배로 만든다.

## 2. 🔴 #164 는 하드웨어 탓이 아니었다 — 다른 CPU 에서 **같은 45%**

```
[B] 다른 사람 번갈아 → 검출률 11/20
    각 «사람» 은 단독 검출 100% 임을 매 실행 전제 확인에서 단언
```

**i3-6100 과 정확히 같은 11/20 이다.** 검출기가 스레드 로컬(`mediapipe_detector.py:107`)인데
FastAPI 가 스레드풀로 돌려(`pose.py:52`) 동시 세션이 한 스레드를 공유하면 서로의 트래킹을
깨뜨린다 — **설계에서 나오는 값이라 CPU 를 바꿔도 그대로**라는 것이 이번에 확인됐다.

추론 비용도 **1.30x** 로 오른다(15.0 → 19.4ms). 로컬은 1.59x 였으니 **비율은 CPU 를 탄다.**

⚠️ **그래서 §0 의 156세션은 상한이다.** [C] 는 *세션이 스레드에 1:1로 붙는 가장 유리한 배치*로
쟀고, 실제 FastAPI 스레드풀은 [B] 의 상황에 가깝다. **#164 를 고치지 않으면 실제 수용량은
156 보다 한참 아래**고, 그 격차가 얼마인지는 **안 쟀다.**

## 3. 측정 조건

| 항목 | 값 |
|---|---|
| 인스턴스 | **c7i.4xlarge** · 16 vCPU / **물리 8코어** · `i-0f7ecfb0f145e5768` |
| CPU | **Intel Xeon Platinum 8488C** (Sapphire Rapids) |
| 실행 | ai-server 이미지(`Dockerfile`) 안에서 — 의존성을 운영과 같게 |
| 라이브러리 | mediapipe **0.10.21** · opencv **4.11.0** · numpy **1.26.4** |
| 커밋 | `a377e41` (`work/2026-08-13`) |
| 판 배치 | 스윕 `[1,2,4,8,16,32]` · **3판 + 버림판 1** · 판마다 **순서를 뒤집는다** |
| 소요 | 부트스트랩·이미지 빌드 포함 **3분 10초** |

판 순서를 뒤집는 이유는 rig 주석에 있다 — 오름차순 1판씩이면 「스레드 수」와 「판 순서」
(터보·써멀 드리프트)가 안 갈린다([[feedback_measure_design_needs_repeats]] 와 같은 규약).

## 4. 미검증 · 한계

- 🔴 **156세션은 순수 추론의 상한이다.** HTTP·디코딩·GIL·같은 박스의 다른 컨테이너를 안 뺐고,
  무엇보다 **#164 의 손실이 안 들어가 있다**
- **GPU 는 안 봤다.** 이 측정은 전 구간 CPU(TFLite XNNPACK delegate)다. GPU 로 무엇이 달라지는지는
  **재지 않았으므로 말할 수 없다**
- **합성 인체 프레임**이다. 프레임당 비용은 고정 해상도 CNN 이라 내용과 무관하지만, 검출 성공
  여부는 내용을 타므로 매 실행 전제 확인으로 단언한다(rig 주석)
- **클라 3fps 가정**은 `exercise.tsx:150`(`intervalMs=330`)·`session_state.py:47`(0.300s 상한)에서 온다.
  이 값이 바뀌면 세션 수가 그대로 반비례로 움직인다
- **물리 16코어 이상은 안 봤다.** 8→16 이 또 선형일지는 이 데이터로 말할 수 없다

## 결정 로그

- **2026-08-14: 08-11 이 열어둔 「물리 8코어급」 질문이 닫혔다.** 동시 **156세션**, 가정 피크
  67 의 **2.3배 여유**. **현 시점 수평확장 근거는 없다** — 한 대로 선다.
- **2026-08-23: 위 「2.3배 여유」에 조건을 붙인다** ([#385](https://github.com/Shadowfit/init/issues/385)). **156 은 이 라운드 값으로
  맞는데**, 그건 **AI 단독 박스 · in-process · HTTP 없음**이다. 배포 구성의 실측 천장은
  **89~105세션**이라 여유는 **1.33~1.57배**다. 🔴 **「수평확장 근거는 없다」는 안 뒤집힌다**
  (67 < 89) — 뒤집히는 것은 **여유의 크기**이고, 그게 「고칠 필요 없다」와 「지금 가정에서만
  필요 없다」를 가른다. 세션 길이가 30분이면 DAU 659 에서 닿는다.
- **2026-08-14: 로컬의 「1.73배 천장」이 장비 탓임이 대조군으로 확정됐다.** 같은 4스레드가
  3.31x. 알고리즘이 아니라 코어 수였다.
- **2026-08-14: #164 가 하드웨어 독립임이 확인됐다.** 다른 CPU·다른 세대에서 **같은 11/20**.
  고치기 전까지 §0 의 156 은 **상한으로만** 인용한다.
