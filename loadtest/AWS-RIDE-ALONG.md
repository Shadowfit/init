# EC2 탑승 목록 — 인프라를 띄울 때 같이 돌릴 것

작성일: 2026-08-12
상태: **5회 탑승 완료 (P1 2026-08-12 · P3 2026-08-13 · P3-b 2026-08-13~14 · P6 2026-08-16 · P6 2라운드 2026-08-17) + 팔 B 교정 1판 (2026-08-14)**
      — 🔴 **정정 2026-08-22**: 이 줄이 「남은 主 는 **P2·P4·P5**」로 서 있었는데 **P2·P5 는 2026-08-18 에 닫혔다**
      ([P5](results/session-spread-aws-2026-08-17/README.md) · [P2](results/session-spread-aws-2026-08-17/P2-downsample-multisession.md)).
      **남은 主 는 P4 의 Q1·Q2 뿐**이고, 그 **2대 rig 은 2026-08-22 에 섰다**
      ([REPL2-RIG.md](results/replication-2026-08-17/REPL2-RIG.md)) — 🔴 **한 판도 안 돌았고
      2대 절차를 실제로 밟아본 적이 없다.** 착수는 여전히 미결정(§7).
      ✅ **2026-08-17 라운드에서 H3 이 닫혔다(반증)** — [결과](results/coresidency-aws-2026-08-17/README.md).
      #254 의 판정 열이 처음으로 생겨 판정이 났고, 옆은 애초에 안 아팠다.
      같은 박스 terminate 전 15분에 **從 R6·R7 도 회수했다**([결과](results/ai-scaling-aws-2026-08-17/README.md)) —
      **GIL 반증**, c7i 배수는 로컬과 달랐다.
      🔴 **열린 채로 남은 것 둘**: 「라운드 간 절대값 비재현(+17.7%)」과
      **「346 RPS 천장에서 서버가 9.5 vCPU 만 쓰는 이유」**(후보 일곱이 지워지고 둘 남음 ·
      [분석](../docs/decisions/ai-receive-path-scaling.md)).
      나머지는 착수·채택 모두 사용자 confirm 후 박제
      · 재고 전체: [`../docs/decisions/experiment-inventory.md`](../docs/decisions/experiment-inventory.md)
      🔴 **2026-08-17 게이트 라운드 추가**(1대 `c7i.xlarge`, [결과](results/payload-uniqueness-gate-aws-2026-08-17/README.md)) —
      P5 를 태우기 전에 rig 을 시험한 판이다. **rig 이 네 번 막혔고**(컨테이너 이름 · 자격증명 ·
      ghz 경로 · MySQL 헬스 대기 경쟁) 전부 «진짜 원인과 다른 곳» 을 가리켰다. §4 준비 목록이
      **환경만** 보고 있었다는 뜻이라, 「보낸 요청이 실제로 행을 만드는가」를 목록에 넣었다.
      🆕 **2026-08-28 R15·R16 추가** — AI 워커 부하-중 장애 빈도를 가속 스트레스로 재려다
      **대상 박스 전체가 두 번 다 응답 불능**에 빠졌다(MySQL 캡을 걸어도 재현). 원래 판정
      채널은 못 봤지만 MySQL 락 정체 가설은 반박됐고, EBS I/O 크레딧 소진이 새 후보로
      [#603](https://github.com/Shadowfit/init/issues/603)에 열려 있다 — 아래 R15·R16.
      ✅ **2026-09-02 — 새 라운드 없이 CloudWatch 사후 조회로 닫혔다**
      ([R15/R16 결과 §6-7](results/ai-worker-load-soak-2026-08-28/README.md#6-7-ebs-처리량-상한--cloudwatch-사후-확인-2026-09-02-추가)) —
      두 사고 모두 EBS 볼륨이 gp3 기본 처리량 상한(125MiB/s)에 그대로 눌러붙어 있었다. 인과
      방향(상한이 원인 vs 결과)은 여전히 안 갈렸다.
연관: [`../docs/decisions/online-ddl-vs-blocking-alter.md`](../docs/decisions/online-ddl-vs-blocking-alter.md), [`../docs/portfolio/one-pager.md`](../docs/portfolio/one-pager.md), [`results/`](results/)

---

## 0. 이 문서가 있는 이유

**2026-08-08 에 한 번 놓쳤다.**

격자 재측정으로 EC2 3대를 띄웠고 `pose_data` 375만 행까지 시딩된 상태였는데, *"다시 띄울 일이
있으면 같은 쿼리를 한 번 돌려보는 것으로 충분하다"* 고 **문서에 적어둔 그 일이 실제로 생겼는데도**
안 돌리고 인프라를 삭제했다 ([`worst-section-rep-resolution.md:261`](../docs/decisions/worst-section-rep-resolution.md)).

원인은 게으름이 아니라 **목록이 흩어져 있었다는 것**이다. "다음에 인프라 띄울 때"는 서너 개
문서에 각자 적혀 있었고, 인프라를 띄운 사람은 그 문서들을 열 이유가 없었다.

이 파일이 그 한 곳이다. 그리고 이 프로젝트의 EC2 는 **항상 «임시 생성 → 측정 → 삭제»** 이므로
([`schema-migration-tracking.md:80`](../docs/decisions/schema-migration-tracking.md)),
실질적인 방어는 하나뿐이다 — **삭제 전에 §5 를 여는 것.**

---

## 1. 탑승 목록

승객은 두 등급이다. **主**는 그것 때문에 인스턴스를 띄우는 항목, **從**은 인프라가 이미 떠
있을 때만 값이 생기는 항목(단독으로는 EC2 비용을 정당화하지 못한다).

### 主 — 이것 때문에 띄운다

| # | 항목 | 왜 AWS 여야 하나 | 준비 상태 | 소요 |
|---|---|---|---|---|
| ~~**P1**~~ | ~~**무중단 DDL 본 측정**~~ | 로컬은 MySQL·writer·도구가 2코어를 공유해 팔 간 차이가 잡음에 묻힐 수 있다. 설계 §9 «AWS 로 올릴지» 미결정 항목 | ✅ **완료 (2026-08-12)** — [결과](results/online-ddl-aws-2026-08-12/README.md) | **실측 29분** (예상 ~5.9시간은 `WRITER_MAX_SEC` 상한 기준이었고, 1,000만 행에선 판당 1~2분) |
| ~~**P2**~~ | ~~**다운샘플 «1.7배» 다세션 재측정**~~ | 🔴 [`one-pager.md:43`](../docs/portfolio/one-pager.md) 의 정본 수치인데 조건이 **단일 핫세션**(`batch.json`, session 801)이다. **fsync 3.47배를 1.03배로 무너뜨린 바로 그 조건**이고, 문서에 «다세션에서 재측정한 적 없다» 가 그대로 붙어 있다 | ✅ **완료 (2026-08-18)** — [결과](results/session-spread-aws-2026-08-17/P2-downsample-multisession.md). **1.7배가 아니라 4.11배** · p99 9.5배. 🔴 저장 처리량으로 보면 방향이 **뒤집힌다**(0.82배). 막던 것이 풀린 경위 — `DOWNSAMPLE_WINDOW` 가 `private static final` 이라 팔을 바꾸려면 재빌드가 필요했고 **그 재빌드가 실험을 막고 있었다.** 설정으로 뺐다(`pose-data.downsample-window`, 기본 5라 동작 불변). rig `downsample_multisession.sh` — 팔 `w5`↔`w1`, ABBA 8판. 🔴 팔마다 **저장 행수가 다른 것이 조작 변수**라 `rows/s` 계산값을 팔별로 바꿔주지 않으면 결론이 통째로 뒤집힌다 | P5 라운드에 얹어 돌았다 · 재빌드 ~5분 + 스윕 ~25분 **(예상치)** — ⚠️ 실소요는 기록이 없다 |
| ~~**P3**~~ | ~~**백업/복구 RTO·RPO**~~ | 「몇 분인가」는 디스크 성능이 지배한다. 단 「PITR 이 되는가」는 이진 사실이라 **로컬에서 먼저** 확인하고 올린다 | ✅ **완료 (2026-08-13)** — [결과](results/backup-restore-aws-2026-08-13/README.md). 미검증 2건은 §6 결정 로그 | **실측 3.11시간**(러너 2.97h — 본 측정만 2h54m) |
| ~~**P3-b**~~ | ~~**백업 재측정 — 내구성·행 크기**~~ | 🔴 08-13 라운드가 두 곳에서 다른 것을 쟀다. ① 팔 B 복구가 **페이지 캐시**를 쟀고([#201](https://github.com/Shadowfit/init/issues/201)), ② 설계에서 확정됐던 **real-JSON 대조**가 rig 에 없었다([#202](https://github.com/Shadowfit/init/issues/202)). 둘 다 **디스크가 지배**해서 로컬로는 못 닫는다 | ✅ **완료 (2026-08-13~14)** — [b 라운드](results/backup-restore-aws-b-2026-08-13/README.md) · [팔 B 교정](results/restore-reflink-2026-08-14/README.md). #201 은 원인이 캐시가 **아니라 xfs reflink** 였고([#210](https://github.com/Shadowfit/init/issues/210)), #202 는 돌았는데 **예상과 방향이 반대**였다 | **실측 3.32시간**(러너 3.06h) + 교정 1판 |
| ~~**P4**~~ | ~~**복제 지연 · 반동기 대가**~~ | 인스턴스 2대가 전제. ⭐ **반동기 대가는 AZ 간 RTT 에 지배**되므로 로컬에선 구조적으로 과소평가된다 | 🟢 **설계·rig 완료 (설계 2026-08-13 · 2대 rig 2026-08-22)** — [설계](../docs/decisions/replication-lag-and-semisync.md) · [2대 rig](results/replication-2026-08-17/REPL2-RIG.md) · 러너 단계 `repl_preflight`·`repl_gate`·`repl`([2대 절차](aws/README.md)). 🔴 **「rig 없음」은 2026-08-22 에 정정됐다** — **로컬 rig 도 있다**([`results/replication-2026-08-17/`](results/replication-2026-08-17/README.md) · `replication_probe.sh` · `docker-compose.yml` · #289 · 커밋 `28fec8d`)이고 그 판으로 **Q4·Q5 는 닫혔다. 단 Q3 은 못 닫았다** — 계측이 현상보다 굵어 `SOURCE_DELAY=5` 로 만든 정확한 5초를 7~11.5초로 찍었고, 그것을 알려준 것이 팔 C(양성 대조군)다([결과](results/replication-2026-08-17/README.md)). **2대 rig 은 바로 그 실패를 첫 설계 조건으로 삼는다** — `lag_net = lag_rep − lag_src` 로 **계측 바닥을 같은 표에 세운다**. ✅ **완료 (착수 확정 2026-08-23 · 측정 2026-08-22)** — [결과](results/replication-aws-2026-08-22/README.md) · [설계 §0-3](../docs/decisions/replication-lag-and-semisync.md). `c7i.2xlarge` **2대 · 같은 AZ** · 1,000만 행 · **10판 전부 유효 · 결함 0**(산포 A 0.6% / B 0.2%). **반동기의 대가는 처리량 −5.9% · 커밋 p95 +13.0%** 이고, 🔴 **H3 이 조건을 만들었다** — 같은 대가가 부하 모양에 따라 **−5.9% ↔ −10.3%** 로 거의 두 배가 된다. 지연은 팔 A 약 60초 · 팔 B 약 57초. 🟢 **급소였던 G3 이 최대 소득** — `lag_net` 이 세 번 통과했고 굵기 **+1.017~1.018초가 무대 100배에도 안 변한다.** 그래서 08-17 이 Q3 을 잃은 원인이 «현상» 이 아니라 «계측» 이었음이 사후 확인됐다. 🔴 **인용 전에 조건 넷** — 같은 AZ(다른 AZ 미측정) · 앱 경로가 아닌 SQL writer(649 RPS 와 **같은 단위로 비교 불가**) · 계측 바닥 ~1초 · `main` 이 아닌 커밋(`e2fbe6c` — G2 결함 수정 [#380](https://github.com/Shadowfit/init/pull/380), 08-23 에 머지되어 해소). ⭐ **스모크 선행이 값을 했다** — 1회차가 G2 에서 막혔고, 1,000만 행 시딩을 마친 뒤에야 만났을 결함이었다 | ~~게이트 2~3h + 측정 2~3h **(예상)**~~ → **실측 러너 1.39h** · 인스턴스 가동 1.85h · **2대**. 🔴 `repl` 단계 80분 중 **절반 이상이 측정이 아니다** — 측정 30분 + **판 사이 따라잡기 50분**(판마다 리플리카를 다시 세우지 않기로 한 설계의 대가). 다음 견적엔 이 값을 쓸 것 |
| ~~**P5**~~ ⭐ | ~~**세션 분산도 스윕**~~ — 실측 격자는 설계의 `1·2·5·20·100` 이 아니라 **`1·2·5·10·20·50`** 이었다 | 🔴 정본 baseline **649.4 RPS 가 «100세션» 에서 나온 값**인데 그 100 은 잰 값이 아니다. 이 앱은 회원당 활성 세션이 1개라 **동시 세션 수 = 동시에 운동 중인 사람 수**다 — baseline 이 가정한 부하보다 분산된 조건일 수 있다. 4대 구성이라 로컬 불가 | ✅ **완료 (2026-08-18)** — [결과](results/session-spread-aws-2026-08-17/README.md). 🔴 **plateau 는 100이 아니라 10 에서 붙는다** — 10세션이 이미 plateau 의 **95%** 이고 10·20·50 사이에 유의한 차이가 없다. **정본 baseline 이 서 있던 「100세션」은 필요 이상이었다.** 기제까지 같이 잡혔다 — 락 대기가 곡선을 **그대로** 설명한다(레벨 1 은 요청 30,000건 중 **29,999건이 락 대기**, 레벨 50 은 **40건** = **750배** · 대기 시간으로는 **8,650배**). **36판 전부 유효**, 산포 0.4~0.9%. ⚠️ **옛 격자로는 이 자리를 못 봤다** — 5와 20 사이가 통째로 비어 있었고, 축소 리허설이 격자를 옮기게 했다. 같은 라운드에 [P2](results/session-spread-aws-2026-08-17/P2-downsample-multisession.md) 와 從 [R4](results/session-spread-aws-2026-08-17/R4-connection-sweep.md) 도 닫혔다 | ~~主 25판 + 從 6판~~ → **본 측정 36판 + 버림 1**(격자가 6레벨 × 6반복으로 바뀌었다) · **2대**(대상·부하기 모두 `c7i.xlarge`) · 라운드 창 **UTC 15:0x~16:1x**([결과 헤더](results/session-spread-aws-2026-08-17/README.md)) |
| ~~**P6**~~ ②차 | **동거 용량 — 2라운드 (부하기 4 vCPU)** | 1라운드가 남긴 셋 중 둘을 닫는다 — 부하기 계측([#250](https://github.com/Shadowfit/init/issues/250))과 팔 A 드리프트. 부하기를 `c7i.large`→`c7i.xlarge` 로 올리고 샘플러 11줄을 얹어 같은 격자를 다시 돌렸다 | ✅ **완료 (2026-08-16, 09:04~11:08 UTC)** — [결과](results/coresidency-aws-b-2026-08-16/README.md) (본 측정 54 관측 · 결함 0 · 라틴 방격). **닫힌 것**: H1(從 부하 비용 = **−0.5%**, 잡음 크기) · #250 계측 축(**부하기 CPU 는 천장이 아니다** — 4 vCPU 중 0.68) · R5 3회차 재현 · 캡 비율 91.1%→92.4% 재현. 🔴 **새로 연 것**: **같은 구성이 라운드를 건너 안 재현된다** — AI CPU 는 869.3%→869.0% 로 같은데 처리량 +17.7%(천장 환산 89.2→105.0세션) → [라운드 간 비재현](../docs/decisions/round-to-round-nonreproducibility.md)([#498](https://github.com/Shadowfit/init/issues/498)). 🔴 **이것과 별개로** 신규 결함 [#255](https://github.com/Shadowfit/init/issues/255)(부하기 샘플러 음수, CLOSED) | **2.13시간**(본 측정 7,387초) · **2대**(대상 c7i.4xlarge + 부하기 **c7i.xlarge**) |
| ~~**P6**~~ ①차 | **동거 용량** — 「156세션」은 혼자 살 때의 값 | 🔴 08-14 의 **156세션은 AI 컨테이너만 있는 박스** 값이다. 실제 배포는 MySQL·Spring 이 한 호스트에 산다. **동거하면 얼마로 내려가는지가 미측정**이고, 그 숫자가 「한 대로 되나 = 수평확장이 필요한가」를 가른다. 단독 대조군이 필요해 로컬 불가 | ✅ **완료 (2026-08-16)** — [정식 라운드 결과](results/coresidency-aws-2026-08-16/README.md) (6레벨 · 라틴 방격 · **본 측정 54 관측** · 결함 0) · [예비](results/coresidency-aws-2026-08-15/README.md) · [설계](../docs/decisions/ai-coresidency-capacity.md) · [rig](results/coresidency-2026-08-15/README.md) · [2대 구성 절차](aws/README.md). **닫힌 것**: Q2(뺏는 주체 = AI 자신) · **Q3 절반**(AI 쪽 — 캡한 만큼 깎는다, 팔 C 교락 해소) · Q4(검출률 57행 전부 100%) · 從 R5(메모리 실측식 ±0.7% 재현). **새 사실**: 16 vCPU 박스에서 천장이 ~8.9 vCPU — **HT 가 거의 안 준다**. 🔴 **남은 것 3건**: ① H1(동거 비용) — 팔 A 가 2시간에 +16% 흘러 판정 불가, ② **부하기 지표 이번에도 미수집**([#250](https://github.com/Shadowfit/init/issues/250)), ③ 천장이 「80 초과 120 미만」 구간으로만 짚힘. → **P6-b 로 간다** | **실측 2.29시간**(러너 2.22h — 본 측정만 7,408초 = 2h03m) · 12스윕 × 6레벨 · **2대**(대상 c7i.4xlarge + 부하기 c7i.large). 예상 ≈3.5시간보다 짧았다 |
| ~~**P6-b**~~ | ~~**동거 용량 2라운드**~~ — 못 닫은 셋을 닫는다 | 🔴 1라운드가 **H1·부하기 천장·H3** 을 못 닫았고, 그 뿌리 둘이 **판 설계 자체**에 있었다: 교대 단위가 «팔» 이라 팔 간 대조점이 **13~27분** 떨어져 있고 그 사이 처리량이 **분당 0.09~0.35%** 흐른다([#251](https://github.com/Shadowfit/init/issues/251)), 레벨 순서가 아홉 블록 전부 오름차순이라 추세가 **plateau 를 정의하는 자리**에 얹힌다([#252](https://github.com/Shadowfit/init/issues/252)). 거기에 캡 적용 단언 부재([#253](https://github.com/Shadowfit/init/issues/253))와 **H3 의 판정 열이 아예 생성되지 않는 문제**([#254](https://github.com/Shadowfit/init/issues/254)) | ✅ **완료 (2026-08-17) — 이 표가 오래 낡아 있었다.** 🔴 **2026-09-02 정정**: 이 줄이 "rig 코드는 아직 안 고쳤다"로 오래 서 있었는데 **사실이 아니었다** — ③④⑤⑥⑦⑧⑨⑩ 전부 2026-08-17 에 코드 반영됐고(①②만 "드리프트 재발 시 되살림"으로 의도적 보류), **같은 날 정식 2라운드가 실제로 돌았다**([결과](results/coresidency-aws-2026-08-17/README.md) · [설계 §11](../docs/decisions/ai-coresidency-capacity.md)). `ARMS='A B C'` × `LEVELS='20 40 60 80 90 100 120 160'` × `REPEATS=3` + 앵커 4 + §T 12 = **본 측정 72판·총 85판·3h33m**, 게이트 6종 전부 통과. **H1 반증**(동거 비용은 잡음 크기) · **부하기 천장 반증**(346 RPS 는 서버 천장, §T 로 확인) · **H3 반증**(캡은 지킬 것 없는데 처리량 −9.7%·AI 지연 3.1배 대가만 낸다) — 셋 다 닫혔다. `results/coresidency-aws-b-2026-08-16/`(아래 이 표의 원래 서술)는 이 정식 2라운드 **이전의 예비 관찰**로 재분류된다 | **실측 3h33m** · **2대**(대상 `c7i.4xlarge` + 부하기 `c7i.xlarge`) · RUN_ID `ec2-20260817-050448` |

> P2 가 눈에 띈다 — **이미 한 번 헤드라인을 무효화한 것과 같은 계열의 조건**이 아직 정본에
> 열린 채로 남아 있다. P1 과 같은 라운드에 태울 수 있으면 가장 싸다(단 §7 오염 주의).

### 從 — 인프라가 살아 있을 때만 (추가 비용 거의 0)

| # | 항목 | 내용 | 근거 |
|---|---|---|---|
| **R1** | **worst-section 쿼리 1회** | 🟡 **돌았지만 이 무대에서는 답이 안 났다 (2026-08-12·08-13 두 라운드)** — [`R1_worst_section.txt`](results/backup-restore-aws-2026-08-13/ridealong/R1_worst_section.txt): `reports`·`detailed_analysis`·`pose_data`·`exercise_sessions` 전부 **0행.** 측정용 박스는 MySQL만 띄우고 앱 트래픽이 없어 당연한 결과이지 원래 질문(EC2 배포분에 `detailed_analysis` 채워진 행이 있는지)의 답이 아니다 — **완료로 닫지 않는다.** 앱 트래픽이 실제로 도는 무대가 다시 필요하다 | [`worst-section-rep-resolution.md:261`](../docs/decisions/worst-section-rep-resolution.md) — §0 의 그 항목 · [#371](https://github.com/Shadowfit/init/issues/371) |
| ~~**R2**~~ | ~~**MySQL 지표 수집**~~ | ✅ **완료 (2026-08-12·08-13 두 라운드에서 걷혔다)** — [`R2_global_status.txt`·`R2_global_variables.txt`·`R2_top_digest.txt`](results/online-ddl-aws-2026-08-12/ridealong/). pool-cliff 초판이 «병목이 백엔드 CPU 로 이동» 을 적었다 **철회**한 사유가 정확히 **MySQL 지표 미수집**이었다 — 이번엔 처음부터 걷혔다(OS 샘플러와 같은 결). ⚠️ 08-13 산출물은 `-p$PW` stderr 경고가 섞여 TSV가 깨졌다는 곁가지가 남아 있다(별건) | [`pose-ingest-downsampling.md:387`](../docs/decisions/pose-ingest-downsampling.md) · [#371](https://github.com/Shadowfit/init/issues/371) |
| **R3** | **3-way 조인 hash join** | `reports ⋈ sessions ⋈ users`. ⑤ 옵티마이저 카드가 «AWS엔 그 테이블들이 비어 있어 범위 밖» 으로 닫아둔 미완부 | [`realmysql-experiments.md:225`](../docs/portfolio/realmysql-experiments.md) |
| ~~**R5**~~ | ~~**AI 메모리 유도식의 «공식 밖 몫»**~~ ✅ **완료 (2026-08-15 예비 라운드)** — 실측 `708 + 106.74×N` MiB, 공식보다 고정 +612·세션당 +12.6 크다. **공식은 212세션을 허용하는데 실측은 181에서 한도**([결과 §2-4](results/coresidency-aws-2026-08-15/README.md)) | 검출기 풀 공식이 **검출기만** 센다 — 프레임 버퍼·파이썬 힙은 미측정이라 안 들어갔고, 딱 맞추면 **풀이 거절하기 전에 OOM** 이 온다. 동시 세션을 ramp 하며 RSS 를 재야 «여유분» 에 근거가 생긴다. ⚠️ **P6 라운드 전용** — 그 스윕이 이미 `docker stats` 를 걷으므로 추가 비용 거의 0 | [#229](https://github.com/Shadowfit/init/issues/229) · [`ai-coresidency-capacity.md` §4-1](../docs/decisions/ai-coresidency-capacity.md) |
| ~~**R6**~~ | ~~**스레드 vs 프로세스 확장성**~~ ✅ **완료 (2026-08-17) · GIL 반증** — 프로세스/스레드 **0.96~1.06x**(판정선 1.7~2배) · 스레드도 16워커 **7.08배** · 순수 추론은 **15.2 vCPU** 까지 간다([결과](results/ai-scaling-aws-2026-08-17/README.md)) | P6 2라운드에서 **40스레드가 in-flight 인데 AI CPU 는 8.69 vCPU** 였다 — 코어가 남는데 31개가 막혀 있었다. 로컬 프로파일이 **추론 밖 직렬 비용은 5% 뿐**임을 확인해([결과 §1](results/ai-path-profile-2026-08-17/README.md)) 「서비스 경로가 비싸다」가 **반증**됐고, 남은 유력 가설이 **GIL** 이다(프레임당 3.17ms 를 넣으면 처리량 315·CPU 8.69 가 **동시에** 맞는다 — [§2-1](results/ai-path-profile-2026-08-17/README.md)). ⚠️ **로컬 물리 2코어로는 구조적으로 못 잰다** — 4워커에서 스레드와 프로세스가 같은 천장에 붙는다(코어가 먼저 걸린다). **판정선**: 프로세스 2개에서 처리량 **1.7~2배** + CPU **8.7→14~16** 이면 GIL, 아니면 캐시·HT 쪽. rig 은 `profile_e2e_and_scaling.py scaling` 하나, 워커만 1·2·4·8·16 으로 넓히면 된다 | [결과 §6](results/ai-path-profile-2026-08-17/README.md) · [P6 2라운드 §4](results/coresidency-aws-b-2026-08-16/README.md) · ⚠️ **소요 ~10분** · 도커·시딩·MySQL 불필요(venv + `frames.json` 만) |
| ~~**R7**~~ | ~~**`model_complexity` 0/1/2 배수 재측정**~~ ✅ **완료 (2026-08-17)** — c7i 는 **lite 0.79x · heavy 2.76x**(로컬 0.64x·3.83x, 방향만 같다). 🔴 덤: **해상도 9배 축소가 추론 시간을 안 바꾼다**([결과](results/ai-scaling-aws-2026-08-17/README.md)) | 로컬에서 **lite = 0.64×(추론 −36%) · heavy = 3.83×** 가 나왔다. 지금 유일하게 «바로 쓸 수 있는» 처리량 레버인데 **c7i 는 AVX-512·XNNPACK 이 달라 배수가 다를 수 있다**(방향은 동일). ⚠️ 채택은 별건이다 — 대가가 정확도라 [#234](https://github.com/Shadowfit/init/issues/234)(정답지 흔들림 → 점수 영향)와 같은 자다 | [결과 §4](results/ai-path-profile-2026-08-17/README.md) · ⚠️ **소요 ~5분** · R6 과 같은 박스·같은 자산 |
| ~~**R8**~~ | ~~**멱등 유니크 키의 쓰기 대가**~~ ([#272](https://github.com/Shadowfit/init/issues/272)) ✅ **완료 (2026-08-18)** — [결과](results/session-spread-aws-2026-08-17/R8-unique-index-cost.md). **처리량 −2.9% · p99 +98% · 버퍼풀 읽기 +17.5%.** 🔴 대가가 평균이 아니라 **꼬리**에 있다 — 「3% 느려진다」로 인용하면 안 된다. ⚠️ 디스크 읽기는 **양쪽 다 0** 이라 이건 «메모리 안의 대가» 다 | 마이그레이션이 **스스로** «이 스키마·이 박스에서 그 크기는 아직 측정된 적이 없다» 라고 적어둔 자리다(`V6__…:62~65`). 지금 급한 이유는 따로 있다 — 정본 baseline **649.4 RPS 는 이 키가 없던 스키마** 값이라, P5 곡선을 그려도 「4차보다 높다/낮다」를 못 말한다. 이 판이 그 단절의 **크기**를 준다. 팔은 **인덱스 존재** 하나(`ADD`↔`DROP INDEX`)이고, 페이로드가 템플릿이라 양쪽이 **같은 행을 넣는다** — 즉 «중복이 안 걸려 빨라진» 것이 아니라 **인덱스 유지 비용만** 빠진다. 기제는 `Innodb_buffer_pool_reads` 와 `Innodb_ibuf_*` 로 직접 센다(주장이 «change buffer 를 못 쓴다» 이므로). ABBA/BAAB 위치 균형 8판 + 버림 1 · rig `uk_index_ridealong.sh` | **실측 ~30분** · P5 라운드에 얹어 돌았다(2026-08-17 결정 → 2026-08-18 실행). 스키마를 만지므로 **본 스윕 뒤**에 돌렸고, `trap` 이 키를 되돌렸다 |
| ~~**R4**~~ | ~~**커넥션 수 스윕 (네트워크 축)**~~ ✅ **완료 (2026-08-18, P5 라운드 從)** — [결과](results/session-spread-aws-2026-08-17/R4-connection-sweep.md). **㉡(fsync 에 가려져 있었다) 반증 · ㉠(HTTP/2 멀티플렉싱이 실제로 작동한다)이 처음으로 근거를 얻었다** | 내구성을 완화해 **fsync 를 10배 걷어낸 조건에서도** 커넥션 1→16 이 처리량을 **안 올린다** — 커넥션 수는 «가려져 있던 게 아니라 **원래 레버가 아니었다**». 커넥션 3수준(1·4·16) × 내구성 2수준 · 위치 균형 6판 + 버림 1. 🔴 **덤으로 열린 것**: 그 완화 조건이 처리량 **−15%** · p99 **3.5배**(315 → 1,087ms)를 냈다 — 재고표 §1 의 «내구성 완화가 왜 «더 느린가»» 가 이 판에서 나왔다 | [`four-axes-depth-experiments.md` §3-2](../docs/decisions/four-axes-depth-experiments.md) · rig `conn_ridealong.sh` |
| **R9** | **관측 스택 동거 비용 — AI 부하 기준** (Q5 · 가설 H5) | 설계의 **팔 D**(= 팔 C + `--profile obs`)를 격자에 한 팔로 얹는다. 🔴 **P6-b 전용이다** — 다른 라운드에는 **AI 무대가 없다**(P4 는 두 대 다 MySQL 이고 부하기·AI 컨테이너·`frames.json` 이 전부 없다). 08-18 세션분산 라운드가 **쓰기 경로 기준**으로 대체 관측했지만(처리량 차 **−0.05%** · 스택 CPU 1% 미만) **두 부하는 병목이 다르다** — AI 부하는 CPU 포화 조건이라 «스택이 쓰는 1%» 가 처리량에서 그대로 빠질 수 있다. **판정선은 설계 그대로**: D 가 C 대비 **두 자릿수 % 내려가면 H5 반증**. ⚠️ **從 중 유일하게 «추가 비용 거의 0» 이 아니다** — 판이 **19개(+42분)** 는다. P6-b(08-17)도 D 없이 끝나 세 라운드째 밀린 채다. 🟢 **2026-09-02 — 다음 P6 류 라운드에 넣을 준비만 하면 되게 정리했다**: `CORES_ARMS="A B C D"` 한 줄이면 켜진다(`run_all.sh`가 이미 `CORES_ARMS`를 `coresidency_sweep.sh`의 `ARMS`로 그대로 넘긴다, `aws/README.md` P6 절 「팔 D」). 관측 스택 이미지(prometheus·grafana·mysqld-exporter 등)도 부트스트랩이 미리 당겨두게 했다 — 판 중간 첫 전환에서 pull 지연이 안 섞이게. **아직 안 한 것은 "이번엔 켤지" 결정과 실제 EC2 실행뿐**이다. ✅ **2026-09-08 — 실행 완료**: `coresidency-R9-2026-09-08-v3` 라운드에서 `CORES_ARMS="A B C D"`로 첫 실측, rig 버그(#686·#687) 없이 91행 전부 정상 수집. **판정 결과: C↔D 차이 ±3~7%, 두 자릿수%는 어디서도 안 나옴 — H5 지지, Q5 답 나옴.** R9는 이제 닫혔다 | [`ai-coresidency-capacity.md` §3(팔 D) · §6 H5 행](../docs/decisions/ai-coresidency-capacity.md) · [재고표 Q5](../docs/decisions/experiment-inventory.md) · 결과: [`results/coresidency-aws-2026-09-08/README.md`](./results/coresidency-aws-2026-09-08/README.md) §3 |
| ~~**R10-a**~~ ⭐ | ~~**프레임 경로 계측 — 1대 판**~~ — GIL 노브가 처리량을 움직이나 | ✅ **완료 (2026-08-23)** — [결과](results/frame-path-r10a-2026-08-23/README.md). `c7i.4xlarge` 1대 · 160세션 · 풀 201 · **5×5 라틴 방격 26판** · 25판 전부 유효 · 에러 0. 🔴 **GIL 스위치 «간격» 은 레버가 아니다** — 100배(0.0005↔0.05)를 흔들어도 −0.82%·−0.74% 로 **A 팔 자체 산포(2.29%) 안**이다. 🔴 **`lease` = 0.01ms** — 검출기 획득 경로(후보 2순위)가 **지워졌다.** 구간은 `wait` 23% · `infer` 33% · `post` 27% · `respond` 13%. 🟢 **그리고 從 판이 답을 냈다 — 천장은 프로세스당이다**: 1 프로세스 270.7 rps / 9.52 vCPU ↔ **2 프로세스 352.6 rps / 14.50 vCPU**(**1.30배**·1.52배). 「16 중 9.5」의 답이 **«한 프로세스라서»** 다. ⚠️ **2배가 아니라 1.30배** — 14.5 vCPU 에 두 번째 천장이 있고 정체는 안 쟀다. 그리고 **원인이 GIL 인지는 미확정**(프로세스 분리가 단일 이벤트 루프·내부 락도 같이 우회한다). 🔴 **인용 조건 둘** — 부하가 **앱 레이트의 2배**(rig 기본 6fps, 앱은 3fps)이고, proc-split 반복이 **팔당 2판**이다(산포 보강 6판은 인스턴스가 측정 중 terminate 돼 잃었다) | [결과](results/frame-path-r10a-2026-08-23/README.md) · **실측 판당 ~100초 · 26판 43분**(설계 예상 52~69분보다 빠르다) · `setup` **1.67~1.72초** · **`c7i.4xlarge` 1대** · 부하기 동거 비용 **0.24 vCPU 실측** |
| ~~**R10-b**~~ | ~~**프레임 경로 계측 — 2대 판**~~ — 「~9 에 눌리나」 | ✅ **완료 (2026-09-02)** — [결과](../docs/decisions/r10-loadgen-topology.md) · [원본](results/frame-path-r10b-2026-09-02/README.md). R10-a 가 GIL 스위치 간격을 반증한 뒤라 **최소 격자**(A/B 대조 없이 `GRPC_MAX_WORKERS` 5·10·20 만, 버림1+본판9)로 좁혀 돌았다 — 원 견적(19판)보다 훨씬 쌌다. `handler_concurrency` 도 **반증** — 팔간 차이 2% 안. 「16 중 9.5」의 남은 후보는 GIL·이벤트루프·내부 락 셋뿐이고 이번 라운드는 그걸 못 가른다. 부수로 2대(원격) 배선 버그 셋을 실전에서 처음 걸러 고쳤고([#647](https://github.com/Shadowfit/init/issues/647)·[#648](https://github.com/Shadowfit/init/issues/648) 새로 열림), 판마다 부하기가 대상 CPU 를 안 먹는 진짜 분리라 §2 의 오염 우려가 원리적으로 해소됐다 | **소요 991초(본 측정) · 2대**(대상 `c7i.4xlarge` + 부하기 `c7i.xlarge`) · RUN_ID `ec2-20260902-094952` |
| **R11** ⭐ | **박스 보정값 — 이 인스턴스가 초당 얼마나 «일» 하나** | 🟢 **`calibration` 단계로 상설화 완료(2026-09-02)** — `run_all.sh` 에 phase 로 들어갔다. ROLE=ai-venv 라운드는 `PHASES` 에 `calibration` 만 추가하면 됨(`aws/README.md` R10-a 절). `profile_e2e_and_scaling.py scaling` 의 **1워커 판**이 그대로 이것이다([R6 이 쓴 것](results/ai-scaling-aws-2026-08-17/README.md), 그때 값 **79.9 fps**). 도커·MySQL·부하기 **전부 불필요**(venv + `frames.json`). ✅ **P6(도커) 손 명령도 2026-09-08 검증 완료** — 별도 `c7i.4xlarge` 1대(ROLE=p6-target 단독, 11분 만에 확인 후 즉시 terminate)에서 `docker cp`+`docker exec` 두 줄이 그대로 통과했고 출력 형식도 ai-venv 경로와 동일해 `run_all.sh`의 awk 추출까지 재사용 가능함을 확인했다(그 박스 값 자체는 67.2 fps — 검증용 별개 인스턴스라 다른 라운드 보정에는 못 쓴다). 상세: `aws/README.md` P6 절 「보정값」 | **「라운드를 건너면 절대값이 안 맞는다(+17.7%)」의 축 0** — [설계 §2·§5](../docs/decisions/round-to-round-nonreproducibility.md). `docker stats` 의 CPU% 는 **시간**이지 «일의 양» 이 아니라서, 물리 호스트의 유효 클럭이 다르면 **같은 CPU% 로 다른 양의 일**을 한다 — 관측된 서명(«AI CPU 869.3%→869.0% 로 같은데 처리량 +17.7%»)이 정확히 그 모양이다. 단일 스레드 고정 작업이라 동시성·네트워크가 빠지고 **그 박스의 작업률만** 남는다. 🔑 **매 라운드 상설로 걷는 것이 요점이다** — 한 판만으로는 비교 대상이 없다. 🔴 **과거는 못 메운다**(설계 §6: P6 1·2라운드 박스는 사라졌다). 이 항목이 답하는 것은 **「앞으로」** 다. ⚠️ **설계 전체(축 A·B — 인스턴스 K대·재부팅 M회)는 여전히 미확정**이고, 여기 올리는 것은 **EC2 결정이 필요 없는 축 0 뿐**이다([#498](https://github.com/Shadowfit/init/issues/498) ①) | [설계](../docs/decisions/round-to-round-nonreproducibility.md) · [검토 #498](https://github.com/Shadowfit/init/issues/498) · 재고표 AI 축 3번 |
| ~~**R12**~~ | ~~**구멍이 `DROP PARTITION` 을 바꾸는가 — 조용한 박스에서**~~ | ✅ **완료 (2026-08-24)** — [결과](results/q2-partition-quiet-box-2026-08-24/README.md). `c7i` 조용한 박스 · 라틴 방격 **13블록** · 판당 하나만 DROP · 행/페이지 게이트 전 블록 통과 | **판정: 자릿수로는 독립이다.** 구멍이 페이지를 **+69%**(10,032 → 16,920) 늘리는데 DROP 은 **+13%** 만 따라간다(중앙값 **17.7 → 21.1 ms**). 10/13 블록이 같은 방향이지만 부호검정 **p=0.092**(Wilcoxon 0.094)로 0.05 를 못 넘는다 → 「DROP 1.8초 · DELETE 대비 625배」는 **구멍 유무와 무관하게 인용해도 된다**. 🔴 **«영향 0» 은 아직 실측이 아니다** — 방향이 일관돼서 0 이라 말할 근거도 없다. 필요한 n 은 팔당 **30~40판**(40~50분)이고 그건 다음 조용한 박스로 넘긴다. 🔑 **로컬이 못 가른 건 무대 문제였다** — 잡음 노브 둘(`WARM_DROP`·`SETTLE_SEC`)로 팔 안 산포가 **9.4배 → 1.9배**로 접히자 팔이 보였다. ⭐ **덤이 닫혔다**: 「먼저 DROP 한 쪽이 느리다」는 교락이 **예열 DROP 을 켜면 사라진다**(먼저−나중 중앙값 **+12.5 → −1.8 ms**) — 원인이 «그 판의 첫 DDL 몫» 으로 좁혀졌다(🔴 두 판 다 p≈0.5 라 좁혀진 건 방향이지 유의성은 아니다). 🔴 **round1 은 무효였다** — 행 0 · 페이지 1 인 파티션을 재고 **17.8·19.4 ms** 를 냈는데 진짜 값과 구별이 안 됐다. rig 에 무대 게이트를 넣었다 | [결과](results/q2-partition-quiet-box-2026-08-24/README.md) · [설계 §3 Q2](../docs/decisions/row-shape-partition-interaction.md) · [로컬 판](results/row-shape-frag-2026-08-24/README.md) · 재고표 MySQL 축 6번 |
| ~~**R13**~~ | ~~**HTTP 쓰기 경로 p99 — 판정선 대면**~~ ✅ **완료 (2026-08-24)** — [결과](results/http-write-p99-aws-2026-08-24/README.md). **가정 피크 ×180(13.5 세션시작/초)까지 p99 17ms**(목표 300ms 의 5.7%). 부하기 분리(c7i.xlarge) · 대상 c7i.2xlarge(관례값 아님 — vCPU 한도). ×360 은 **rig 천장**(계정 ÷ 완결 왕복)에 걸려 인용 불가. 🔴 이 라운드가 [#528](https://github.com/Shadowfit/init/issues/528)(StopAnalysis NameError → 세션이 IN_PROGRESS 에 갇힘)을 끄집어냈다 | [`measure_http_write_p99.sh`](measure_http_write_p99.sh) |
| ~~**R14**~~ | ~~**HTTP 읽기 경로 p99 — 판정선 대면**~~ ✅ **완료 (2026-08-28)** — [결과](results/http-read-p99-ec2-2026-08-28/README.md) | **가정 피크의 360배(27/초)까지 네 엔드포인트 전부 p99 두 자릿수 ms**(최악 `session` p99 12ms) — 목표 1,000ms 의 1.2%. 부하기 분리(c7i.xlarge) · 대상 c7i.2xlarge · 게이트(bad_status·dropped) 3배수 전부 통과. ×360 은 이 실험이 건 상한이지 rig 천장이 아니다(쓰기축 R13과 달리 세션을 안 만들어 계정 완결 왕복 제약이 없음) — 더 큰 배수는 미실행. 🔴 **R14 최초 실행에서 rig 결함 셋을 걸렀다** — ① `run_all.sh` 예시 `PHASES` 의 `preflight` 가 DDL/backup 전용이라 부하기 박스에서 즉시 FAIL → 남은 단계 스킵 → 그래도 빈 결과 업로드가 성공해 `FINAL_OK=1` → **AUTO_SHUTDOWN 오작동으로 부하기 박스를 두 번 잃음**(원인 규명 전 terminate) ② `seed/seed_report_rig.sh` 가 git에 실행권한 없이(100644) 커밋돼 rc=126 ③ p6-target 부트스트랩이 `python` 심볼릭 링크를 안 만들어 시드 중 `python: command not found`. 셋 다 **박스에서 손으로 우회했을 뿐 저장소 소스는 안 고쳤다** — 다음 실행에서 재발함, 이슈 등록 여부는 미결 | [결과](results/http-read-p99-ec2-2026-08-28/README.md) · [로컬 판](results/http-read-p99-2026-08-23/README.md) · [`slo-baseline.md` §4-2](../docs/decisions/slo-baseline.md) · 재고표 Spring 축 1번 |
| ~~**R15**~~ | ~~**AI 워커 부하-중 자발적 장애 빈도 — 가속 스트레스**~~ ✅ **완료 · 원 질문은 미답 (2026-08-28)** — [결과](results/ai-worker-load-soak-2026-08-28/README.md) | 동접 3배(≈203세션, 세션 시작/종료 반복)로 3시간 계획. **부하 시작 90초 만에 대상 박스 전체(SSH·전 앱 포트)가 응답 불능** — 원래 판정 채널(`shadowfit-ai`의 `RestartCount`/`OOMKilled`)은 대상이 먼저 죽어서 한 번도 못 봄. 소프트 리부트로 회수. 정황상 MySQL InnoDB 내부 락 정체 의심(로그가 "장기 세마포어 대기" 헤더에서 끊김) — 확정 아님, R16이 재검증 | [설계](../docs/decisions/ai-worker-load-soak-experiment.md) · 재고표 AI 축 |
| ~~**R16**~~ | ~~**같은 실험 재현 — MySQL 캡 3072m + 실시간 계측**~~ ✅ **완료 · R15 가설 반박 (2026-08-28)** — [결과 §6](results/ai-worker-load-soak-2026-08-28/README.md) | MySQL에 `mem_limit=3072m` 걸고(근거: `innodb-buffer-pool-size=2G` 기준, idle 실측 583.7MiB는 InnoDB가 점진적으로 채워져서 하한 근거로 못 씀) `SHOW ENGINE INNODB STATUS`·스레드덤프 7초 간격 계측 추가해 45분 재현 시도. **캡은 못 막았다** — 10분 만에 재발. 그런데 사고 30~40초 전 직접 증거로 **MySQL은 완전 idle·healthy** — R15의 락 정체 가설을 반박. 대신 systemd 저널·INNODB 폴러·Prometheus 스크레이핑 셋이 같은 30초 창에서 동시에 멎는 패턴 발견(메모리는 평평) → EBS I/O 크레딧 소진을 새 후보로 [#603](https://github.com/Shadowfit/init/issues/603) 등록. 🆕 **CloudWatch 사후 조회로 한 겹 좁혔다 (2026-09-02, [§6-7](results/ai-worker-load-soak-2026-08-28/README.md))** — «크레딧 소진» 이 아니라 **두 사고 모두에서 gp3 기본 처리량 상한(125MiB/s)에 도달**했다. 🔴 **인과는 여전히 미확정** — 상한에 닿는 것과 그것이 박스를 죽이는 것은 다른 주장이고, 애플리케이션 쓰기량을 안 걷어서 못 가른다. 이 라운드에서 obs 프로파일(Grafana·Prometheus·cAdvisor·node-exporter)도 처음 붙임(다른 작업과 겹쳐 깨끗한 단일변수 대조는 아님) | 재고표 AI 축 |

⚠️ **R1 과 R3 은 값 분포 한계에 걸린다.** 시딩이 단일 템플릿 복제라 카디널리티가 균일하다.
분포에 의존하는 결론은 내지 말고, **구조·존재 여부만** 본다.

---

## 2. 타지 않는 것 (이미 «AWS 불필요» 로 판정됨)

이 절이 목록을 짧게 유지한다. 「AWS니까 이것도 겸사겸사」로 부풀면 **또 안 보게 된다.**

| 항목 | 판정 근거 |
|---|---|
| 락 · MVCC · 격리수준 | «동시성 메커니즘 실험이라 AWS 불필요» — [`realmysql-experiments.md:188`](../docs/portfolio/realmysql-experiments.md) |
| admin `EXPLAIN` 스윕 | «`EXPLAIN` 은 옵티마이저의 선택이라 코어 수·경합과 무관. 같은 시딩이면 같은 답» — [`admin-page-scope.md:548`](../docs/decisions/admin-page-scope.md) |
| DELETE 파편화 후속 | 로컬 rig 로 이미 실측 완료 (`results/delete-fragmentation-2026-08-09/`). 남은 미검증(행 모양·파티션 상호작용)도 로컬로 충분 |

## 3. 이번엔 안 태우는 것 (비싸서)

| 항목 | 왜 |
|---|---|
| 통계 정확도 ↔ 플랜 흔들림 | 무대였던 1억 행 real-JSON(~230GB, m6i.xlarge+EBS 700GB)이 삭제됐다. **재구축 비용이 실험을 압도한다** |

---

## 4. 탑승 전 준비

매번 새 인스턴스라 **매번 든다.** 이 목록이 빠지면 판이 통째로 날아간다.

- [ ] `.env` 생성 — `MYSQL_ROOT_PASSWORD` 를 rig 기본 PW(`1234`)와 맞춘다. 안 맞으면 전 판 실패
- [ ] `docker pull percona/percona-toolkit` — 없으면 팔 B 4판이 전부 «DDL실패» 로 찍힌다. **도구의 성질이 아니라 환경 결함인데 표에는 똑같이 보인다**
- [ ] 🔴 **`batch_multi*.json` 은 `.gitignore` 되어 있다**(`ghz/.gitignore`, ~64MB). EC2 에는 없으므로 `gen_batch_multi.py` 로 재생성한다. FK 통과를 위해 **`exercise_sessions` 시드가 먼저** 있어야 한다
- [ ] `probe.sh` 재실행 — 환경이 바뀌었으므로 로컬 통과 이력을 재사용하지 않는다
- [ ] 🔴 **「보낸 요청이 실제로 행을 만드는가」를 한 번 확인한다** — 이 목록의 나머지는 전부
  **환경**을 본다(시드·내구성·풀·전송·도구). 2026-08-17 에 멱등 키가 rig 의 쓰기를 통째로
  삼키고 있었는데 **그걸 볼 확인이 하나도 없었다**([#271](https://github.com/Shadowfit/init/issues/271)).
  `SavePoseDataBatch` 를 몇 건 쏘고 `pose_data` 가 «요청 × 5» 만큼 늘었는지 세면 끝난다
- [ ] **빌드 대상 커밋을 명시한다** — `bootstrap.sh` 기본값은 `REF=main` 인데, 측정하려는 변경이
  main 에 없으면 **다른 코드를 재고도 모른다**. 2026-08-17 게이트가 그 직전까지 갔다
- [ ] **`WRITER_MAX_SEC` 상향** — 기본 5,400s(90분)인데 팔 B 로컬 실측이 2,360s 다. EBS 가 로컬 NVMe 보다 느려 2~3배 늘면 **writer 가 DDL 도중 먼저 죽어 `max_stall`·`p50` 이 통째로 구멍난다**
- [ ] **축소 리허설** — `SESSIONS=134` 로 8판(~15분). 전 경로를 먼저 밟는다
- [ ] 디스크 — 팔 B 는 사본을 만든다. binlog 도 B판당 ~445MB 누적
- [ ] 🔴 **설계 문서의 «확정된 것» ↔ rig 대조** — 08-13 라운드에서 사용자 confirm 으로 확정됐던
  «real-JSON 축소 대조 1판» 이 **rig 에 아예 구현돼 있지 않았고**, 그대로 무인 실행됐다.
  표는 정상으로 나오므로 **사후에 안 보인다.** 설계의 확정 목록을 열어 **팔·무대가 실제로
  코드에 있는지** 한 줄씩 대조한다

**P6 전용 — 이 라운드만 2대다** (다른 라운드는 위까지로 끝난다)

- [ ] 인스턴스 **2대** — 대상 `c7i.4xlarge` · 부하기 **`c7i.xlarge` 이상**, 같은 VPC·서브넷
      🔴 2 vCPU(`c7i.large`)면 §T(⑩)가 «제한 = 전체» 라 **한 판도 안 도는데 게이트는 통과한다** (#312)
- [ ] 보안그룹 인바운드 — 부하기 → 대상 **22 · 8000 · 8080 · 6565**
- [ ] **SSH 키를 부하기에** 둔다(`/root/.ssh/measure.pem`, `chmod 600`). 러너가 부하기에서 돌며 대상을 몬다
- [ ] **두 박스가 같은 토큰을 든다** — `AI_PUBLIC_TOKEN`·`INTERNAL_API_TOKEN` 을 대상·부하기에서 각각 같은 값으로.
      어긋나면 preflight 가 막는다. ⚠️ **두 변수끼리는 달라야** 한다 — 같으면 AI 가 안 뜬다(#230 단언 · #261)
- [ ] `ROLE=p6-target` / `ROLE=p6-loader` 로 부트스트랩을 **갈라 돌린다**. 기본 `ROLE=db` 는 MySQL 만 띄운다
- [ ] 🔴 `PHASES` 에 `coresidency*` 를 `ddl`·`backup` 과 **섞지 않는다** — 러너의 자리가 다르다
- [ ] `GHZ_BIN=/usr/local/bin/ghz` 를 실행 명령에 적는다 — 러너 기본값이 옛 경로다
- [ ] 절차 전문: [`aws/README.md` 「P6 — 2대 구성」](aws/README.md)

**P6-b(2라운드) 추가분** — 위 목록에 얹는다 (설계 §11)

> 🔴 **아래는 «과거 사전 실행 기록» 이다 — 지금 할 일 목록이 아니다.** P6-b 는 §7 이 적은 대로
> **2026-08-17 에 탑승·완료**했고, 아홉 항목은 그 라운드에서 반영됐다. 체크박스가 빈 채로
> 남아 있어서 다음 운영자가 이미 끝난 준비를 다시 하거나 «미실행» 으로 오독할 수 있다는
> 지적을 받아 이 단서를 붙인다(2026-09-04). **다음 P6 계열 라운드를 열 때 재사용할 목록**으로 읽을 것.

- [ ] 🔴 **rig 코드가 아직 2라운드 조건이 아니다.** 아홉 항목(§11) 전부 미반영 — 탑승 전 반영·리허설
- [ ] 로컬에서 **`docker update --cpus` 가 물리는지** 먼저 확인. 안 되면 교대 비용이 +4분 → +23분
- [ ] 부하기 → 대상 **8080 actuator** 와 MySQL `SHOW GLOBAL STATUS` 가 닿는지 (H3 의 판정 열)
- [ ] **자기 프로브 자산**(`frames.json`·`load_ai.py`·계정 1개)이 `ROLE=p6-target` 에 들어갔는지
- [ ] 리허설 판정에 **`loader_*.tsv` 게이트**가 들어갔는지 — 지금은 빈 파일이어도 라운드가 성공한다

---

## 5. 삭제 전 체크리스트 ⭐

**이 문서의 급소다. 인스턴스를 죽이기 전에 여기서 멈춘다.**

- [ ] 🔴 **§1 從 항목을 «표를 열어» 확인했는가** — 지금이 아니면 다음 인스턴스까지 못 잰다. 08-08 이 여기서 무너졌다.
      **항목 이름을 이 줄에 적어두지 않는다** — 2026-08-17 에 이 줄이 «R1·R2·R3» 인 채로 낡아,
      그날 새로 붙은 **R6·R7 을 못 보고 `c7i.4xlarge` 를 껐다**(아래 참조). §1 從 표가 유일한 원본이다
- [x] ~~🔴 **R6 — GIL vs 캐시.**~~ ✅ **완료 (2026-08-17)** — P6 2라운드 박스를 끄기 전 **15분**에
      R6·R7 을 같이 회수했다([결과](results/ai-scaling-aws-2026-08-17/README.md) · **GIL 반증**:
      프로세스/스레드 0.96~1.06x, 판정선은 1.7~2배였다). 🔴 **이 줄을 지우지 않고 남기는 이유는 교훈이다** —
      從 항목 중 **박스 조건이 까다로운 것**(R6 는 물리 8코어 이상)은 «다음 라운드» 가 없다.
      **그런 항목이 새로 생기면 §1 從 표에서 찾아 여기에 다시 세운다.** 이름을 미리 적어두면
      그 줄이 낡는다 — 08-17 에 이 체크리스트가 «R1·R2·R3» 인 채로 낡아 R6 를 놓칠 뻔했다
- [x] ✅ **R10-a — 프레임 경로 계측 1대 판. 돌았다 (2026-08-23).** 「16 중 9.5」의 답은 **«한 프로세스라서»** 이고, `lease`(후보 2순위)가 지워졌다. 🔴 **원인 셋(GIL·단일 이벤트 루프·내부 락)은 아직 안 갈렸다.** 아래는 착수 전 기록이다.
- [ ] 🔴 **R10-a — 프레임 경로 계측 1대 판.** 現 미실행 從 항목 중 **박스 조건이 까다로운 유일한 것**이다:
      **16 vCPU**(= `c7i.4xlarge`)에서만 질문이 성립한다 — 「9.5 of 16」이 그 박스의 관측이다.
      8 vCPU 박스로도, 로컬 물리 2코어로도 **못 잰다**(후자는 R6 이 확인한 벽).
      🟢 **막던 둘은 닫혔다 (2026-08-23)** — rig 이식([#396](https://github.com/Shadowfit/init/pull/396):
      리눅스 인터프리터·규모·GIL 팔)과 무대 결정([§7](../docs/decisions/r10-loadgen-topology.md):
      **쪼갠다, R10-a 먼저**)와 **venv ROLE**(`ROLE=ai-venv` — [#400](https://github.com/Shadowfit/init/issues/400) §2 가 열어둔
      자리다. 컨테이너로 띄우면 계측 노브가 안 넘어간다 [#399](https://github.com/Shadowfit/init/issues/399)).
      🟢 **남은 것은 박스 하나다.** 절차는 [`aws/README.md` R10-a](aws/README.md).
      ⚠️ 단 그 ROLE 로 **실제로 띄워 본 적은 없다** — `bash -n` 과 역할 분기 검증만 거쳤다.
      🔴 **소요는 못 박혀 있다** — 버림판이 `setup` 을 처음 준다. 34~45분으로 보고 **인스턴스는
      1시간**으로 잡는다(설계 §13-7). **1대**(부하기 동거) · 도커·MySQL·시딩 **불필요**(venv +
      `frames.json` + 부하기) · 판은 **17판**(4×4 라틴 방격 + 버림 1).
      계측은 켜도 된다(대가 ≤2.4%, `results/frame-path-overhead-2026-08-23/`) — **ON/OFF 앵커 판 한 장**은 끼울 것.
      🔴 **`handler_concurrency` 는 안 읽는다** — 그건 R10-b(2대)의 몫이다.
      → **이 줄은 R6 이 남긴 자리다.** 그 항목이 닫히면서 *「그런 항목이 새로 생기면 §1 從 표에서
      찾아 여기 다시 세운다」* 로 끝났고, R10 이 그 첫 번째다
- [ ] **원시 파일을 회수했는가** (S3 sync 또는 stop 후 scp). pool-cliff 가 원시 39개를 커밋한 이유가 있다 — *"지난번에 안 남겨서 같은 질문에 두 번 돈을 냈다"*
  ```bash
  # 위치는 MANIFEST.txt 의 «S3 결과» 줄에 있다 (#198 로 추가). 읽기 권한은 #199 참조.
  aws s3 sync s3://<버킷>/<프리픽스>/<RUN_ID>/ loadtest/results/<주제>-aws-<날짜>/
  ```
  🔴 **삭제 전에 회수한다.** 인스턴스를 지우면 `/root/run_all.log` 는 사라지고 S3 사본만 남는다
- [ ] 🔴 **회수한 라운드를 «읽었는가»** — 파일을 내려받는 것과 결과를 읽는 것은 다른 일이다.
      **P6 에서 두 번 났다**: 08-15 예비 라운드는 회수 없이 끊겼고(EC2 를 다시 켜서 발견),
      **08-16 b 라운드는 디스크에 완주한 표가 있는데 아무도 안 읽어** 「부하기가 천장」이라는
      판정이 하루 늦었다. **표 한 줄(`coresidency.tsv` plateau)만 봐도 났을 일**이다
- [ ] **조건을 기록했는가** — 인스턴스 타입 · 리전 · 디스크 종류/크기 · 측정 일시 · 판 수. 조건 없는 수치는 이 프로젝트에서 인용 불가다
- [ ] **판정이 뒤집혔는가** — 뒤집혔으면 설계 문서 결정 로그에 «뒤집힘» 으로 남긴다. 지우고 새로 쓰지 않는다
- [ ] **요금 태그가 붙어 있는가** (`Project=shadowfit-measure`, 인스턴스 + 볼륨). 살아 있을 때만 붙일 수 있다. 이 repo 에 EC2 요금 기록이 한 줄도 없는 이유가 이것이다 — 하루 이틀 뒤 Cost Explorer 에서 뽑아 `MANIFEST.txt` 의 `# 요금` 칸을 채운다
- [ ] 인스턴스·볼륨 **삭제** (볼륨이 남으면 요금이 계속 나간다)
  ```bash
  bash scripts/aws_teardown.sh list          # 무엇이 남아 있나 + 정지 인스턴스 경고
  bash scripts/aws_teardown.sh sweep --yes   # stopped 인스턴스 + 미연결 볼륨
  ```
  🔴 **`AUTO_SHUTDOWN=1` 은 stop 이지 terminate 가 아니다.** 그건 의도다(`/root/run_all.log` 가
  사라지면 사후 진단이 막힌다 — #203 이 그 로그로 잡혔다). 그래서 **지우는 일은 사람이 따로**
  해야 하고, 08-13 에 실제로 그 단계가 빠져 gp3 250GB 가 남아 있었다. `sweep` 은 **도는
  인스턴스를 안 건드리고** 다른 프로젝트 태그(`Project=DOCKin`)는 거부한다

---

## 6. 결과를 어디에 기록하나

측정이 끝나면 **repo 에 남기는 것까지가 한 판**이다. 경로는 기존 관례를 따른다.

| 대상 | 위치 | 무엇을 |
|---|---|---|
| 원시 데이터 · rig | `loadtest/results/<주제>-<날짜>/` + `README.md` | 판별 결과 · 측정 조건 · 재현 절차 · **방법론 오류까지** (pool-cliff §5 전례) |
| 실험 카드 | [`docs/portfolio/realmysql-experiments.md`](../docs/portfolio/realmysql-experiments.md) | 해당 카드에 «결과 ✅» 를 이어붙인다 |
| 정본 수치 | [`docs/portfolio/one-pager.md`](../docs/portfolio/one-pager.md) | **조건 칸을 비우지 않는다** |
| 서류 | `career-statement-dba.md` · `application-smilegate-dba.md` | 정본에서만 인용. 옮길 때 **조건을 떼지 않는다** |
| 판단이 바뀌면 | 해당 설계 문서 `결정 로그` | 뒤집힌 과정을 남긴다 |
| 결함이면 | GitHub 이슈 | 재현 절차 · 근거 `file:line` · **미검증 항목 명시** |

> 🔴 P2 가 «1.7배» 를 뒤집으면 **`one-pager.md:43` 뿐 아니라 그 수치를 인용한 곳 전부**를
> 같이 고쳐야 한다. 「3.47배를 인용한 13곳에 조건을 소급 표기」한 전례와 같은 작업이다.

---

## 7. 미결정 (사용자 confirm 필요)

- [ ] **인스턴스 타입·대수** — 4차 관례(c7i.2xlarge / m6i.xlarge). P4 는 2대 필수
- [ ] **P1 과 P2 를 같은 인스턴스에 태울지** — ⚠️ P1 의 DDL 이 디스크를 크게 먹어서 P2 의 쓰기 부하와 섞이면 **둘 다 오염된다.** 순차로 돌리거나 인스턴스를 나눠야 한다
- [x] **회수 경로** — ~~S3 sync vs stop 후 scp~~ → **S3 sync 로 확정.** 08-12 에 결정하고
  #198·#199 로 읽기 쪽을 닫았으며, **08-13 라운드에서 69개 객체 전량 회수로 실사용 확인**했다
- [x] **P3·P4 착수 여부** → 🟢 **둘 다 착수했고 둘 다 돌았다.** P3 는 08-13 라운드,
  **P4 는 2026-08-23 착수 확정 → 2026-08-22(UTC) 측정 완료** — `c7i.2xlarge` **2대 · 같은 AZ** ·
  스모크 선행([설계 §0-3·§10](../docs/decisions/replication-lag-and-semisync.md)). 설계 문서 상태줄도 같은 날 뒤집혔다.
  🔴 **남은 것은 «안 잰 것» 하나 — 다른 AZ 대조**이고, 그건 인스턴스를 다시 띄우는 **새 라운드**다.
  아래는 그 전까지의 경위다 — ~~설계 문서부터 써야 한다. 그 전엔 탑승 대상이 아니다~~
  → **둘 다 설계 완료.** P3 는 게이트 통과 후 08-13 라운드로 착수했고, **P4 는 설계만 서고 착수
  미결정**이다([`../docs/decisions/replication-lag-and-semisync.md`](../docs/decisions/replication-lag-and-semisync.md) §9 미결정 9건)
  🔴 **2026-08-22 정정**: 「설계만 서고」가 아니다 — **로컬 라운드가 2026-08-17 에 돌았고**
  (범위 Q3·Q4·Q5 — **Q4·Q5 는 닫혔고 Q3 은 「이 계측으로는 못 잰다」가 답**),
  설계 문서 상태줄은 남은 **Q1·Q2 를 «EC2 2대 필요, 안 함» 으로 박제**했다(사용자 confirm).
  그리고 같은 날 **2대 rig 이 섰다**([REPL2-RIG.md](results/replication-2026-08-17/REPL2-RIG.md)) —
  **장치는 있는데 상태줄은 「안 함」 그대로다.** 한 판도 안 돌았으니 그 줄이 틀린 것은 아니지만,
  「못 해서 안 함」과 「안 하기로 해서 안 함」은 다른 말이고 지금은 **뒤쪽만 남았다.**
  ⚠️ 그런데 재고표는 같은 항목을 **「우선 1 · 가장 먼저 착수」**로 적고 있었다. **두 문서가 갈렸던 자리**이고,
  어느 쪽으로 맞출지는 **착수 결정 자체**라 여기서 정하지 않는다
- [ ] 🔴 **P4 는 2대 구성이라 이 목록의 전제가 하나 깨진다** — 지금까지 라운드는 전부 1대였다.
  보안그룹 3306 인바운드 · `server_id` 분리 · (다른 AZ 대조판이면) AZ 간 전송 요금이 새로 붙는다
  → **rig 이 이 중 셋을 흡수했다 (2026-08-22)**: `server_id` 부여·`server_uuid` 분리(물리 사본이
  소스의 `auto.cnf` 를 데려온다)·리플리카 도달성 preflight. **남는 것은 사람이 정할 둘**이다 —
  **AZ 구성**(`REPL_AZ_MODE`, 설계 §9-1 ② — Q2 의 답이 여기서 자릿수로 갈린다)과 **인스턴스 타입
  동일 여부**(다르면 지연이 복제 구조의 값이 아니다). 매니페스트가 둘 다 조건 칸에 박는다
- [x] **P6 착수 여부** → 🟢 **착수 확정 (2026-08-16 사용자 confirm).** 조건 6건이 전부 닫혔다
  ([`ai-coresidency-capacity.md` §10](../docs/decisions/ai-coresidency-capacity.md)).
  **P6 도 2대다** — 위 P4 항목과 같은 전제 파괴이고, 이쪽은 §4 에 전용 준비 목록을 넣어 막았다
- [x] **P6-b(2라운드) 범위** → 🟢 **「전부」 (2026-08-16 사용자 결정).** 아홉 항목을 한 라운드에
  얹는다([설계 §11](../docs/decisions/ai-coresidency-capacity.md)) — 대상 인스턴스를 두 번 띄우는 것이
  판 18개보다 비싸다. **팔 D(Q5)와 부하기 스케일업 대조는 여전히 뺀다** — 후자는 자기 프로브
  결과를 보고 정한다
- [x] **P6-b 탑승 시점** → 🟢 **이미 탑승·완료했다 (2026-08-17).** 이 줄이 "아직 미정"으로
      낡아 있었다 — [`ai-coresidency-capacity.md`](../docs/decisions/ai-coresidency-capacity.md)
      상태줄이 이미 "2라운드 회수 완료"로 뒤집혀 있었는데 이 문서엔 안 옮겨졌다(2026-09-02 정정)

---

## 결정 로그

- 2026-08-12: 목록 초안. 08-08 에 놓친 사고(§0)를 계기로 흩어져 있던 «다음에 띄울 때» 항목을
  한 곳에 모았다. 主 4건 · 從 3건 · 「타지 않음」 3건. **실행 미착수**, §7 미결정 4건.

- **2026-08-12: 첫 탑승. P1 완료 · R2 완료 · R1·R3 은 못 탔다.** 인스턴스 1대(m6i.xlarge),
  부팅~정지 49분(러너 29분). 결과 [`results/online-ddl-aws-2026-08-12/`](results/online-ddl-aws-2026-08-12/README.md).
  - **이 목록이 실제로 값을 했다.** R2(MySQL 지표)가 같은 라운드에 실려 추가 비용 없이 걷혔다.
  - 🔴 **그런데 §5 체크리스트 1번(「從 항목을 돌렸는가」)이 또 절반 무너졌다.** R1·R3 이 둘 다
    **`reports` 테이블 부재**로 막혔다 — 러너는 DDL 측정에 필요한 mysql 만 띄우고 백엔드·Flyway 를
    올리지 않는다. **08-08 과 같은 실패가 아니라 새 실패다**(그때는 「목록이 흩어져서」, 이번엔
    「부트스트랩이 스키마를 안 만들어서」). 다음 라운드 전에 `bootstrap.sh` 에 Flyway 실행을 넣어야
    한다. 인프라가 살아 있을 때만 잴 수 있는 항목이라 또 놓치면 다음 인스턴스까지 밀린다.
  - **회수 경로에서 결함 2건**([#198](https://github.com/Shadowfit/init/issues/198)
    ·[#199](https://github.com/Shadowfit/init/issues/199)): 러너가 «어디에 올렸는지» 를 안 남기고,
    S3 를 읽을 권한을 가진 주체가 «업로드에 성공하면 스스로 꺼지는 인스턴스» 뿐이었다.
    §7 「회수 경로」 미결정 항목이 **S3 sync 로 정해졌지만 읽기 쪽이 안 닫혀 있었다.**
  - **요금 기록의 첫 사례**: 가동 0.80시간 / 러너 0.48시간. 차이 19분은 부팅 + `compose up` 실패
    디버깅 + 수정(`6996e1c`)이다. 청구액은 Cost Explorer 에서 `Project=shadowfit-measure` 로 뽑아
    `MANIFEST.txt` 의 `# 요금` 칸에 채운다.

- **2026-08-13: P4 설계 문서가 섰다** — [`replication-lag-and-semisync.md`](../docs/decisions/replication-lag-and-semisync.md).
  §7 「미결정」의 「P3·P4 착수 여부」 중 P4 쪽 전제(「설계 문서부터」)가 닫혔다. **착수 결정은
  아니다.** 반동기 플러그인은 착수 전에 이미 확인됐고(`semisync_source.so`·`semisync_replica.so`
  가 측정용 `mysql:8.0` 이미지에 있음), **리플리카 초기화는 08-13 백업 라운드의 G5(XtraBackup
  복구 유효)를 그대로 재사용**한다 — 主 항목이 다음 主 항목의 무대를 세운 첫 사례다.
  - 🔴 **이 항목만 2대 구성이라 §4·§5 의 전제가 하나 깨진다** — 보안그룹 3306 인바운드,
    `server_id` 분리, (다른 AZ 대조판이면) AZ 간 전송 요금. 지금까지 라운드는 전부 1대였다.

- **2026-08-13: 두 번째 탑승. 主 P3 완료 · 從 R1·R2 완료 · R3 만 못 탔다.** 인스턴스 1대
  (m6i.xlarge / ap-northeast-2c / gp3 200GB), **가동 3.11h · 러너 2.97h**, 무인 실행.
  결과 [`results/backup-restore-aws-2026-08-13/`](results/backup-restore-aws-2026-08-13/README.md).
  - ⭐ **§5 체크리스트 1번이 처음으로 안 무너졌다.** 08-08 은 「목록이 흩어져서」, 08-12 는
    「부트스트랩이 스키마를 안 만들어서」 從 항목을 놓쳤다. `bootstrap.sh` 에 Flyway 를 넣은
    수정이 여기서 효과를 확인했고, **R1 이 「테이블 부재」가 아니라 「테이블은 있고 전부 0행」**
    이라는 답을 냈다. R3(3-way 조인)은 시딩이 선행이라 여전히 SKIPPED — 이건 인프라 문제가
    아니라 무대 문제다.
  - **회수 경로가 실제로 값을 했다**(#198·#199 수정분의 첫 실사용). 러너가 스스로 정지했고
    (최종 업로드 성공 시에만 끄므로 그 자체가 완주 신호), 사람이 `aws s3 sync` 로 69개 객체를
    전량 회수했다. **08-12 에 열려 있던 「S3 를 읽을 주체가 없다」가 닫혔다.**
  - 🔴 **결과 표에서 결함 2건이 나왔다** — 둘 다 「숫자는 나오는데 조용히 다른 것을 잰」 부류다.
    ① 팔 B 복구 시간이 페이지 캐시를 쟀다([#201](https://github.com/Shadowfit/init/issues/201)),
    ② 설계에서 확정됐던 **real-JSON 축소 대조 1판이 rig 에 아예 없었다.**
    ②는 「탑승 목록」이 못 막는 종류다 — 이 문서는 «무엇을 태울지» 를 막지 «태운 것이 설계대로인지»
    를 막지 않는다. 다음 라운드 전에 **설계 §9-1 확정 항목 ↔ rig 대조**를 §4 준비에 넣어야 한다.
  - **삭제까지 끝냈다** — 인스턴스 terminate, 볼륨 200GB 는 `DeleteOnTermination=true` 로 함께
    삭제 확인(`InvalidVolume.NotFound`). 08-12 에 지적된 「stopped 인스턴스에 볼륨이 붙어 계속
    과금」 이 이번엔 반복되지 않았다. 청구액은 Cost Explorer 반영 후 `MANIFEST.txt` 에 채운다.

- **2026-08-16: P6 착수가 확정됐다 — 아직 탑승은 안 했다.** 조건 6건이 닫히고(설계 §10),
  ramp 레벨이 4개 → **6개(20/40/60/80/120/160)** 로 늘었다. 초판 격자가 «단독 156» 을 감싸는
  것이라 **가설(«동거하면 내려간다»)이 참일수록 빗나가는 쪽**이었다.
  - **§4 에 P6 전용 준비 목록을 넣었다.** P4 때 *「2대 구성이라 §4·§5 의 전제가 깨진다」* 를
    적어만 두고 목록으로 만들지 않았는데, P6 가 먼저 2대로 뜨게 됐다. SSH 키·SG 4포트·토큰
    일치·`ROLE` 갈래가 그것이고, **절차가 `bootstrap.sh` 출력에만 있던 것**([#247](https://github.com/Shadowfit/init/issues/247))도
    같이 닫았다 — 인스턴스 타입·대수를 정하는 시점은 부트스트랩보다 **앞**이다.
  - **§4 대조가 또 한 건을 잡았다** — 설계 §5 「판 사이 스택 재기동」이 rig(AI 만 재기동)과
    달랐다([#246](https://github.com/Shadowfit/init/issues/246)). #245 의 3건과 달리 **이번엔 rig 이 맞아** 설계를 고쳤다.
    §4 에 이 대조를 넣은 것이 08-13 의 ② 결함 이후인데, **두 라운드 연속으로 값을 했다.**
  - 🔴 **덤으로 하나 더 나왔다** — `run_all.sh:118` 의 `GHZ_BIN` 기본값(`/home/ec2-user/go/bin/ghz`)이
    `bootstrap.sh:179` 의 설치 경로(`/usr/local/bin/ghz`)와 다르다. preflight 가 막으므로
    라운드를 버리진 않지만, **README 대로 따라 하면 걸린다.** 실행 명령에 명시하는 것으로 덮었다

- **2026-08-16: P6 1라운드를 회수하고, 2라운드(P6-b)를 목록에 올렸다.** 정식 라운드는 2.29시간에
  완주했는데(예상 3.5h보다 짧다) **못 닫은 것이 셋** 남았다 — H1(동거 비용) · 부하기 천장 ·
  H3(캡이 옆을 지키는가).
  - 🔴 **그 뿌리 둘이 «판 설계» 에 있었다.** 결과 해석을 다시 판 게 아니라 판 배열을 다시 봤더니,
    교대 단위가 «팔» 이라 팔 간 대조점이 **13~27분** 떨어져 있고 그 사이 처리량이 분당
    **0.09~0.35%** 흐른다([#251](https://github.com/Shadowfit/init/issues/251)). 레벨 순서는 아홉 블록
    전부 오름차순이라 그 추세가 **plateau 를 정의하는 120·160 에 얹힌다**([#252](https://github.com/Shadowfit/init/issues/252)).
    **라틴 방격을 팔에만 걸고 레벨엔 안 걸었다**는 것을 라운드를 돌고 나서야 봤다
  - 🔴 **H3 은 애초에 판정할 열이 없었다**([#254](https://github.com/Shadowfit/init/issues/254)) —
    설계 §4 가 요구한 세 소스 중 rig 은 `docker stats` 하나만 걷는다. §4 대조를 «팔·무대» 에만
    걸고 **«지표» 에는 안 걸었던 것**이고, 이번에 그 항목을 §4 준비 목록에 넣었다
  - 🔴 **캡이 물렸는지 확인하는 게이트도 없었다**([#253](https://github.com/Shadowfit/init/issues/253)).
    1라운드의 「물렸다」는 사후 관측(789%)이지 단언이 아니다 — #223 과 같은 부류를 캡 축에서만
    안 막고 있었다
  - **결정: 「전부」 한 라운드에 얹는다**(사용자). 78판 · 인스턴스 ≈ 3h20m.
    ⚠️ 실제 리드타임은 라운드 길이가 아니라 **리허설 재시도**가 정한다 — 1라운드가 거기서 세 번 막혔다

- 🔴 **2026-08-17: 박스를 갖고도 R6 를 못 태웠다 — §5 체크리스트 1번이 «낡아서» 무너졌다.**
  P6 2차 리허설로 **`c7i.4xlarge`(물리 8코어 / 16 vCPU)** 를 45분 띄웠다가 terminate 했다.
  그 박스가 **R6 의 전제 조건을 정확히 만족하는 유일한 무대**였는데(로컬 물리 2코어로는
  구조적으로 못 잰다 — 4워커에서 스레드와 프로세스가 같은 천장에 붙는다) 아무도 안 챙겼다.
  - **원인은 「목록에 없어서」가 아니다.** R6·R7 은 §1 從 표에 **그날 이미 등록돼 있었다**
    (판정선·소요 시간까지). 무너진 것은 §5 체크리스트 1번이 **「§1 從 항목(R1·R2·R3)을
    돌렸는가」로 굳어 있었던 것**이다 — 항목 이름을 요약해 적어둔 줄이 목록보다 먼저 낡았다.
  - 이 프로젝트의 문서 드리프트가 **한 방향**이라는 것의 또 한 사례다: 표는 갱신되고
    **표를 가리키는 문장이 안 갱신된다.** 그래서 그 줄에서 **이름을 지우고 «표를 열어라» 로
    바꿨다.** 요약은 또 낡지만 「원본을 열어라」는 안 낡는다.
  - R6 는 從 항목 중 **박스 조건이 까다로운 유일한 것**이라 §5 에 **자기 줄**을 따로 줬다.
    R1·R2·R3 은 다음 라운드에도 기회가 있지만, R6 는 **물리 8코어 박스가 살아 있는 동안만**이다.
  - ⚠️ **손실은 없다** — 2차는 리허설이라 어차피 정식 라운드가 남아 있다. 다만 **그날 15분이면
    끝날 일**(R6 10분 + R7 5분 · 도커·MySQL·시딩 불필요)이 정식 라운드까지 밀렸다.

- **2026-08-22: P5 와 從 R4 를 표에서 닫는다 — 라운드는 08-18 에 돌았는데 표만 나흘 늦었다**
  ([#356](https://github.com/Shadowfit/init/issues/356)). 같은 라운드에서 닫힌 **P2 행은 이미 지워져
  있었는데 P5 행만 「다음 라운드 主 로 확정」으로 서 있었다** — 헤더가 «남은 主 는 P4 뿐» 으로
  갱신된 뒤라 **한 문서 안에서 앞뒤가 갈렸다.** 늘 나오는 방향의 드리프트다([[문서 드리프트는 한 방향]]).
  - **主 P5** → ✅ 완료. 실측 격자가 설계의 `1·2·5·20·100` 이 아니라 **`1·2·5·10·20·50`** 이었다는
    것도 같이 적었다 — **옛 격자로는 무릎을 못 봤다**(5와 20 사이가 통째로 비어 있었다)
  - **從 R4** → ✅ 완료. 「P5 라운드에만 얹힌다」로 남아 있었는데 **그 라운드에 실제로 얹혔다**
    ([결과](results/session-spread-aws-2026-08-17/R4-connection-sweep.md)). ㉡ 반증 · ㉠ 이 처음으로 근거를 얻었다
  - ⚠️ **소요 칸은 예상치를 지우지 않고 실측을 나란히 뒀다** — 판수가 25 → 36 으로 바뀐 것이
    격자 변경 때문이라는 사실이 예상치를 지우면 안 보인다

- **2026-08-23: §5 체크리스트의 R6 줄을 닫는다 — 라운드는 08-17 에 돌았고 이 줄만 엿새 남았다**
  - `[ ]` → `[x]` ✅ 완료(2026-08-17 · **GIL 반증** · [결과](results/ai-scaling-aws-2026-08-17/README.md)).
    §1 從 표는 그날 바로 갱신됐는데 **§5 절차가 안 따라왔다** — 표는 맞고 «보장·절차» 가 낡는,
    이 repo 의 늘 그 방향이다([[project_doc_drift_pattern]]).
  - **줄은 지우지 않았다.** 「박스 조건이 까다로운 從 항목은 «다음 라운드» 가 없다」가 그 줄의
    존재 이유다. 그런 항목이 새로 생기면 §1 從 표에서 찾아 여기 다시 세우라고 적었다.
    🔴 **이름을 미리 박아두면 그 줄이 낡는다** — 08-17 에 이 체크리스트가 «R1·R2·R3» 인 채로 낡아
    R6·R7 을 놓칠 뻔한 것이 정확히 그 실패였다. 같은 실수를 이름이 아니라 **표를 가리키는 방식**으로 막는다.

- **2026-08-23: 從 표에 R9(관측 스택 동거 비용 · Q5/H5)를 등록한다 — 세 라운드째 «길어지니 뺀다» 로 밀렸다**
  - 이 항목은 «어디에 얹나» 가 먼저 걸렸다. 처음 제안된 자리가 **P4 였는데 그 무대에는 AI 가 없다**
    — 두 대 다 MySQL 이고 부하기도 `frames.json` 도 없다. 「인프라가 살아 있으니 공짜」가 성립하려면
    **그 인프라가 이 항목이 묻는 무대여야 한다**는 것이 從 의 조건이다. R2 가 P4 에 붙을 때와 달리
    (도는 MySQL 에 `SHOW GLOBAL STATUS` 한 번) R9 는 무대를 새로 세워야 한다. → **P6-b 로 붙였다.**
  - ⚠️ **R9 는 從 의 예외다** — «추가 비용 거의 0» 이 아니라 **+19판(+42분)** 이다. 그래서 등록은
    「그 라운드에서 넣을지 정한다」까지고, **채택은 P6-b 격자 확정 시점의 결정**이다.
  - 🔴 **설계 문서의 제외 사유가 이미 만료돼 있었다** — `ai-coresidency-capacity.md` §11 이
    「못 닫은 셋이 먼저다」로 팔 D 를 뺐는데 **그 셋은 2라운드에서 전부 닫혔다.** 사유를 그대로 두면
    다음 라운드도 같은 문장으로 밀린다. 그 줄과 §6 H5 행, 재고표 Q5 행을 같이 R9 로 이었다.

- **2026-08-23: 從 R10(프레임 경로 계측 본 판)을 등록하고 §5 에 자기 줄을 준다**
  - 계측 코드가 오늘 들어갔고([#384](https://github.com/Shadowfit/init/pull/384)) 대가도 로컬에서 쟀는데(≤2.4%),
    **정작 그 계측으로 한 판도 안 돌았다.** 등록해두지 않으면 다음 인스턴스에서도 안 보인다 —
    R6 이 정확히 그렇게 놓칠 뻔했다.
  - 🔴 **P4 라운드에는 못 얹었고, 그건 실수가 아니다.** 그 박스는 `c7i.2xlarge`(8 vCPU)에
    MySQL 2대뿐이라 **AI 무대 자체가 없었다.** R9(Q5)를 P4 에 못 얹은 것과 같은 이유다 —
    「인프라가 살아 있으니 공짜」는 **그 인프라가 이 항목이 묻는 무대일 때만** 성립한다.
  - **§5 에 자기 줄을 줬다.** R6 줄이 닫히면서 *「박스 조건이 까다로운 항목이 새로 생기면
    §1 從 표에서 찾아 여기 다시 세운다」* 로 끝났는데, **R10 이 그 첫 번째**다.

- **2026-08-23: 從 R11(박스 보정값)을 등록한다 — 설계 전체가 아니라 «축 0» 만이다**
  - [비재현 설계](../docs/decisions/round-to-round-nonreproducibility.md)가 축 셋을 세웠는데, **축 0 만**
    성질이 다르다: **라운드당 1~2분 · 새 인스턴스 0 · rig 이 이미 있다**(R6 이 쓴
    `profile_e2e_and_scaling.py scaling` 1워커 판). 설계 §5 자신이 *「1번은 EC2 결정이 필요 없다
    — 다음 라운드에 얹으면 된다」* 고 적었고, 그건 이 문서가 정의한 **從 등급 그대로**다.
  - 🔴 **축 A·B(인스턴스 K대 · 재부팅 M회)는 여전히 미확정이다.** 여기 올리는 것은 축 0 뿐이고,
    그 구분을 안 하면 «설계가 착수 확정됐다» 로 오독된다([#498](https://github.com/Shadowfit/init/issues/498) ①).
  - 🔑 **한 판만으로는 값이 없다** — 매 라운드 상설로 걷어야 «라운드 간» 비교가 생긴다. 그래서
    특정 라운드의 從 이 아니라 **모든 라운드의 從** 으로 올린다. 이 표에서 그런 항목은 처음이다.
  - 🔴 **과거는 못 메운다**(설계 §6). P6 1·2라운드 박스는 사라졌고, 이 항목이 답하는 것은 **「앞으로」** 다.
    즉 **오늘 등록해두지 않으면 다음 라운드도 같은 자리에서 못 가른다** — R10 을 등록한 이유와 같다.


- **2026-08-24: 從 R12 회수 — 조용한 박스가 실제로 갈랐다(절반은).** [결과](results/q2-partition-quiet-box-2026-08-24/README.md)
  - **등록한 날 회수했다.** 위 항목이 «로컬에서 못 갈라서 올린다» 였는데, 같은 날 조용한 EC2 박스에서
    라틴 방격 13블록을 돌려 **자릿수 판정까지** 왔다: 페이지 **+69%** 에 DROP **+13%**(17.7 → 21.1 ms).
  - 🔑 **무대가 원인이었다는 게 확인됐다.** 잡음 노브 둘로 팔 안 산포가 **9.4배 → 1.9배**로 접혔다.
    로컬에서 «두 팔이 완전히 겹친다» 던 것이 조용한 박스에서는 10/13 블록이 같은 방향으로 정렬됐다.
  - 🔴 **그래도 p=0.092 다.** 「구멍은 영향이 없다」로 닫으면 안 된다 — **자릿수로 의존하지 않는다**까지가
    이 판이 말할 수 있는 전부고, 남은 +13% 를 가르려면 **팔당 30~40판**(40~50분)이 더 필요하다.
  - ⭐ **위 항목이 «더 클 수 있다» 던 덤이 실제로 닫혔다** — 「먼저 DROP 한 쪽이 느리다」는 예열 DROP 을
    켜면 사라진다(**+12.5 → −1.8 ms**). 「1.8초·625배」 인용이 첫/둘째 DROP 에 의존하던 문제는,
    **첫 DDL 몫을 떼어내면 없어지는 것**으로 좁혀졌다(🔴 유의성은 아직, p≈0.5).
  - 🔴 **회수 과정이 함정 하나를 보여줬다** — round1 은 무대가 안 서서 **행 0 · 페이지 1** 을 쟀는데
    그 시간(**17.8·19.4 ms**)이 진짜 판(17.7·21.1 ms)과 **구별이 안 됐다.** 러너의 게이트가 아니었으면
    무효 판을 결과로 인용했을 것이다. rig 본체에도 같은 게이트를 넣었다.

- **2026-08-24: 從 R12(Q2 — 구멍이 DROP PARTITION 을 바꾸는가)를 등록한다 — «로컬에서 못 갈라서» 올린다**
  - R11 과 등록 사유가 다르다. R11 은 «싸니까 상설로 걷자» 였고, **R12 는 «로컬에서 시도했는데
    답이 안 나와서»** 다. [로컬 판](results/row-shape-frag-2026-08-24/README.md)에서 페이지는
    팔을 정확히 갈랐는데(**+69~84%**) DROP 시간의 팔 안 산포가 **7.6배**라 두 팔이 겹쳤다.
  - 🔴 **설계가 적어둔 「Q2 는 상대비교까지 로컬」이 틀렸다.** 그 문장을 로컬 실측이 정정했고,
    그래서 이 항목은 «안 해본 것» 이 아니라 **«해봤고 무대가 부족한 것»** 이다.
  - 🟢 **從 조건을 잘 만족한다** — MySQL 하나만 있으면 되고(앱·부하기·AI 무대 불필요), 판당
    약 1분이며 rig 이 완성돼 있다. **어느 라운드에도 얹을 수 있다.**
  - ⭐ **덤이 더 클 수 있다.** 스모크에서 «한 판에 둘을 연달아 DROP 하면 먼저 한 쪽이 느리다»
    (부호가 뒤집힌다)가 나왔다. `realmysql-experiments.md` 의 **「DROP PARTITION 1.8초 ·
    DELETE 대비 625배」** 가 그 판의 첫 DROP 이었는지 둘째였는지에 의존하는데 **그 조건이
    기록돼 있지 않다.** 포폴에서 인용되는 수치라, 조용한 박스면 이것도 같이 갈린다.


- **2026-08-24: 從 R14(HTTP 읽기 p99 — 판정선 대면)을 등록한다 — 쓰기축이 판 형태를 이미 세웠다**
  - SLO 11번은 **절반씩 두 번** 채워졌다. 읽기(08-23 로컬)가 먼저였는데 **판정선과 대면 못 했고**,
    쓰기(從 R13, 08-24 EC2)가 그 대면 형태를 세웠다 — 가정 P1 배수 · `constant-arrival-rate` ·
    부하기 분리. **그 형태를 읽기에 그대로 이식한 것이 이 항목**이다.
  - 🔑 **읽기가 더 싸다.** 쓰기는 회원당 IN_PROGRESS 1개 제약 때문에 「계정 완결 왕복」이
    rig 천장을 만들어 ×360 을 못 걸었다(R13 §1-2). 읽기는 세션을 안 만들어 그 천장이 없다.
  - 🔴 **대신 선결이 하나 있다 — 데이터 소유권.** 시더가 `member_id` 를 1·5·12 로 하드코딩해
    새 계정엔 읽을 것이 없다. 로컬 판은 그걸 손으로 했고, EC2 판은 러너가 시드 단계를 돌려
    `K6_SIDS` 를 넘겨야 한다. **이 항목의 유일한 추가 비용이 그것**이다.
  - ⚠️ 등록이지 착수가 아니다. 라운드가 서면 얹는다.
