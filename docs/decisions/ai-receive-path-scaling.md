# AI 수신 경로 — 「16코어 중 9.5만 쓰는」 마지막 후보를 어떻게 확인하고, 고칠 것인가

작성일: 2026-08-17
상태: 🟢 **잤다 — 從 R10-a 완료 (2026-08-23).** 「16 중 9.5」의 답은 **«한 프로세스라서»** 다
([결과](../../loadtest/results/frame-path-r10a-2026-08-23/README.md)) — 프로세스를 둘로 쪼개니 CPU 9.5 → **14.5 vCPU**, 처리량 **1.30배**.
🔴 **그 안에서 원인 셋이 아직 안 갈렸다** — GIL · 단일 이벤트 루프 · 프로세스 내부 락.
프로세스 분리는 셋을 **한꺼번에** 우회한다. §10 이 그 자리다.
대상: ~~후보 일곱을 지우고 하나 남은 것의 확인 방법~~ → **확인은 끝났다.** 남은 것은
**원인 분리**와 **선택지**다
연관: [`../../loadtest/results/ai-scaling-aws-2026-08-17/README.md`](../../loadtest/results/ai-scaling-aws-2026-08-17/README.md)(R6·프로파일·부하기 재확인) ·
[`../../loadtest/results/coresidency-aws-2026-08-17/README.md`](../../loadtest/results/coresidency-aws-2026-08-17/README.md)(P6 2라운드) ·
[`./ai-coresidency-capacity.md`](./ai-coresidency-capacity.md) · [#92](https://github.com/Shadowfit/init/issues/92) · [[feedback_state_assumption_design_to_it]]

---

## 0. 지금까지 지워진 것

| 후보 | 어떻게 지웠나 |
|---|---|
| GIL | R6 — 프로세스/스레드 0.96~1.06x |
| 하드웨어·코어 | R6 — 순수 추론은 **15.2 vCPU** 까지 간다 |
| 검출기 풀 상한 | 풀 = **201** (컨테이너 로그) |
| 부하기 CPU | §T — 코어 4→2 에도 무변화 |
| anyio 워커 한도(40) | 40↔128 교차 4판 — 차이 −0.6%(변동 이내) |
| 부하기 프로세스 수 | 1×160 = **350.85** vs 2×80 = **350.28** (0.2%) |
| AI 프로세스 안의 락 | 프로파일 — 파킹 스택이 `queue.get`(**일감 대기**)이지 락이 아니다 |

**남은 하나: 수신 경로(단일 이벤트 루프).** 348 RPS × 40KB 페이로드를 **한 스레드**가 읽고
파싱해 워커에 넘긴다. 프로파일 3판 중 2판에서 `active+gil` 이었다.

⚠️ **이건 가설이다.** 「워커 앞에서 막힌다」까지가 관측이고, 그 앞이 이벤트 루프라는 것은
소거법의 결론이지 직접 측정이 아니다.

---

## 1. 구조 — 왜 «워커를 늘리면 되잖아» 가 안 되나

```
uvicorn (단일 프로세스 · 단일 이벤트 루프)   ← Dockerfile:19, --workers 없음
├─ anyio 워커 스레드풀 (기본 40)             ← def detect_pose 가 여기서 돈다
├─ gRPC 서버 스레드 (포트 8585)              ← main.py lifespan 이 띄운다
├─ 검출기 풀 (201)                           ← 프로세스 메모리
└─ 세션 레지스트리                            ← 🔴 프로세스 메모리
```

**`uvicorn --workers N` 을 그냥 켤 수 없는 이유가 셋이고, 큰 순서로 이렇다:**

| # | 걸림돌 | 왜 |
|---|---|---|
| **1** | 🔴 **세션 상태가 프로세스 메모리에 있다** | 워커가 여럿이면 같은 세션의 프레임이 **다른 워커**로 갈 수 있다. 그 워커엔 그 세션이 없다 |
| 2 | **gRPC 서버를 lifespan 이 띄운다** | 워커마다 하나씩 띄워 **같은 포트를 잡으려 든다** |
| 3 | 검출기 풀이 프로세스마다 생긴다 | 메모리가 워커 배수로 는다(개당 98.7MB × 201) |

1번이 본질이다. 2·3번은 배치 문제지만 1번은 **설계 문제**다.

---

## 2. 확인 방법 — 초안이 틀렸고, 실제로 해보고 알았다

### 2-0. ❌ 폐기된 초안 — 「컨테이너 2개에 세션을 반씩」

*"AI 컨테이너를 2개 띄우고 부하기가 세션을 반씩 나눠 보낸다. 세션 하나의 프레임은 항상 같은
컨테이너로 가므로 문제가 없다. **코드 변경 0.**"*

🔴 **틀렸다. 2026-08-17 에 실제로 띄워 보고 알았다.**

`shadowfit-ai2` 를 8001 에 띄우고 5세션 스모크를 돌렸더니 **«20초 안에 분석기가 안 붙었다»**
로 판이 무효였다. 이유는 §1-1 이 **프레임 경로에만** 적용된다고 본 것이 좁았기 때문이다:

> **세션이 어느 AI 에 붙을지는 클라이언트가 아니라 Spring 이 정한다.**
> `AI_SERVER_HOST=shadowfit-ai` — **주소가 하나뿐이다.** 세션 생성 때 Spring 이 그 주소로
> gRPC `StartAnalysis` 를 불러 **1번 컨테이너에** 분석기를 붙인다. 프레임을 2번으로 보내면
> 요청은 도달하지만(로그에 `200 OK` 가 찍힌다) 그 컨테이너엔 세션이 없어 반려된다.

**즉 «코드 변경 0» 이 아니다.** 스티키 라우팅(§3-ㄴ)을 하려면 **Spring 의 attach 경로부터**
어느 인스턴스에 붙일지 고를 수 있어야 한다. 이건 프록시만으로 안 되고 **Spring 이 관여한다.**

### 2-1. 🟢 대체안 — 페이로드만 줄인다 (계산량 고정)

가설이 「단일 이벤트 루프가 **바이트를 읽고 파싱하는 것**이 천장」이면, **보내는 양을 줄이면
천장이 올라가야 한다.**

**같은 160세션·같은 코드로, 프레임만 640x480 → 320x240 으로 바꾼다.**

- 🔑 **R7 이 이 실험을 가능하게 한다** — 해상도는 **추론 시간을 안 바꾼다**(13.7 ↔ 13.7ms).
  즉 **계산량은 고정되고 전송·파싱 바이트만 변한다.** 변수가 하나다
- 실측 축소율: 프레임당 **13,376자 → 5,184자 (39%)**
- 코드 변경 0(진짜로). `frames.json` 축소판 하나면 된다

| 결과 | 뜻 |
|---|---|
| 처리량이 **크게 오른다** | **확정** — 수신·파싱 바이트가 천장이다 |
| **그대로다** | 바이트 양은 아니다. 이벤트 루프 스케줄링·소켓 처리 등 다른 곳 |

⚠️ 이건 §2-0 보다 **약한 확인**이다 — 「프로세스를 늘리면 배수로 는다」를 직접 보는 게 아니라
「부담을 줄이면 는다」를 보는 것이라, 반증이 나와도 이벤트 루프를 완전히 지우지는 못한다.

### 2-2. ✅ 실행 결과 (2026-08-17) — **바이트 양은 천장이 아니다**

160세션 · 90초 · 교차 배열. **`nolease` 로 유효판을 갈랐다**(§2-3).

| 판 | 페이로드 | RPS | p50 | `nolease` | 유효 |
|---|---:|---:|---:|---:|:--:|
| big_1 | 100% | 350.58 | 448.3 | 0 | ✅ |
| big_2 | 100% | 346.43 | 454.0 | 0 | ✅ |
| small_2 | **39%** | **375.76** | 418.2 | 0 | ✅ |
| small_4 | **39%** | **376.42** | 417.6 | 0 | ✅ |
| ~~small_1~~ | 39% | ~~480.38~~ | 14.0 | **38,084** | ❌ |
| ~~small_3~~ | 39% | ~~478.86~~ | 14.1 | **38,254** | ❌ |

**페이로드를 61% 줄였는데 처리량은 348.5 → 376.1, 즉 +7.9% 다.**
두 small 판은 서로 0.2% 안에서 일치한다(변동 대비 효과가 충분히 크다).

→ **반증에 가깝다.** 바이트 양이 천장이면 61% 를 덜어냈을 때 훨씬 크게 올랐어야 한다.
효과는 **실재하지만 작다.**

### 🔑 그래서 레버의 값이 갈린다

| 레버 | 예상 효과 | 근거 |
|---|---|---|
| base64 → 바이너리 (−25% 바이트) | **미미** | 61% 를 덜어도 +7.9% 였다 |
| 해상도 낮추기 | **없음** | R7 (추론 무차별) + 이 실험(바이트 무관) |
| **프레임 «수» 줄이기 ([#92](https://github.com/Shadowfit/init/issues/92))** | 🟢 **비례** | 천장이 **요청 «건수»** 쪽이라면 건수를 줄이는 것만 듣는다 |

**남은 형태는 «크기» 가 아니라 «건당 고정비»** 다 — 요청 하나를 받아 넘기는 데 드는
스케줄링·컨텍스트 전환·ASGI 처리. 크기와 무관한 부분이다. **이것도 아직 직접 측정은 아니다.**

### 2-3. 🔴 곁다리 — `nolease` 무효판. **처음 읽은 것이 틀렸다**

무효판이 여러 번 났다. 전부 같은 서명이다: **`nolease` 가 요청의 99.6~100% · RPS 480 · p50 13~44ms.**
**RPS 만 보면 «천장을 뚫었다» 로 읽힌다.** 실제로는 즉시 반려를 초당 480번 받은 것이다.

🔴 **정정 (2026-08-17 저녁).** 처음에 `nolease` 를 **「세션이 AI 에 안 붙었다」** 로 읽고
「판 간격이 세션 정리 주기보다 짧아서」라고 적었다. **둘 다 틀렸다.**

rig 이 스스로 답을 적고 있었다:

> *"🔴 nolease 가 있다 — **풀에 자리가 없다.** «용량» 이지 «품질» 이 아니다"*

무효판 로그를 보면 **`setup_fail=0` 이고 「분석기가 안 붙었다」도 0건**이다 — **세션은 정상적으로
붙었다.** `nolease` 는 **검출기 풀에 빈자리가 없어** 프레임이 반려된 것이다.

### 그러면 이게 결함 신호다

풀은 **201**이고 판은 **160세션**이다. 세션당 검출기 하나면 **160 < 201 이라 여유가 있어야 한다.**
그런데 소진됐다는 것은 **앞 판의 리스가 안 풀렸다**는 뜻이다.

[`session-detector-ownership.md`](./session-detector-ownership.md) §8 이 구현 조건으로 못 박은
**「세션 종료 시 `close()`」·「비정상 종료 회수」** 가 바로 이 자리다. 그 안(ㄴ)은 채택 확정이지만
**구현 미착수**다.

⚠️ **미확인**: 왜 keep-alive 팔(§2-4)에서만 소진됐는지 모른다. 리스 해제 경로가 연결 재사용과
얽히는지, 아니면 판 순서·누적 때문인지 **안 갈렸다.**

🔴 **판정에 미치는 영향은 없다.** 이 문서의 모든 판정은 **`nolease=0` 인 유효판만으로** 냈다.
무효판의 «원인» 해석이 바뀐 것이지 «무효였다» 는 사실은 그대로다.

### 2-4. keep-alive 실험 — **답을 못 냈다**

`load_ai.py:57` 은 `urlopen` 이라 **요청마다 새 TCP 연결**을 연다(연결 재사용 없음).
348 RPS = 초당 348개의 새 연결이고, 그 비용은 **요청 크기와 무관**하다 — §2-2 의 「크기는 별
영향 없다」와 잘 맞는다. 그리고 **실제 모바일 클라이언트는 keep-alive 를 쓰므로**, 확인되면
「동시 116명」에 **조건이 붙는다.**

그래서 `http()` 를 스레드별 지속 연결로 바꾼 사본(`load_ai_ka.py`)을 만들어 쟀다.

| 판 | 팔 | RPS | `nolease` | 유효 |
|---|---|---:|---:|:--:|
| ka_1 | keep-alive | 479.82 | 38,347 | ❌ |
| url_1 | urllib | 348.95 | 0 | ✅ |
| url_2 | urllib | 348.17 | 0 | ✅ |
| ka_2 | keep-alive | 480.83 | 38,400 | ❌ |

**keep-alive 판만 전부 무효였다** — 순서가 아니라 **팔로 갈렸다.**
따라서 **이 질문은 답을 못 냈다.** urllib 판 둘은 기준선(348.9 / 348.2)을 재확인했을 뿐이다.

> 🔴 **2026-08-23 정정 — «풀 소진» 이 아니다.** `PoolT.lease()` 는 **용량을 안 본다**:
> `self._detectors.get(session_id)` 가 없으면 `None` 이고, 핸들러 메시지도 *「세션 …에 배정된
> 분석기가 없습니다 (StartAnalysis 먼저 호출 필요)」* 다
> (`mediapipe_detector.py:230` · `pose.py:136`). **세션이 안 붙은 것**이지 자리가 없는 게 아니다.
>
> 🔴 **그리고 38,400 요청 중 38,347 이 nolease 다 — 99.86% 가 즉시 반려됐다.**
> **480 RPS 는 처리량이 아니라 «빨리 실패한 것»** 이고, **「keep-alive 가 +38%」는 인용하면
> 안 된다.** 다시 재는 설계는 [`ai-process-ceiling-cause.md` §12](./ai-process-ceiling-cause.md)
> 이고, **선결은 그 사본이 왜 세션을 못 붙였는지**다.

⚠️ 원본 `load_ai.py` 는 **안 건드렸다.** 사본으로만 쟀다.

---

## 3. 확인됐다면 — 고치는 길 네 갈래

### ㄱ. 세션 상태를 프로세스 밖으로 (Redis 등) + `--workers N`

| | |
|---|---|
| 장점 | 정공법. 수평 확장의 문 자체가 열린다 |
| 단점 | **가장 크다.** 세션 레지스트리·검출기 소유권이 전부 이 전제 위에 서 있다. 검출기는 애초에 **직렬화가 불가능**하다(MediaPipe 객체) — 상태를 밖에 둬도 **검출기는 여전히 프로세스에 묶인다** |
| 판정 | 🔴 **이 문제를 푸는 방법으로는 부적합.** 검출기가 못 따라오므로 절반만 푼다 |

### ㄴ. 스티키 라우팅 — 앞에 프록시를 두고 `session_id` 로 고정 배분

| | |
|---|---|
| 장점 | **세션 상태를 안 옮겨도 된다.** 같은 세션은 항상 같은 프로세스로 간다 = 지금 구조 그대로 |
| 단점 | 프록시가 하나 는다(§[reverse-proxy-and-tls](./reverse-proxy-and-tls.md) 와 같은 자리). gRPC 포트·검출기 풀 메모리는 여전히 프로세스마다 |
| 🔴 비고 | **초안은 이 안을 «프록시만 두면 된다» 로 봤는데 틀렸다**(§2-0). Spring 이 attach 대상을 정하므로 **Spring 도 같이 고쳐야 한다** — 안의 크기가 커진다 |

🟢 **그 «커진 크기» 를 2026-08-23 에 폈다 — [`ai-sticky-routing.md`](./ai-sticky-routing.md).**
묶여 있는 결정이 **셋**이고(프레임 엔드포인트 계약 · 매핑 위치 · 장애 재배치) 따로 못 정한다.
그 문서가 정정한 것 둘 — ① 「Spring 이 정한다」는 **고른다는 뜻이 아니다.** 오늘 라우팅을 하는
주체는 없고 **고정 주소가 두 군데**(여기 `static://` 와 프론트 빌드 상수) 박혀 있다.
② 프레임 주소가 **설치본 안에** 있어서, 이 절이 본 것보다 한 겹 깊다.
🔴 **그 문서도 기본값은 «안 한다» 다** — 아래 ㄹ-1 계산과 從 R10-a 가 먼저다.

### ㄷ. 수신 부담 자체를 줄인다 (구조 무변경)

이벤트 루프가 힘든 이유가 **40KB base64 JSON × 348/s** 라면, 그 양을 줄이면 된다.

| 레버 | 크기 | 비고 |
|---|---|---|
| base64 → 바이너리 업로드 | 페이로드 **약 −25%** | base64 는 3바이트를 4자로 부풀린다. 계약 변경 |
| 프레임 수 감소 | [#92](https://github.com/Shadowfit/init/issues/92) — 휴식 중 프레임을 안 보내면 **세션당 800~1,350회** 절약 | 이미 등록된 항목 |
| 해상도 | ❌ **효과 없음** | R7 에서 320x240~960x720 이 추론 시간 무차별로 확인됨 |

| | |
|---|---|
| 장점 | **구조를 안 건드린다.** #92 는 어차피 할 일이다 |
| 단점 | 천장을 **옮길 뿐 없애지 못한다.** 배수가 아니라 상수만큼 |

### ㄹ. 고치지 않는다 — **먼저 「지금 천장이 모자란가」를 묻는다**

🔴 **이 문서에서 가장 중요한 갈래다.**

지금 천장 **348 RPS ÷ 3fps = 동시 116세션**이다. 그런데 이 앱은 **회원당 활성 세션이 1개**이므로
**동시 116세션 = 동시에 운동 중인 사람 116명**이다.

DAU 1,000 가정([[feedback_state_assumption_design_to_it]])에서 **동시에 운동 중인 사람이
116명을 넘는지**가 답이다. 그 숫자를 **아직 세운 적이 없다.**

| | |
|---|---|
| 장점 | 안 필요한 최적화를 안 한다. **비용 0** |
| 단점 | 「용량 천장을 알고도 안 고쳤다」가 아니라 **「필요를 계산해보고 안 고쳤다」** 여야 한다 — 그 계산이 선행 |

### ㄹ-1. 🟢 그 계산 (2026-08-23) — **안 넘는다. 단 여유가 인용되던 것의 절반이다**

**입력은 넷이고, 셋은 repo 에 있고 하나는 없었다.**

| 값 | 얼마 | 출처 |
|---|---|---|
| DAU | **1,000** | [`load-test-strategy.md`](./load-test-strategy.md) §4.2 «성공 시나리오». ⚠️ 같은 문서 §4.1 의 **냉정한 기본값은 DAU 30~50** 이다 |
| 세션/일/사용자 | **1.5** | 같은 문서 §4.2 |
| 세션 길이 | **15분** | `backend/src/main/resources/application.yml:252` — 「스쿼트 예상시간 15분」 |
| 피크 1시간 집중도 | **0.18** | 🔴 어디에도 없어서 아래 역산으로 복원했고, **2026-08-23 사용자 confirm 으로 «가정 P1» 로 못박혔다** — 정본은 [`load-test-strategy.md` §4.2](./load-test-strategy.md). 실서비스 로그가 생기는 날 버린다 |

**「67」의 감춰진 가정이 그 넷째다.** §4.2 는 `DAU 1,000 × 1.5 = 1,500세션/일 → 피크 동시 ~67`
이라고만 적고 **중간 단계를 안 적었다.** 역산하면 `67 ÷ (1500 × 15/60) = 0.179` —
**피크 1시간에 하루 세션의 18%** 가 몰린다는 가정이다. 그러면 식이 닫힌다:

```
피크 동시 = DAU × 1.5 × p × (L / 60)
          = 1,000 × 1.5 × 0.18 × 0.25 = 67.5   ✓ §4.2 의 67 이 재현된다
```

### 🔴 천장이 하나가 아니다 — 여기가 이 계산의 실제 발견이다

| 천장 | 3fps 환산 | 조건 |
|---|---:|---|
| AI 단독 · in-process | **156세션** | `measure_ai_concurrency.py` — **HTTP 없음 · 동거 없음** |
| HTTP 경로 산술 | **116세션** | 348 RPS ÷ 3fps (이 문서 §3-ㄹ 머리) |
| **동거 + HTTP 실측** | **89~105세션** | P6 1·2라운드 plateau(267.6 fps = 89.2세션 · 2라운드 105.0) |

**배포 구성은 세 번째다** — `docker-compose.prod.yml` 이 MySQL·Spring·AI 를 한 호스트에 올린다.

### 답

**67 < 89. 안 넘는다.** 그런데 여유가 **1.33~1.57배**(89/67 · 105/67)다.

🔴 **여러 문서가 인용하는 «2.3배 여유» 는 156/67 이고, 그 156 은 단독 박스 값이다.**
([`ai-load-budget.md`](./ai-load-budget.md) §2 · [`ai-coresidency-capacity.md`](./ai-coresidency-capacity.md) §0 ·
[`load-test-strategy.md`](./load-test-strategy.md) §1 · 결과 문서 둘 — [#385](https://github.com/Shadowfit/init/issues/385))

### 언제 닿나 — 가정 하나만 움직여도 DAU 1,000 안에서 닿는다

| 조건 | 계수 | 89세션에 닿는 DAU |
|---|---:|---:|
| 기본 가정 (p=18% · L=15분) | 0.0675 | **1,319** |
| 세션이 **30분**이면 | 0.135 | **659** |
| 피크가 **25%** 면(저녁 집중) | 0.0938 | **949** |
| 둘 다면 | 0.1875 | **475** |

**그래서 결론은 「고칠 필요 없다」가 아니라 「지금 가정에서만 필요 없다」다.**
세션 길이 15분과 피크 18% 는 **둘 다 실측이 아니다** — 앞의 것은 타임아웃 계산용 예시값이고
뒤의 것은 아무도 적은 적이 없다. **DAU 보다 이 둘이 먼저 흔들린다.**

⚠️ **이 계산은 실측이 아니다.** 실사용 로그가 생기면 세션 길이와 시간대 분포를 **직접 세는 것**이
이 표를 대체한다 — 그때까지는 「가정을 적어두고 그 기준으로 설계한다」쪽이다([[feedback_state_assumption_design_to_it]]).

---

## 4. 자원 — 이게 지원축에 무엇을 주나

정직하게: 이 프로젝트의 지원축은 **백엔드(Spring)·DBA** 다([[user_career_target]]).
이 항목은 **AI 축**이라 서류 키워드로는 안 붙는다.

**그런데 서사로는 값이 있다:**

> 「용량 천장을 재고 → 후보를 일곱 개 세워 하나씩 반증하고 → 프로파일로 좁히고 →
> **고치기 전에 필요량부터 계산했다**」

이건 «성능을 올렸다» 보다 강한 이야기다. 특히 §2 의 확인 실험은 **코드를 안 고치고 가설을
가르는 설계**라 그 자체가 사례가 된다.

⚠️ 반대로, ㄱ~ㄷ 을 실제로 **구현**하는 것은 지원축에 거의 기여하지 않는다.
**「확인까지」와 「구현까지」는 다른 결정이다.**

---

## 5. 순서 (추천)

1. ~~**§2-1 확인 실험**(페이로드 축소)~~ → **완료(2026-08-17). 바이트 양은 천장이 아니다**(§2-2)
2. **ㄹ 의 계산** — DAU 가정에서 동시 운동자 수 상한. **로컬에서 되고 비용 0.** 이게 다음이다
3. §2-3 의 **풀 리스 누수를 이슈로** 등록 — 이건 이 항목과 별개로 **모든 부하 측정에 걸리고**,
   `session-detector-ownership` ㄴ안(채택 확정·구현 미착수)의 §8 조건과 같은 자리다
4. ~~§2-4 의 **keep-alive 비교를 다시**~~ → ✅ **완료(2026-08-24).**
   [§12 판](../../loadtest/results/pre-asgi-transport-2026-08-24/README.md)이 **사본을 안 만들고
   rig 의 팔로** 넣어 `nolease` **0** 으로 12판을 냈다 — **재사용 +0.86%**(§9 의 −0.9% 재현),
   그리고 §9 가 못 갈랐던 **라이브러리 교체분 −0.24%** 를 따로 냈다
5. 위 결과를 보고 **ㄷ 의 «프레임 수» 레버(#92)만** 남는지 판단. ㄱ·ㄴ 은 **효과 근거가 약해졌다**

**2 가 실질적으로 남은 유일한 선행이다** — 「천장이 모자란가」에 답하기 전에는 어떤 안도 정당화되지 않는다.

---

## 6. 미결정 (사용자 confirm 필요)

- [ ] §2-1 확인 실험을 **지금** 태울 것인가 (박스 시한부)
- [ ] ㄹ 의 «동시 운동자 수» 를 어떤 가정으로 세울 것인가
- [ ] 확인되면 ㄴ·ㄷ 중 무엇을 할 것인가 — **또는 안 할 것인가**
- [ ] 이 항목을 재고표의 어느 축에 둘 것인가(AI 축 신규 항목)

---

## 7. 정직하게 비어 있는 것

- **「이벤트 루프가 범인」은 여전히 소거법의 결론이다.** §2-2 는 그 중 «바이트 파싱» 형태를
  지웠을 뿐, «건당 고정비» 형태는 안 쟀다
- **400ms 지연의 소재를 안 쟀다.** 요청당 450ms 중 계산은 14~26ms 인데, 나머지가
  **어디서** 소모되는지는 관측하지 않았다(수신 큐·이벤트 루프·소켓 중 어디인지)
- **동시 운동자 수 상한을 세운 적이 없다**(§3-ㄹ). 이게 없으면 「고쳐야 하나」에 답할 수 없다
- **`nolease` 무효판의 조건을 규명하지 못했다**(§2-3). 풀 리스가 왜 안 풀리는지, 왜 keep-alive
  팔에서만 소진되는지 **둘 다 모른다** — **모든 부하 측정에서 `nolease` 를 반드시 볼 것**
- ~~**연결 재사용이 천장에 얼마나 기여하는지 못 쟀다**(§2-4).~~ ✅ **쟀다(2026-08-24)** —
  [§12 판](../../loadtest/results/pre-asgi-transport-2026-08-24/README.md): **연결 하나 0.207ms**,
  재사용 **+0.86%**, 라이브러리 교체 **−0.24%** — 전부 판 간 산포(5.10%) 안이다.
  🟢 **그래서 「동시 116명」에 붙어 있던 «부하기가 매번 새 연결을 연다» 조건이 풀린다**

---

## 결정 로그

- **2026-08-17: 문서 신설.** 하루 동안 후보 일곱을 지우고 하나가 남았는데, 그 하나를 확인하려면
  `uvicorn --workers` 가 필요하고 그건 **5분짜리가 아니라는 것**이 드러나서 설계로 돌렸다.
  구조를 읽어 보니 걸림돌이 gRPC 포트가 아니라 **세션 상태가 프로세스 메모리에 있다는 것**이었다.
  ⚠️ **초안의 «컨테이너 2개에 세션을 반씩» 확인법은 틀렸다.** 같은 날 실제로 띄워 보고 알았다 —
  세션이 어느 AI 에 붙을지는 **Spring 이 정하고 그 주소는 하나뿐**이라, 2번 컨테이너로 보낸
  프레임은 도달해도 세션이 없어 반려된다(§2-0). 대신 **페이로드만 줄이는 확인법**으로 갈아탔다(§2-1)
  — R7 이 «해상도는 추론 시간을 안 바꾼다» 를 확인해 준 덕에 계산량을 고정한 채 바이트만 흔들 수 있다.
  그리고 §3-ㄹ 을 갈래로 세웠다 — **고칠지 말지는 「천장이 모자란가」를 먼저 계산할 일**이다.

---

## 8. 실험 설계 — 「accept 경로가 천장인가」 (2026-08-17 작성, 미실행)

### 8-0. 가설과 판정선 (실행 전에 박는다)

**H: 연결 수락(accept) 경로가 천장이다.**
부하기가 `urlopen` 이라 **요청마다 새 TCP 연결**을 연다. 그 수락이 초당 ~348개로 묶이면
나머지 클라이언트는 **커널 accept 백로그**에서 기다린다.

이 하나로 관측 넷이 동시에 설명된다:

| 관측 | H 로 설명되나 |
|---|---|
| 1세션·로컬 프로브도 452ms | ✅ **같은 백로그에 줄 선다** — 이게 결정적 단서다 |
| 워커가 파킹(일감 큐가 빔) | ✅ 앱 안에는 `348 × 26ms ≈ 9`개만 들어와 있다 |
| CPU 어디도 포화 아님 | ✅ 대기는 커널 큐라 CPU 를 안 쓴다 |
| 페이로드 61%↓ 에 +7.9% | ✅ accept 는 **크기와 무관** |

**판정선은 유도해서 정한다**(임의값 금지 — [[feedback_no_arbitrary_threshold_values]]):
accept 가 유일한 제약이면 그것을 없앴을 때 **다음 제약은 CPU** 다.
R6 실측(순수 추론 15.2 vCPU · 프레임당 ~26ms)에서 **상한은 약 580 RPS** 다.

- **348 → 450 RPS 이상**으로 오르면 **H 지지**(CPU 천장 쪽으로 이동)
- **348 ±10% 에 머물면 H 반증**(연결은 범인이 아니다)
- 그 사이면 **부분 기여** — 몫을 숫자로 적는다

### 8-1. 1단계 — 커널이 이미 세고 있다 (부하기 안 고쳐도 된다, 2분)

**백로그가 넘치면 커널 카운터가 올라간다.** keep-alive 를 만들 필요 없이 **직접 증거**가 나온다.

| 지표 | 무엇 | 어디 |
|---|---|---|
| `ListenOverflows` | 백로그가 꽉 차서 **연결을 버렸다** | `nstat -az TcpExtListenOverflows` |
| `ListenDrops` | 같은 이유의 드롭 | `nstat -az TcpExtListenDrops` |
| 리스닝 소켓 `Recv-Q` / `Send-Q` | **현재 백로그 깊이 / 상한** | `ss -lnt` |

부하 전/후로 카운터를 찍고 **증분**을 본다. 부하 중 `Recv-Q` 도 같이 샘플링한다.

- **증분이 크거나 `Recv-Q` 가 상한에 붙어 있으면** → H **거의 확정**
- **증분 0 이고 `Recv-Q` 가 낮으면** → 백로그는 안 막혔다. H 가 **약해진다** → 2단계로

⚠️ **한계**: 백로그가 «넘치지 않으면서도» 수락이 느릴 수 있다. 그러면 이 단계는 조용하다 —
**반증이 아니라 «증거 없음»** 으로 읽어야 한다.

### 8-2. 2단계 — keep-alive 대조 (몫을 재려면)

`load_ai_ka.py`(오늘 만들어 둠)로 팔을 가른다. **선행 관문이 하나 있다.**

🔴 **풀 소진을 먼저 막아야 한다.** 오늘 keep-alive 판 둘이 `nolease` 100% 로 무효였다.
**우회는 실증됐다** — anyio 실험은 **판마다 컨테이너를 재시작**해서 4판 전부 유효했다.

| 요소 | 값 | 왜 |
|---|---|---|
| 팔 | urllib(현행) ↔ keep-alive | 나머지 전부 고정 |
| 판마다 | **AI 컨테이너 재시작** | 풀 리셋(실증된 우회) |
| 게이트 | **`nolease > 0` 이면 그 판 폐기·재실행** | 오늘 무효판 다섯을 그냥 지나칠 뻔했다 |
| 배열 | 버림 1 + 교차 3쌍 | 라운드 내 추세가 팔에 얹히지 않게 |
| 판당 | 160세션 · 90초 | 기존 라운드와 같은 조건 |
| 소요 | 약 25분 | 재시작·간격 포함 |

### 8-3. 무엇이 나오면 내가 틀린 것인가

- keep-alive 로 바꿨는데 **348 ±10%** → H 반증. 남는 건 앱 안쪽 건당 처리다
- 1단계가 조용한데 2단계에서 처리량이 오르면 → 「백로그」가 아니라 **연결 수립 자체의 비용**
  (소켓 생성·TIME_WAIT 등)이다. 같은 H 의 다른 형태
- **둘 다 아니면** §0 후보 목록을 다시 세워야 한다

### 8-4. 이 실험이 성공하면 바뀌는 것

**「동시 116명」이 실제 용량이 아니게 된다.** 실제 클라이언트는 keep-alive 를 쓴다.
그러면 오늘 잰 천장은 **부하기가 만든 천장**이고, `ai-coresidency-capacity.md` 와
[P6 라운드 README](../../loadtest/results/coresidency-aws-2026-08-17/README.md)의 용량 수치에
**전부 조건이 붙는다.**

⚠️ §T·프로세스 수 실험이 「부하기 아님」을 가리킨 것과 **모순되지 않는다** — 그 둘은 부하기의
**CPU·프로세스**를 흔들었고, 이건 부하기의 **연결 방식**이다. 다른 축이다.

---

## 9. 실행 결과 (2026-08-17 밤) — **H 반증. accept 큐는 증상이었다**

### 9-1. 1단계 — 백로그는 실제로 길었다

부하 중 uvicorn 리스닝 소켓(`:8000`, 컨테이너 netns):

| 지표 | 값 |
|---|---|
| **`Recv-Q`(백로그 대기 연결 수)** | **132~135 지속** |
| `Send-Q`(상한) | 2048 |
| `ListenOverflows` / `ListenDrops` | **0** (넘치진 않았다) |
| 호스트 `:8000` `Recv-Q` | **0** (호스트는 안 막힘) |
| `TcpPassiveOpens`(90초) | **17,336** — 요청당 거의 1연결 |

그 판은 237 RPS · p50 590ms 였고, **리틀의 법칙이 맞았다**: `133 ÷ 237/s = 561ms ≈ 590ms`.
**지연의 대부분이 accept 큐 대기 시간이었다.**

### 9-2. 2단계 — 그런데 연결을 재사용해도 안 변한다

판마다 컨테이너 재시작(풀 리셋) + `nolease` 게이트 + 교차 배열. **4판 전부 유효**(`nolease=0`).

| 팔 | 판 | RPS | p50 |
|---|---|---:|---:|
| keep-alive | ka_a / ka_b | 270.89 / 253.47 | 576.6 / 615.6 |
| urllib(현행) | url_a / url_b | 267.39 / 261.58 | 590.2 / 601.8 |

**keep-alive 262.2 vs urllib 264.5 → −0.9%.**
사전 판정선은 **「450 RPS 이상이면 H 지지 · 348 ±10% 면 반증」** 이었다 → **반증.**

### 9-3. 그러면 accept 큐 133개는 무엇이었나

**증상이다.** 앱이 한 번에 ~9개만 처리하니 나머지가 커널 큐에 쌓였을 뿐이고,
큐를 없애도(keep-alive) **처리 능력이 그대로라 총량이 안 변했다.**

> **역방향 인과였다.** 「accept 가 느려서 앱이 굶는다」가 아니라
> **「앱이 천천히 가져가서 큐가 길다」** 였다.

🔴 **그리고 이것 때문에 §8-0 의 «관측 넷을 동시에 설명한다» 가 무너진다** — 그 넷은
**H 가 아니어도** 「앱 안 동시 처리가 ~9로 묶임」 하나로 똑같이 설명된다. **설명력이 같으면
증거가 아니다.** 백로그 관측(9-1)은 H 를 지지한 게 아니라 **양쪽과 다 맞는 관측**이었다.

### 9-4. 곁다리 둘

- **판마다 컨테이너를 재시작하면 처리량이 348 → 260~270 으로 −24%.** 검출기 풀이 차갑게
  시작해 판 안에서 검출기를 만들며 돌기 때문으로 보인다(⚠️ 원인 미검증).
  **풀 소진을 피하려고 넣은 우회가 절대값을 바꾼다** — 앞으로 이 우회를 쓰는 판은
  **다른 판과 절대값 비교 불가**다
- **판 순서대로 270.9 → 267.4 → 261.6 → 253.5 (−6.4%) 단조 감소.** 재시작을 해도 무언가
  누적된다. 교차 배열 덕에 팔 판정엔 안 얹혔다. **원인 미상**

### 9-5. 남은 것

| 후보 | 상태 |
|---|---|
| GIL · 하드웨어 · 검출기 풀 · 부하기(코어·프로세스) · anyio 한도 · 락 · 페이로드 크기 · docker-proxy · 호스트 CPU | ❌ |
| **연결 재사용 / accept 경로** | ❌ **9-2** |
| **「앱 안 동시 처리가 ~9로 묶이는 이유」** | 🔶 **미해결. 후보를 다시 세워야 한다** |

⚠️ 「~9」 자체가 **산술 추론**이다(348 × 26ms). 직접 세어본 적이 없다 —
**다음 판의 첫 일은 «동시에 앱 안에 있는 요청 수»를 직접 세는 계측**이다.

> ✅ **그 계측이 들어갔다(2026-08-23).** `frame_path` 구간이 ASGI 안을 나누고
> [§11-5](./ai-process-ceiling-cause.md)가 **「ASGI 안에 30개, 밖에 130개」** 를 냈다 —
> **「~9」는 스레드풀 안이고 ASGI 안 전체로는 ~30개**다. 밖의 130개는
> [§12 판](../../loadtest/results/pre-asgi-transport-2026-08-24/README.md)이 **accept 큐**로
> 확인했다 — **9-1 의 132~135 와 같은 것**이다.

---

## 10. 후보 재정렬 (2026-08-17 밤, 하루 끝)

§9 로 «연결/accept» 가 지워지고 **후보를 다시 세웠다.**

### 10-1. 🟢 1순위 — **GIL. 확정 (2026-08-24 직접 계측)**

> 🔴 **從 R10-a 가 이 후보를 반쯤 깎았다**([결과 §2](../../loadtest/results/frame-path-r10a-2026-08-23/README.md)). `GIL_SWITCH_INTERVAL` 을
> **100배**(0.0005 ↔ 0.05) 흔들었는데 처리량이 −0.82%·−0.74% 로 **A 팔 자체 산포(2.29%) 안**이다.
> ⚠️ **단 「GIL 이 아니다」로 읽으면 안 된다** — GIL 이 직렬화 중이라면 **스케줄링 양자를 바꿔도
> 총량은 원래 안 변한다.** 이 판은 「간격이 레버가 아니다」만 말한다.
> 🟢 그리고 **프로세스를 쪼개니 1.30배가 나왔다**(같은 결과 §5) — GIL 을 우회하는 조작이
> 처음으로 효과를 냈다. **다만 단일 이벤트 루프·내부 락도 같이 우회되므로 셋이 안 갈린다.**
>
> 🔑 **그 «안 갈린다» 를 가르는 설계가 섰다 (2026-08-23)** —
> [`ai-process-ceiling-cause.md`](./ai-process-ceiling-cause.md). 그 문서가 코드를 다시 읽고
> 찾은 것 셋이 여기의 판정 하나를 **좁힌다**:
> ① 핸들러가 `def`(동기)라 **anyio 스레드풀**(기본 상한 **40**)에서 돈다 — 후보가 하나 는다.
> ② `inflight_handler` 는 **스레드 점유가 아니다** — `leave` 가 `http.response.start` 라
> **루프 직렬화까지 포함**한다.
> ③ 🔴 **그래서 「GIL 간격은 레버가 아니다」를 좁혀야 한다** — 처리량은 그대로지만
> **동시성 꼬리는 3배 달랐다**(최대 105~113 ↔ 36~40). 정확히는 **«처리량의»** 레버가 아니다.
> 설계는 **계측 셋 + 조건부 조작**이고 **미착수**다.

R6 가 지운 것은 **「순수 추론 루프」의 GIL** 이다 — MediaPipe 는 C++ 안에서 GIL 을 놓는다.
**그런데 서비스 경로에는 추론 말고도 프레임마다 도는 파이썬 코드가 있다**:
JSON 파싱 · Pydantic 검증 · base64 디코드 · 응답 직렬화. **이건 전부 GIL 을 잡는다.**

산술: 프레임당 GIL 구간이 **~2.8ms** 면 `348 × 2.8ms = 0.97 코어` — **GIL 상한(1코어)에 붙는다.**

**그리고 오늘 초반 덤프에 증거가 있었는데 안 읽었다:**

| 스레드 | 상태 |
|---|---|
| MainThread(이벤트 루프) | 3판 중 **2판이 `active+gil`** ← GIL 을 쥐고 있었다 |
| AnyIO 워커 | 전부 `idle` ← **GIL 을 못 쥔 상태** |

이러면 「워커가 논다」의 정체가 **「일이 없어서」가 아니라 「GIL 을 못 얻어서」** 가 된다.
CPU 9.5(파이썬 구간은 1코어 상한 + 추론만 병렬) · 페이로드 축소 효과가 작았던 것(파싱은
GIL 구간의 일부) · keep-alive 무효까지 **하나로 설명된다.**

⚠️ **단 이건 §8 에서 내가 저지른 실수와 같은 종류의 논증이다** — 「여러 관측을 설명한다」는
것만으로는 증거가 안 된다(§9-3). **직접 재야 한다.**

> ## 🟢 직접 쟀다 (2026-08-24) — 이 후보가 맞다
>
> [`ceiling-cause-stage2-2026-08-24`](../../loadtest/results/ceiling-cause-stage2-2026-08-24/README.md).
> 팔 셋(F 현행 · Z 널 핸들러 · C 프로브 끔) **13판** + 추가 5판. 게이트 통과(에러 0 · `nolease` 0).
>
> **① GIL 총 점유율 `rho` = 0.939.** 일 없는 스레드가 무부하엔 p50 **0.077ms** 에 깨는데
> 부하 중엔 **1.172ms** 다(15배). p99 는 **5.62ms** — **정확히 한 GIL 양자(기본 5ms)**.
> 히스토그램으로 「깰 때 GIL 이 놀고 있던 비율」을 세어 그 여집합을 냈다.
> 🔑 **이 값은 하한이다**(늦게 깨면 표본을 적게 낳는다) → 실제 점유는 **≥93.9%**.
>
> **② 위 산술이 맞았다.** `main` 스레드는 **0.50 vCPU** 뿐인데 GIL 은 94% 잡혀 있다 —
> 차이가 **워커의 파이썬 몫**이다. 「1코어 상한에 붙는다」는 예측이 관측과 만난다.
> ⚠️ **다만 `main` 만으로는 못 봤다** — 스레드 CPU 는 루프 몫만 줘서 «하한» 이고,
> 총량은 프로브 분포에서만 나온다.
>
> **③ 순수 파이썬 경로가 정확히 1코어에서 막힌다.** 널 핸들러 팔의 fps 스윕:
>
> | 제시 | 실측 rps | `cpu.ai` |
> |---|---|---|
> | 960 | 959.1 | 0.87 |
> | 1,920 | **1,165.8** | **1.04** |
> | 3,840 | **1,158.1** | **1.04** |
>
> MediaPipe 를 통째로 뺐는데 16 vCPU 박스에서 **1.04 vCPU** 를 못 넘는다. 부하기는
> 0.55 vCPU 라 한계가 아니다. **이것이 GIL 벽 자체다.**
>
> **그래서 「9.5 of 16」은** 파이썬 ~1코어(GIL 직렬화) + MediaPipe ~8.5코어(C++ 에서
> GIL 놓음, 병렬)다. 프로세스를 쪼개면 1.30배가 나온 이유가 **GIL 이 프로세스당 하나**라서다.
> 요청당 GIL 은 현행 **3.8ms** · 널 경로 0.86ms — 위 어림 «~2.8ms» 와 같은 자리다.
>
> 🔴 **이 판이 안 답한 것**: 「그래서 몇 개로 쪼개나」. 근거는 여전히 1.30배 하나뿐이다.

### 10-2. ~~2순위 — 검출기 획득 경로~~ → 🔴 **지워졌다 (2026-08-23)**

> **`lease` 실측 p50 = 0.01ms**([결과 §3](../../loadtest/results/frame-path-r10a-2026-08-23/README.md), B 계열 10판 · `partial 0`).
> 전체 핸들러 시간 106.9ms 의 **0.0%** 다. 풀 201 에 세션 160 이라 경합이 없다는 산수와도 맞는다.
> **이 후보는 닫힌다.** 아래는 그때의 근거이고 기록으로 남긴다.

풀은 201개지만 **리스 획득 지점에 전역 락·큐**가 있으면 거기서 직렬화된다. 그리고 검출기가
**지연 생성**이라 새 세션마다 MediaPipe 인스턴스를 만든다 — §9-4 의 **「재시작하면 −24%」**
가 그 비용을 시사한다.

### 10-3. 3순위 — 세션별 유입 속도 제어

`accept_frame` 에 데드라인 로직이 있다([#162](https://github.com/Shadowfit/init/issues/162)).
`348 ÷ 160 = 세션당 2.18 fps` 인데 클라는 3 fps 를 노린다. **이 숫자가 우연인지 확인 안 했다.**

---

## 11. 🔴 도구 한계 — py-spy 로는 더 못 간다

오늘 세 번 썼고 **두 번 무효**였다.

| 시도 | 결과 |
|---|---|
| `py-spy dump` (초반, 재시작 전) | ✅ 정상 — 4~7 워커 busy · MediaPipe 프레임 관측 |
| `py-spy record --idle` | ❌ **스레드당 스택 1개 · 60초 내내 변화 0 · MediaPipe 0건** |
| `py-spy dump` ×30 (재시작 여러 번 후) | ❌ **630 샘플 전부 파킹 · MediaPipe 0건** |

마지막 판은 **244.5 RPS · 검출 100% · `nolease=0`** 으로 멀쩡히 돌던 판이다.
`244 × 26ms = 동시 6.4개` 가 일하고 있어야 하는데 **30번 다 0개를 잡을 확률은 사실상 0** 이다.
**컨테이너를 여러 번 재시작한 뒤로 py-spy 가 제대로 못 붙는 것으로 보인다**(미확인).

→ **1순위(GIL)를 재려면 py-spy 가 아니라 «앱에 붙이는 계측» 이 필요하다.**

---

## 12. 다음 한 걸음 — 계측을 붙인다 (🟢 **붙였다, 2026-08-23** · 한 판도 안 돌았다)

**박스가 필요 없다. 필요한 건 코드다.**

> 🟢 **2026-08-23: 코드가 들어갔다.** `ai-server/app/observability/frame_path.py` ·
> 읽기 `GET /api/v1/diag/frame-path` · 초기화 `POST /api/v1/diag/frame-path/reset` ·
> 스위치 `FRAME_PATH_METRICS`(기본 **OFF**) · `GIL_SWITCH_INTERVAL`(기본 **0 = 안 건드림**).
> 🔴 **아직 아무것도 안 쟀다.** 붙인 것과 재는 것은 다른 일이고, 아래 ⚠️ 두 개는 그대로 살아 있다.
> 🟢 **2026-08-23: 그 «재는 일» 이 從 R10 으로 등록됐다**([`AWS-RIDE-ALONG.md` §1 從](../../loadtest/AWS-RIDE-ALONG.md)) — `c7i.4xlarge` **한 대 · ~30분**.
> 계측의 대가는 먼저 쟀다: **A 팔 판 간 산포(2.4%) 이하**([결과](../../loadtest/results/frame-path-overhead-2026-08-23/README.md)) — **켜도 된다.**
> 🔴 **2026-08-23: 그 «한 대» 가 분기다.** 기준 조건 「346 RPS · 9.5 vCPU」는 **부하기가 별도
> 박스(`c7i.xlarge`)에 있을 때** 난 관측이고, 현 rig 은 부하기를 대상 박스에 올린다 —
> 부하기 몫이 **평균 0.5 · 피크 1.0~1.4 vCPU**(§T 실측)라 **설명하려는 6.5 vCPU 의 8~22%** 다.
> 판정선 넷 중 `handler_concurrency`(절대값) 하나가 거기서 다친다.
> 🟢 **2026-08-23: 그 분기가 닫혔다 (사용자 confirm)** — [`r10-loadgen-topology.md` §7](r10-loadgen-topology.md):
> **쪼갠다. R10-a(1대 동거)를 먼저 돌리고, `handler_concurrency` 는 R10-b(2대)로 미룬다.**
> R10-a 가 쓰는 판정선은 상대값 셋(`GIL_SWITCH_INTERVAL` 팔 · 구간 비율 · `lease`)뿐이다 —
> 위 표의 「동시 처리 수 카운터」 칸은 **이 판에서 안 읽는다.**
> 🔴 **박스는 그대로 `c7i.4xlarge` 1대**이고, **EC2 실행은 아직 0판**이다.

| 계측 | 무엇을 답하나 | 구현 |
|---|---|---|
| **동시 처리 수 카운터** | 「~9」가 **산술 추론**이다 — ASGI 미들웨어에서 진입/이탈을 세면 **직접 값**이 나온다 | ✅ `handler_concurrency` — **최대값이 아니라 분포**다. 최대 하나로는 «가끔 9» 와 «내내 9» 가 안 갈린다 |
| **구간별 시간** | 수신 → 검증 → 워커 진입 → 추론 → 응답. **434ms 가 어디서 소모되는지** 갈린다 | ✅ 7구간 — `wait`·`decode`·**`lease`**·`infer`·`post`·`respond`·`total`. 🔴 **`lease` 를 따로 뗐다** — 합쳐 재면 후보 2순위(§10-2)가 「추론이 비싸다」로 읽히고 그건 R6 가 이미 반증한 답이다 |
| **GIL 점유** | 1순위 직접 검증. `sys.setswitchinterval` 을 흔들어 처리량이 움직이면 GIL 기여의 증거 | ✅ `GIL_SWITCH_INTERVAL` 기동 노브. ⚠️ **판을 만드는 손잡이지 튜닝 값이 아니다** — «좋은 값» 은 없다 |

🟢 **2026-08-26**: `decode`(base64 디코드+`cv2.cvtColor`) 값이 이미 여러 라운드에 찍혀 있다 —
mean 3.6~3.9ms, `infer`(36~38ms) 대비 **~9~10%**(`sticky-routing-probe-2026-08-26/aws-run/sticky_run.json`).
base64 제거의 서버 CPU 이득은 작다. 단 페이로드 크기(base64 +33%)의 **절대 바이트 수**는 여전히
미측정 — `takePictureAsync`가 `pictureSize`를 안 지정해 실기기 값이 없으면 답을 못 낸다.

⚠️ **셋 다 ai-server 코드 변경이다**([[feedback_minimize_python_changes]]) — 2026-08-23 에 붙였고,
서비스 경로에 남은 것은 **`mark_*` 호출 4줄**이다(계측이 꺼져 있으면 즉시 반환).

🔴 **계측 자체가 재려는 자원을 쓴다.** 켜면 요청당 **락 4회 + 타임스탬프 6회**가 늘고 그건
전부 GIL 을 잡는 일이다. 그래서 **「계측 ON/OFF」가 한 판의 팔**이고, 그 대조 없이 절대값을
인용하면 안 된다([[feedback_measure_design_needs_repeats]]).

⚠️ 그리고 **측정 조건이 오늘로 흔들렸다**(§9-4): 판마다 재시작하면 −24%, 판 순서로 −6.4% 드리프트.
**다음 라운드는 이 둘을 먼저 통제해야 절대값을 말할 수 있다.**

- **2026-08-23: §12 의 계측을 실제로 붙였다. 🔴 아직 한 판도 안 돌았다.**
  `app/observability/frame_path.py`(순수 ASGI 미들웨어 + 링 버퍼 + 읽기 라우트) 하나에 모으고,
  서비스 경로(`pose.py`)에는 **`mark_*` 4줄만** 남겼다 — 계측이 꺼져 있으면 그 넷은 즉시 반환한다.
  - **설계에서 하나 늘렸다** — §12 표는 구간을 「수신 → 검증 → 워커 진입 → 추론 → 응답」으로 적었는데
    **`lease`(검출기 획득)를 따로 뗐다.** 붙여서 재면 후보 2순위(§10-2)가 추론에 섞여 들어가고,
    그러면 결과가 「추론이 비싸다」로 읽힌다 — **R6 가 이미 반증한 답**이 다시 나온다.
  - **`BaseHTTPMiddleware` 를 안 썼다.** 그건 요청마다 태스크와 메모리 스트림을 하나 더 만든다.
    재려는 것이 **이벤트 루프의 여유**라 재는 도구가 그 자원을 더 먹으면 안 된다.
  - 🔴 **핸들러는 다른 스레드에서 돈다**(FastAPI 가 `def` 핸들러를 threadpool 로 보낸다).
    contextvar 가 그 경계를 넘어가는지가 이 계측의 **유일한 실패 모드**이고, 안 넘어가면
    구간이 **조용히 빈다**(0 이 아니라 무음이다). `test_frame_path_metrics.py` 의 첫 테스트가 그것만 지킨다.
  - «표본 없음» 을 **0ms 로 적지 않는다**. 조기 반환(NO_LEASE·NO_POSE·RATE_LIMITED)이면 뒷 구간은
    비고, 그건 `partial` 로 따로 센다 — 0 으로 채우면 「빠르다」로 읽힌다.
  - ⚠️ **측정 조건은 아직 안 통제됐다**(§9-4: 재시작 −24% · 판 순서 −6.4%). 이 계측은 프로세스
    메모리라 **재시작하면 사라진다** — 판마다 재시작하는 절차와 같이 쓰면 안 된다.
  - 검증: 신규 테스트 7 + `pose.py` 경로를 덮는 기존 테스트 8 통과, 실제 앱 기동·라우트 등록 확인.
    미검증: **부하 하에서 안 돌려봤다.** 계측 ON 의 처리량 대가도 아직 모른다.

- **2026-08-23: §3-ㄹ 이 물었던 계산을 했다 — 「안 넘는다」인데 여유가 인용되던 것의 절반이다.**
  §3-ㄹ-1. **DAU 1,000 의 피크 동시는 67**(§4.2 값 재현), **배포 구성의 실측 천장은 89~105** 이므로
  **안 넘는다.** 그런데 여러 문서가 인용하는 **«2.3배 여유» 는 156/67** 이고 그 **156 은 단독 박스**
  값이다 — 배포 구성(MySQL·Spring 동거 + HTTP)에서는 **1.33~1.57배**다([#385](https://github.com/Shadowfit/init/issues/385)).
  - 🔴 **「67」에 감춰진 가정을 역산해 꺼냈다** — §4.2 는 `1,500세션/일 → 피크 동시 ~67` 이라고만
    적고 중간 단계를 안 적었다. 역산하면 **피크 1시간에 하루의 18%** 라는 가정이 있어야 닫힌다.
    그 값은 **repo 어디에도 없었다.**
  - **가정 하나만 움직이면 DAU 1,000 안에서 닿는다** — 세션이 30분이면 DAU 659, 피크가 25% 면
    DAU 949 에서 89세션이다. 세션 15분은 **타임아웃 계산용 예시값**이지 실측이 아니다.
  - 그래서 §3-ㄹ 은 여전히 **1순위 갈래**다. 다만 「고칠 필요 없다」가 아니라
    **「지금 가정에서만 필요 없다」**로 적었다 — 그 둘은 면접에서 다른 말이다.

---

## 13. R10-a 격자 (2026-08-23 작성, 미실행)

무대는 [`r10-loadgen-topology.md`](r10-loadgen-topology.md) §7 이 정했다 — **1대 동거 · `c7i.4xlarge`**.
이 절은 **그 한 판의 격자**다. §8 과 같은 규칙으로 쓴다: **판정선을 실행 전에 박는다.**

### 13-0. 답하는 것 / 안 답하는 것

| | |
|---|---|
| ✅ **주 질문** | `GIL_SWITCH_INTERVAL` 을 흔들면 처리량이 움직이나 → **움직이면 1순위(서비스 경로 GIL) 확정 · 안 움직이면 지워진다** |
| ✅ | 구간 비율 — 434ms 가 어디서 소모되나 |
| ✅ | `lease` 가 큰가 (§10-2) |
| ✅ | 계측의 대가 (이 박스에서의 앵커) |
| 🔴 **안 읽는다** | `handler_concurrency` — 절대값이라 동거에서 다친다. **값은 걷되 판정선에서 뺀다**(무대 §7) |
| 🔴 안 읽는다 | 절대 처리량 · 「9.5 of 16」 — 그건 R10-b(2대)의 몫이다 |

### 13-1. 팔 넷

| 팔 | 계측 | `GIL_SWITCH_INTERVAL` | 무엇을 위한 팔인가 |
|---|---|---|---|
| **A** | OFF | 0 (파이썬 기본 0.005) | 기준 처리량 · 앵커 한쪽 · **GIL 팔의 대조군** |
| **B** | **ON** | 0 | 구간 비율·`lease` 를 걷는 유일한 팔 · 앵커 다른쪽 |
| **C** | OFF | **0.0005** | 자주 전환 |
| **D** | OFF | **0.05** | 드물게 전환 |
| **E** | **ON** | **0.0005** | 🆕 **계측 × GIL 상호작용** — 2×2 를 닫는 칸 |

**C·D 가 계측 OFF 인 것이 이 격자의 핵심 하나다.** 주 질문은 **처리량 비교**인데, 계측을 켜면
그것도 GIL 을 잡는다(§12 🔴) — 재려는 자원을 재는 도구가 같이 쓰면 안 된다.
**A·C·D 는 계측 OFF 로 고정되고 오직 노브 하나만 다르다.**

⚠️ **0.0005 / 0.05 는 근거 있는 값이 아니다.** 파이썬 기본이 0.005 이므로 **「기본에서 양방향
한 자릿수」** 라는 **규칙**으로 정했다. 값이 아니라 **방향 둘**을 보려는 것이다 — 한쪽만 흔들면
「안 움직였다」와 「그 방향이 아니었다」가 **안 갈린다.**

### 13-1-1. E 는 왜 있나 — [#421](https://github.com/Shadowfit/init/pull/421) 이 물은 것을 2×2 로 닫는다

이 격자의 초판에는 E 가 없었다. [#421](https://github.com/Shadowfit/init/pull/421) 이 **「계측이 GIL 과 상호작용하는지」** 를 따로
물었고(그쪽 팔 이름은 `B@0.001`), 그건 A·B·C·D 로는 **못 답하는 질문**이다.

E 를 **`C` 와 같은 GIL 값(0.0005)** 으로 두면 넷이 **2×2** 가 된다:

| | GIL 기본 | GIL 0.0005 | 이 행이 주는 것 |
|---|---|---|---|
| **계측 OFF** | A | C | 노브의 «깨끗한» 효과 (Δ_off) |
| **계측 ON** | B | E | 계측이 켜진 채의 효과 (Δ_on) |

**Δ_off ≠ Δ_on 이면 상호작용이 있다** — 그러면 B 팔의 구간값을 GIL 조건과 분리해 읽을 수 없다.
값을 아무 데나 새로 고르지 않고 **C 와 맞춘 것**이 이 칸의 전부다. 값이 다르면 2×2 가 안 닫힌다.

> 🟢 **#421 이 잡은 결함은 그대로 살아 있다.** [#408](https://github.com/Shadowfit/init/pull/408) 의 예시 `--plan "A,A@0.001,A,A@0.001"` 은
> **전부 계측 OFF** 라 `frame_path` 가 아예 안 실리고, 그러면 판정선 셋 중 둘(구간 비율·`lease`)이
> 빈다. 이 격자는 **B 와 E 로 계측 ON 판을 10판** 갖는다.

### 13-2. 조건 — 기준 라운드를 그대로 재현한다

| | 값 | 근거 |
|---|---|---|
| 세션 | **160** | §2-2 의 그 판 |
| fps | **3** | `load_ai.py` 기본. 유입 상한(300ms) 위로 올리면 재는 것이 달라진다 |
| 판 길이 | **90초** | §2-2 (160세션 · 90초) |
| 검출기 풀 | **201** | 기준 라운드의 컨테이너 유도값과 같게 **명시**한다(venv 엔 cgroup 한도가 없다). 201 × 98.7MB ≈ 19.4GB < 31GB |
| 프레임 | `coresidency-2026-08-15/frames.json` (커밋본) | 기준 라운드와 같은 자산 |
| 무대 | **AI + 부하기 한 박스** | 무대 §7 결정 |
| 워밍업 버림 | 🔴 **버림판이 정한다** — 13-3 | 08-23 판의 5초는 **8세션** 값이다 |

### 13-3. 순서 — 5×5 라틴 방격 + 버림판 1

```
버림:  A
블록1: A B C D E
블록2: B C D E A
블록3: C D E A B
블록4: D E A B C
블록5: E A B C D
```

각 팔이 **각 위치에 정확히 한 번**씩 온다. 재기동이 −24%, 판 순서가 −6.4% 를 흔들기
때문에(§9-4) 위치가 팔에 얹히면 안 된다([[feedback_measure_design_needs_repeats]]).

🔑 **버림판의 두 번째 임무**: 그 판의 시간대별 처리량·검출률을 보고 **워밍업 버림 구간을
정한다.** 그리고 rig 이 돌려주는 `setup_sec`(세션 160개 여는 시간)이 거기서 처음 나온다 —
**소요 예측(13-7)이 그 값에 달려 있다. 숫자를 미리 박지 않는다.**

러너에 넘길 `FP_PLAN` 은 이것이다(rig 표기로 옮긴 것 — `C`=`A@0.0005`, `D`=`A@0.05`):

```
A,A,B,A@0.0005,A@0.05,B@0.0005,B,A@0.0005,A@0.05,B@0.0005,A,A@0.0005,A@0.05,B@0.0005,A,B,A@0.05,B@0.0005,A,B,A@0.0005,B@0.0005,A,B,A@0.0005,A@0.05
```

⚠️ **26개다**(버림 1 + 본판 25 = 5팔 × 5판). `FP_DISCARD=1`.

### 13-4. 사전 판정선 (실행 전에 박는다)

#### ㄱ. 주 질문 — GIL 노브

**A 대비 C 또는 D 의 처리량이 «A 팔 자신의 판 간 산포» 를 넘어 움직이면 지지.**
산포는 이 라운드가 스스로 준다(A 팔 4판 + 버림 1). 08-23 판이 쓴 것과 같은 규칙이고,
**임의의 % 를 안 박는다.**

- 움직인다 → **1순위 확정.** R10-b 는 «어디서» 만 재면 된다
- 안 움직인다 → **1순위가 지워진다.** 남은 후보가 «수신 경로(이벤트 루프)» 하나로 좁혀지고,
  **R10-b 의 격자가 줄어든다** — 쪼갠 값이 여기서 나온다

#### ㄴ. 434ms 3분할

기준: 160세션에서 **부하기 p50 ≈ 450ms**(§2-2), c7i 순수 추론 **14.0ms**(R7 중앙).
→ **약 435ms 가 추론 밖**이고, 그것이 셋 중 어디에 있는지 가른다.

| 몫 | 어떻게 읽나 | 과반이면 |
|---|---|---|
| **ASGI 앞** | 부하기 p50 **−** 서버 `total` | **수신 경로** — 계측 미들웨어보다 앞이다(소켓·이벤트 루프 읽기·accept) |
| **워커 진입 대기** | 서버 `wait` | **GIL 지지** |
| **리스 획득** | 서버 `lease` | **§10-2 확정** |

🔑 계측은 **ASGI 안쪽만** 본다. 그 **바깥**은 「부하기가 잰 값 − 서버가 잰 값」으로만 보이므로
**두 표를 같은 판에서 걷어야** 성립한다.

🔴 **1대 동거의 편향을 여기 박아둔다** — CPU 경합은 `wait` 을 **부풀리는 쪽**으로 편향된다.
그래서 이 3분할은 **「어디가 과반인가」까지만 읽고 «몇 ms 인가» 는 인용하지 않는다.**
크기가 필요하면 R10-b 다.

#### ㄷ. 계측 × GIL 상호작용 (2×2)

**Δ_off = A→C 처리량 변화 · Δ_on = B→E 처리량 변화.**
**두 델타의 차가 A 팔 판 간 산포보다 크면 상호작용 지지.**

- 상호작용 **없다** → B 팔의 구간값을 GIL 조건과 분리해 읽어도 된다. 기본 가정이 선다
- 상호작용 **있다** → 🔴 **B 팔 구간값에 「GIL 기본 조건에서」라는 단서가 붙는다.**
  그리고 계측 자체가 이 경로의 GIL 을 만진다는 뜻이라 **주 질문(ㄱ)의 해석도 좁아진다**

#### ㄹ. 앵커 — 계측의 대가

**A ↔ B 처리량 차가 A 팔 판 간 산포보다 크면** 「이 박스에서는 대가가 있다」이고,
그러면 **B 팔 구간값 인용에 조건이 붙는다.** 로컬 판정(≤2.4%)은 물리 2코어 값이라 못 옮긴다.

#### ❌ `handler_concurrency` — 판정선이 아니다

값은 걷힌다(계측이 켜진 B 팔에서). **그러나 이 라운드에서는 읽지 않는다** — 부하기가 같은
박스의 CPU 를 먹으므로 「9 에 눌린 것」과 「부하기에 밀려 9 인 것」이 안 갈린다(무대 §5).
결과 문서에 **「걷었고 안 읽었다」로 적을 것.** 지우지 않는다 — R10-b 와 대조할 값이다.

### 13-5. 유효성 게이트 — 판정 **전에** 판을 버린다

| 조건 | 왜 |
|---|---|
| `nolease > 0` | 🔴 **풀 소진.** §2-3 에서 이게 RPS 480 으로 «천장 돌파» 처럼 보였고 그 판들은 전부 무효였다 |
| `setup_fail > 0` | 세션이 다 안 열렸다 = 부하 수준이 다르다 |
| `nopose` 가 유의미 | 검출률이 100% 가 아니면 프레임이 다른 갈래를 탄다 |
| B 팔의 `partial` 이 큼 | 구간이 **조용히 빈** 것이다(0 이 아니라 무음, §12) |
| `cpu.error` 가 있음 | CPU 축이 통째로 빈다 — 부하기 몫(13-6)을 못 뗀다 |

### 13-6. 걷는 것 — CPU 가 이번엔 있다

| | 어디서 |
|---|---|
| 요청별 지연·분류 | 부하기 TSV |
| 구간·동시성 스냅샷 | 서버 `GET /api/v1/diag/frame-path` (**서버를 내리기 전에**) |
| **AI · 부하기 CPU** | rig 의 `/proc` 샘플러([#419](https://github.com/Shadowfit/init/pull/419)) — `docker stats` 와 같은 눈금(100% = 1 vCPU) |
| 스택 조건 | `bootstrap.sh` 의 `/root/ai_venv_conditions.txt` ([#413](https://github.com/Shadowfit/init/pull/413)) |

🟢 **덤이 하나 나온다** — **1대 동거에서 부하기가 실제로 얼마를 먹는지**가 이 라운드에서 처음
걷힌다. 무대 문서 §8 의 첫 줄(「잰 적이 없다」)이 그 자리이고, **R10-b 를 정당화하는 근거가
추정에서 실측으로 바뀐다.**

### 13-7. 소요 (예상)

```
판당   90초(부하) + setup(160세션 여는 시간, 🔴 미측정) + 기동·헬스·정리
판 수  버림 1 + 본 측정 25 = 26
```

🔴 **`setup` 을 모르므로 총 소요를 못 박는다.** 08-23 판(8세션)의 `setup_sec` 는 0.45초였고
160세션이 그 20배라는 보장이 없다 — **버림판이 그 값을 처음 준다**(13-3). 판당 120초면 **52분**,
160초면 **69분**이다. **인스턴스는 1.5시간으로 잡고, 버림판 뒤에 다시 계산할 것.**

⚠️ **팔이 넷에서 다섯이 되면서 판이 17 → 26 으로 늘었다**(라틴 방격은 팔 수의 제곱이다).
**줄여야 하면 `D`(GIL 0.05, 둘째 방향)를 뺀다** — 그러면 4팔 · 17판 · 34~45분이다.
🔴 **잃는 것은 반증력이다**: 한 방향만 흔들면 「안 움직였다」와 「그 방향이 아니었다」가
안 갈리고, 그러면 **주 질문에서 「1순위를 지운다」를 못 한다**(13-4 ㄱ). 쪼갠 값의 절반이
거기서 나온다는 것을 알고 자를 것.

### 13-8. 이 격자가 못 하는 것

- **「왜」가 아니라 「어디」만 답한다.** 435ms 가 ASGI 앞에 있다고 나와도 *그 안의 무엇*은 다음 판이다
- **절대값을 못 준다** — 1대 동거다. 3분할도 «과반» 까지만이다(13-4 ㄴ)
- **`handler_concurrency` 를 안 읽는다** — 하필 이 계측을 붙인 이유였고, R10-b 로 미뤄져 있다
- **C·D 의 값은 규칙이지 근거가 아니다**(13-1 ⚠️)
- **keep-alive 를 안 쓴다** — `urlopen` 이라 요청마다 새 연결이다. §2-4 가 그 축에서 답을 못 냈으므로
  **기준 라운드와 같은 조건을 유지**하는 쪽을 골랐다. 실제 모바일 클라이언트와 다르다는 조건이 붙는다
- 🟢 08-23 rig 결함 §3-1(Windows 타이머 15.6ms 양자화)은 리눅스라 사라진다 — **단 확인은 안 됐다**
