# 동거 용량 — 「156세션」은 혼자 살 때의 값이다

작성일: 2026-08-15
상태: **3라운드 회수 완료 (2026-09-08)** — 예비 1회(08-15, #245 이전) + 정식 1회(08-16, 6레벨·라틴 방격, 본 측정 54 관측)
      + b 라운드(08-16, 미회수분) + 정식 2라운드(08-17, 8레벨 · 91행 · 3h33m) + **從 R9(09-08, 팔 D 첫 실측 · 91행)**.
      ✅ **못 닫았던 것 중 H5(관측 스택 비용, AI 부하 기준 Q5)도 답이 나왔다** — C↔D 차이 ±3~7%, 두 자릿수%는
      어디서도 안 나옴(가설 지지). H3(캡 대가)도 09-08 라운드에서 더 큰 폭으로 재확인(120세션 이상 처리량이
      245~250 rps 근방에 고정). 판정은 **§6 표의 H3·H5 행 안에 09-08 결과를 병기**, 원본은
      [`coresidency-aws-2026-09-08/`](../../loadtest/results/coresidency-aws-2026-09-08/README.md).
      🔴 **여전히 남은 것**: 「몇으로 캡을 걸 것인가」는 이 라운드로도 못 정한다 — 옆(Spring·MySQL)이
      캡 없이도 안 아팠다는 게 재확인됐을 뿐, **이 호스트가 실제로 다른 워크로드와 동거하는지(사실)와
      동거한다면 그 워크로드의 요구치(정책)가 먼저 있어야** 캡 값 논의가 성립한다 — 둘 다 미확인.
      그 외: 라운드 간 **절대값 비재현(+17.7%, 08-16↔08-17)** · 「346 RPS 천장에서 서버가 **9.5 vCPU** 만
      쓰는 이유」(→ [`ai-receive-path-scaling.md`](./ai-receive-path-scaling.md)).
      ⚠️ **08-16 의 «부하기가 천장이었다» 는 08-17 §T 가 반증했다**(§12-1 갱신). §12 는 그 경위의 기록으로 읽을 것.
      rig: [`../../loadtest/results/coresidency-2026-08-15/`](../../loadtest/results/coresidency-2026-08-15/README.md) · 측정값은 `coresidency-aws-<측정일>/` 로 따로 남는다

대상: 「한 대로 되나」를 사실로 만드는 것. **수평확장 결정의 선행 조건**이지 수평확장 설계가 아니다.
연관: [`./load-test-strategy.md`](./load-test-strategy.md) §1, [`../19-deployment.md`](../19-deployment.md) §7,
[`../../loadtest/results/ai-concurrency-aws-2026-08-14/`](../../loadtest/results/ai-concurrency-aws-2026-08-14/README.md),
[#212](https://github.com/Shadowfit/init/issues/212) · [#214](https://github.com/Shadowfit/init/issues/214),
[[project_loadtest_env_constraint]] · [[feedback_measure_design_needs_repeats]]

> 📊 **정식 라운드 결과 (2026-08-16, 6레벨 · 라틴 방격 · 본 측정 54 관측):**
> [`../../loadtest/results/coresidency-aws-2026-08-16/`](../../loadtest/results/coresidency-aws-2026-08-16/README.md)
> — 천장은 **80 초과 120 미만**(팔 B plateau 267.6 fps = 89.2세션 환산) · 뺏는 주체는 **AI 자신**
> (869~893% vs Spring 4.5% · MySQL 7.8%) · **HT 가 거의 안 준다**(16 vCPU 박스에서 천장 ~8.9 vCPU) ·
> 팔 C 캡은 **깎은 만큼 그대로 깎는다**(CPU 90.8% → 처리량 92.9%, 3판 전부 같은 방향) →
> **예비의 「팔 C 교락」이 닫혔다** · 從 R5 실측식 `708 + 106.74×N` 이 **오차 ±0.7% 로 재현**.
> 🔴 단 **팔 A 가 2시간에 걸쳐 +16% 흐른다** — 「동거 비용(A−B)」이 라운드마다 −0.8%/−6.2%/−13.3% 라
> **H1 은 지지도 반증도 안 됐다.** 🔴 **부하기 지표는 이번에도 안 걷혔다**(§4-2 · [#250](https://github.com/Shadowfit/init/issues/250))
> — ✅ **이후 닫혔다**: 08-17 라운드가 부하기 자신을 걷었고, **346 RPS 는 서버 천장**으로 갈렸다
> ([08-17 결과 §2-3](../../loadtest/results/coresidency-aws-2026-08-17/README.md) · 아래 §12-1).
>
> 이전: [예비 라운드 (2026-08-15, #245 이전 코드)](../../loadtest/results/coresidency-aws-2026-08-15/README.md)
> — 동거 천장 ≈ 97세션. 정식값과 약 8% 다르고 **원인은 못 가른다**(격자·코드·판 수가 전부 다르다).

---

## 0. 이 문서가 여는 것

2026-08-14 에 **물리 8코어에서 동시 156세션**을 실측했다. 가정 피크 67 의 2.3배라
「용량은 해결됐다」로 읽히기 쉽다. 그런데 그 값에는 조건이 하나 붙어 있다 —
**AI 컨테이너만 있는 박스**에서 난 값이다.

🟢 **2026-08-23: 그 조건을 뗀 값이 결국 이 문서의 라운드에서 나왔다.** 동거 실측 천장은
**89~105세션**이고, 가정 피크 67 대비 여유는 **2.3배가 아니라 1.33~1.57배**다 — 계산은 [§3-ㄹ-1](./ai-receive-path-scaling.md) · 인용처 정정은 [#385](https://github.com/Shadowfit/init/issues/385).

실제 배포는 그렇지 않다. `docker-compose.prod.yml` 은 **MySQL·Spring·AI 를 한 호스트**에
올린다(관측 스택은 profile 로 분리). 그러면 156 은 내려간다. **얼마나 내려가는지는 안 쟀다.**

> **그래서 지금은 「수평확장이 필요한가」에 답할 수 없다.**
> 동거 값이 120 이면 100 동접은 한 대로 끝나고 수평은 «성능» 이 아니라 **«가용성» 결정**이 된다.
> 40 이면 반대다. **같은 질문에 정반대 답이 나오는데 그걸 가르는 숫자가 없다.**

이 문서는 그 숫자 하나를 만든다.

---

## 1. 측정으로 답할 질문

| # | 질문 | 왜 이게 질문인가 |
|---|---|---|
| Q1 | **동거하면 동시 세션이 얼마가 되나** | 156 은 단독 값이다. 이 프로젝트의 모든 용량 서술이 그 위에 서 있다 |
| Q2 | **무엇이 먼저 뺏는가** — MySQL·Spring·AI 중 | 「내려갔다」만으로는 못 고친다. 뺏는 쪽을 알아야 캡을 어디에 거는지가 정해진다 |
| Q3 | **캡을 걸면 회복되나** ([#212](https://github.com/Shadowfit/init/issues/212)) | 캡의 목적은 «AI 를 보호» 가 아니라 «서로 안 뺏기» 다. 실제로 그 효과가 있는지는 미검증 |
| Q4 | **품질도 같이 내려가나** | 처리량이 아니라 **검출률**이다. #164 가 30% → 95.6% 로 고친 그 지표가 CPU 경합에서 다시 무너지는지 |
| Q5 | 관측 스택을 같이 올리면 얼마를 더 먹나 | 2026-08-08 에 obs 를 분리한 이유가 이것인데, **비용을 숫자로 적은 적이 없다** |

**Q2 가 급소다.** Q1 은 한 숫자지만 Q2 는 다음 작업을 정한다.

---

## 2. 🔴 기존 rig 으로는 못 잰다 — 새로 만들어야 하는 이유

[`loadtest/measure_ai_concurrency.py`](../../loadtest/measure_ai_concurrency.py) 가 이미 있고
08-14 의 156 을 낸 물건이다. 그런데 **이 실험에는 쓸 수 없다.**

```python
from app.core.mediapipe_detector import get_detector   # rig:50
```

**검출기를 프로세스 안에서 직접 부른다.** 컨테이너도, FastAPI 도, gRPC 도 안 거친다.
그건 의도였다 — 「프레임당 추론 비용」은 환경에 안 묶이는 양이라 그렇게 재야 옮겨 쓸 수 있다.

하지만 **동거는 정확히 «환경» 에 관한 질문**이다. 컨테이너 경계·cgroup·네트워크 스택이
빠지면 잴 대상 자체가 없다. 그래서 이 실험의 부하는 **실제 서비스 경로**여야 한다:

```
부하기 ──POST /api/v1/pose (8000, AI_PUBLIC_TOKEN)──> AI 컨테이너
                                                        │ rep 완성 시
                                                        ↓ gRPC SavePoseDataBatch
                                                     Spring ──> MySQL
```

프론트 → AI HTTP 직결(분기 H2)이 실제 프레임 경로이므로 **그 경로에 건다.**

> 🔴 **초판은 여기에 «Spring·MySQL 부하는 AI 부하의 결과로 자연히 따라온다» 고 적었다.
> rig 을 짜면서 실측으로 반증됐다** (2026-08-15, [rig README §2](../../loadtest/results/coresidency-2026-08-15/README.md)).
> 합성 인체의 무릎 각도가 **86.95° ~ 124.30°** 라 rep 상태기계의 «서있음»(≥155°) 문턱에
> 30° 이상 모자란다 — **rep 이 영원히 0 이고, 따라서 `SavePoseDataBatch` 가 한 번도 안 나간다.**
> 검출은 30/30 으로 멀쩡하다. 기존 rig 이 단언한 것이 «검출» 뿐이라 아무도 몰랐다.
>
> → **부하를 두 갈래로 나눈다**(사용자 결정): AI 는 프레임으로, Spring·MySQL 은 **기존 ghz
> `SavePoseDataBatch` rig** 으로 **동시에** 건다. rep 에 안 기대므로
> [#196](https://github.com/Shadowfit/init/issues/196)·[#192](https://github.com/Shadowfit/init/issues/192)
> (둘 다 열린 이슈)에 막히지 않는다.
> **대가**: 두 부하의 비율이 «생겨난» 값이 아니라 **«정한» 값**이다. 실사용의 프레임↔적재 비는
> 이 실험이 답하지 않는다 — 결과 문서에 이 문장을 그대로 넣는다.

> ⚠️ **그래서 이 rig 은 `measure_ai_concurrency.py` 를 대체하지 않는다.** 저쪽은 «프레임당 비용»,
> 이쪽은 «동거 상태의 서비스 용량» 이다. 두 값을 같은 표에 올리지 않는다.

---

## 3. 팔 설계

무대는 **한 박스**다. 팔은 그 박스에 무엇이 같이 사는가로 갈린다.

| 팔 | 구성 | 성격 |
|---|---|---|
| **A** | AI + Spring + MySQL, 캡 없음, **ghz 부하 없음** | **유휴 동거 기준선.** 🔴 초판의 «AI 단독» 은 성립할 수 없어 2026-08-16 에 재정의됐다(§3-1) |
| **B** | A 와 **같은 구성** + **ghz 부하** | 현행 `docker-compose.prod.yml` 이 «일하는» 상태. Q1·Q2 |
| **C** | AI + Spring + MySQL, **캡 있음** | [#212](https://github.com/Shadowfit/init/issues/212)·[#214](https://github.com/Shadowfit/init/issues/214) 적용. Q3 |
| **D** | C + 관측 스택(`--profile obs`) | Q5. **선택** — 라운드가 길어지면 뺀다 |

### 3-0. 🔴 팔 A 재정의 (2026-08-16) — 「AI 단독」은 이 부하기로 성립하지 않는다

초판의 팔 A 는 **AI 컨테이너만 띄우고** 재는 것이었다. 그런데 이 rig 의 부하기는 세션을
**Spring 을 통해** 연다(`load_ai.py:107` → `POST /exercises/sessions`). 프레임이 수락되려면
세션 전용 검출기 자리가 있어야 하고, 그 자리는 gRPC `StartAnalysis` 가 만들며, 그것을 부르는
것은 Spring 이기 때문이다. **Spring 을 내리면 팔 A 의 전 판이 `setup_fail` 이다**
([#222](https://github.com/Shadowfit/init/issues/222)).

**세 안 중 (a) 를 채택한다** (2026-08-16 사용자 결정):

| 안 | 내용 | 판정 |
|---|---|---|
| **(a)** | 팔 A 를 «B 와 같은 구성 + **ghz 부하 없음**» 으로 재정의 | ✅ **채택** |
| (b) | 부하기가 AI 의 세션 시작을 직접 부른다 | ✗ 실물 경로가 아니게 된다 |
| (c) | 팔 A 를 빼고 08-14 의 156 을 그 자리에 쓴다 | ✗ rig 간 비교가 된다 — §3-1 이 막으려던 바로 그것 |

**대가 두 개를 그대로 적어둔다.**

1. **§3-1 의 산출물이 순수하지 않아진다.** A 에 MySQL·Spring 이 유휴로 얹히므로
   A↔156 의 차이는 «서비스 경로 오버헤드» 가 아니라 **«경로 오버헤드 + 유휴 동거 비용»** 이고,
   이 라운드는 그 둘을 **안 가른다.** 결과 문서에 이 문장을 그대로 넣는다.
2. **[#223](https://github.com/Shadowfit/init/issues/223)(ghz 부하 미배선)이 «품질 캐비엇» 에서
   «선행 조건» 으로 승격된다.** (a) 이후 A 와 B 를 가르는 것은 ghz 하나뿐이라, 그것이 없으면
   두 팔이 **같은 조건**이다. 스윕이 그 상태를 시작 전에 거부한다
   (`coresidency_sweep.sh` `assert_arms_distinguishable`) — 「같은 것을 두 번 재고 표는 정상」이
   #201·#202 와 같은 부류라 사후 주의가 아니라 원천에서 막는다.

> 📌 **이 결함은 「무엇을 재는가」를 바꾼 것이지 rig 의 버그가 아니었다.** 초판이 «AI 단독» 을
> 팔로 세울 수 있다고 본 전제는, 부하가 **실제 서비스 경로여야 한다**(§2)는 이 문서 자신의
> 요구와 애초에 양립하지 않았다. §2 를 쓰는 순간 팔 A 는 이미 불가능했다.

### 3-1. 팔 A 가 왜 필요한가 — 「재현」이 아니라 「환산」이다

⚠️ **아래는 초판의 논리다. §3-0 의 재정의로 절반만 남았다** — 「rig 이 달라서 내려간 몫」은
여전히 A 가 걷지만, 그 값에 «유휴 동거 비용» 이 섞인다.


08-14 의 156 은 **다른 rig**(in-process)으로 잰 값이다. 새 rig 은 HTTP·base64 디코딩·
컨테이너 경계를 포함하므로 **같은 박스에서도 다른 숫자가 나온다.** 팔 A 가 없으면
B 와 156 을 비교하게 되고, 그러면 **「동거 때문에 내려간 몫」과 「rig 이 달라서 내려간 몫」이
분리되지 않는다.**

📌 그리고 A ↔ 156 의 차이 자체가 산출물이다 — **서비스 경로의 오버헤드**가 그 값이다.

### 3-2. 팔 C 가 #212 의 값을 만들어 준다

[#212](https://github.com/Shadowfit/init/issues/212) 는 「관측 스택·MySQL 에 얼마를 줄지」의
근거가 없어 열려 있다. 팔 B↔C 대조가 **캡이 실제로 무엇을 지키는지**를 보여주면
그 값이 판단이 아니라 측정 위에 선다. **이 실험을 하는 두 번째 이유다.**

---

## 4. 지표 — 걷는 것과 보는 것

| 축 | 지표 | 출처 | 1라운드 |
|---|---|---|---|
| **용량** | 포화 시 초당 처리 프레임 → ÷ 3fps = 동시 세션 | 부하기 | ✅ |
| **지연** | 프레임 응답 p50·p99 | 부하기 | ✅ |
| **품질** 🔴 | **검출률**(랜드마크가 실제로 나온 프레임 비율) | 응답 본문 | ✅ |
| 귀속 | 컨테이너별 CPU·메모리 (cgroup) | `docker stats` 샘플러 | ✅ |
| 귀속 | MySQL `threads_running`·`innodb_os_log_fsyncs` | mysqld_exporter | 🔴 **안 걷혔다** |
| 귀속 | Spring gRPC 처리량·p99, HikariCP `pending` | `/actuator/prometheus` | 🔴 **안 걷혔다** |
| **從** | **AI 컨테이너 RSS 실사용 ↔ 유도식 차이** ([#229](https://github.com/Shadowfit/init/issues/229)) | 같은 `docker stats` 샘플러 | ✅ |
| 🆕 무대 | **부하기 자신의 CPU·메모리·네트워크** ([#250](https://github.com/Shadowfit/init/issues/250)) | `/proc` 샘플러 (부하기 로컬) | 🔴 2회 다 누락 |
| 🆕 분리 | **대상 박스 로컬 1세션 프로브의 RTT** | 자기 프로브 (§4-2) | — 2라운드 신규 |
| 🆕 從 부하 | ghz 지연 p50·p95·p99 ([#254](https://github.com/Shadowfit/init/issues/254)) | ghz JSON `latencyDistribution` | 🔴 표에 열이 없다 |

🔴 **표의 두 줄(mysqld_exporter·actuator)이 1라운드에서 통째로 비었다.** 설계가 요구한 세
소스 중 `docker stats` 하나만 걷혔고, 나머지 둘은 **스크레이프하는 코드가 rig 에 없다**
(관측 스택은 전 팔에서 `stop` 시킨다). 그래서 **H3 은 판정할 열 자체가 없었다**
([#254](https://github.com/Shadowfit/init/issues/254) · §6).

### 4-1. 從 — 메모리 유도식의 «공식 밖 몫» ([#229](https://github.com/Shadowfit/init/issues/229))

> ✅ **답변·반영 완료 (2026-09-02).** 아래는 이 라운드가 **묻던 시점**의 질문이라 그대로 둔다.
> 결과·결정은 [`detector-pool-ceiling-formula.md`](detector-pool-ceiling-formula.md) —
> 공식이 `100.5+98.7×N` 에서 실측 `708+106.74×N`(MiB)으로 바뀌었다
> (`ai-server/app/core/mediapipe_detector.py`). 아래 §의 100.5·98.7 은 **그 시점의 옛 값**이다.

**이 라운드에 얹으면 추가 비용이 거의 0 이다.** 이미 동시 세션을 ramp 하며 `docker stats` 로
컨테이너별 메모리를 걷는다 — 필요한 것은 **걷은 값을 유도식과 대조하는 것뿐**이다.

검출기 풀 상한 공식이 **검출기만** 센다(`memory_ceiling()`):

```
상한 = (컨테이너 한도 − 기본 RSS 100.5MB) ÷ 검출기 98.7MB
```

그리고 코드 자신이 *"프레임 버퍼·base64 임시·파이썬 힙·gRPC 는 **미측정**이라 안 뺐다"* 고
경고한다. 그래서 공식에 **딱 맞추면 풀이 세션을 거절하기 전에 컨테이너가 OOM 으로 죽고**,
이 프로젝트가 전제로 삼는 비대칭 실패(«덜 받아짐»=안전)가 **«죽음»=위험으로 뒤집힌다.**

| 세션 수 N 에서 볼 것 | 무엇을 답하나 |
|---|---|
| N=0(기동 직후) RSS | 기본 RSS **100.5MB** 가 지금도 맞는가 (M2 실측값, x86 미검증) |
| `RSS − (100.5 + 98.7×N)` | **공식 밖 몫.** 이게 «여유분» 의 근거가 된다 |
| 그 몫이 N 에 비례하는가 | 비례하면 «세션당 상수», 아니면 «고정 오버헤드» 로 공식에 넣는다 |

🔴 **지금은 숫자를 못 적는다.** 여유를 «몇 %» 로 할지 근거가 없어 `docker-compose.prod.yml`·
`.env.example`·`19-deployment.md` 는 **경고만** 달아뒀다([[feedback_no_arbitrary_threshold_values]]).
이 從 이 그 빈칸을 채운다.

⚠️ **팔 A~D 전부에서 걷는다.** 캡이 걸린 팔(C·D)에서는 «한도에 얼마나 붙었나» 가 같이 보이고,
그게 [#212](https://github.com/Shadowfit/init/issues/212) 의 CPU 캡 값과 **같은 라운드에서**
답해지는 자리다.

🔴 **검출률을 처리량과 같은 급으로 둔다.** [#164](https://github.com/Shadowfit/init/issues/164) 가
검출률 30% → 95.6% 로 고친 전례가 있고([`pool-verify-2026-08-11`](../../loadtest/results/pool-verify-2026-08-11/README.md)),
그건 **처리량 지표에 안 잡히는 고장**이었다. CPU 를 뺏겨 프레임이 밀리면 같은 종류의 붕괴가
다시 날 수 있다 — **처리량만 보면 「느려졌네」로 읽고 넘어간다.**

### 4-2. 🆕 자기 프로브 — 「서버가 느린 것」과 「부하기가 느린 것」을 가른다 (2라운드)

1라운드 결과를 검산하면 plateau 가 **세션수 ÷ p50** 으로 오차 2% 안에 맞는다:

| | p50 | N ÷ p50 | 실측 plateau |
|---|---:|---:|---:|
| A 120 | 412.6ms | 290.8 | 286.8 |
| A 160 | 555.2ms | 288.2 | 284.9 |
| B 120 | 440.2ms | 272.6 | 267.8 |
| C 120 | 476.8ms | 251.7 | 249.0 |

부하기가 3fps 를 **페이싱**하므로(`load_ai.py:154` — 밀린 만큼은 버린다) 왕복지연이 간격
333ms 를 넘는 순간부터 처리량이 그 나눗셈으로 **자동 결정**된다.

📌 **그래서 이 rig 이 재는 「천장」은 RTT 가 333ms 를 넘는 지점이고, 그 RTT 가 어디서
났는지는 클라이언트 시계 하나로 원리적으로 안 갈린다.**
[#250](https://github.com/Shadowfit/init/issues/250) 의 CPU 샘플러가 「부하기 CPU 는 안
붙었다」를 보여줘도, 파이썬 스케줄링·큐잉으로 RTT 는 늘 수 있다. **샘플러만으로는 절반이다.**

→ 판마다 **대상 박스에서 1세션짜리 프레임 프로브**를 같이 돌린다(로컬 루프백).
부하기 p50 이 413ms 인데 로컬이 50ms 면 큐는 **부하기 쪽**이고, 둘 다 413ms 면 **서버**다.
부하 +3 req/s · 앱 변경 0 · 측정 시간 +0.
⚠️ 대가: 프로브가 검출기 슬롯 1개와 RSS ~107MiB 를 먹는다. **팔 A~C 전부에서 같이 돌므로**
그 비용은 팔 간 대조에서 상쇄된다. 조건 칸에 적는다.

**✅ 코드 반영 (2026-08-17)** — `coresidency_sweep.sh` 의 `setup_probe`·`start_probe`·`stop_probe`.
설계에서 정하지 않았던 것 셋이 짜면서 정해졌다:

| 정한 것 | 왜 |
|---|---|
| **부하기(`load_ai.py`)를 그대로 재사용**하고 대상 박스로 밀어 넣는다(`$SSH "cat > …"`) | 프로브를 따로 짜면 세션 개설 경로(온보딩 `preferredUrl` 같은 함정)가 두 벌이 된다. 자산은 크기로 대조해 «반쯤 간 파일» 을 막는다 |
| **`--dur 0` = SIGTERM 까지** | 프로브는 «판이 끝날 때» 같이 끝나야 하는데 그 시각을 미리 못 정한다 — 부하기 계정 준비가 세션 수에 비례해 길어져(160세션이면 20초 초과) 측정창 시작이 판마다 다르다. `kill -9` 면 그 판의 서버 시계가 통째로 사라진다(요약을 끝에 한 번 쓴다) |
| **부하기의 warm 창으로 잘라서** 요약 | 프로브는 판보다 먼저 시작해 «부하 없는 구간» 을 같이 담는다. 안 자르면 «서버가 빠르다» 로 치우친다. `window` 칸에 `warm`·`full`·`no_overlap`·`empty` 를 갈라 적고, **겹치지 않으면 백분위를 안 적는다** |

🔴 **계정 네임스페이스를 갈랐다**(`--user-prefix probe`). 안 그러면 프로브가 부하기의 0번
세션과 같은 계정을 쓰고, «회원당 활성 세션 1개» 제약에 걸려 **둘 중 하나가 조용히 빠진다.**
`reset_sessions` 도 `probe%` 를 같이 푼다 — SIGTERM 종료는 «닫고 나가는» 경로를 못 탈 수 있다.

---

## 5. 판 설계

- **버림판 팔당 1** — 첫 판은 컨테이너 워밍업·버퍼풀·JIT 를 가장 크게 탄다
- 본판 **팔당 3판**, 위치 합을 맞춘 배열(각 팔이 판 순서 1·2·3 에 한 번씩)
- 판 사이 **AI 컨테이너만 재기동 + 從 부하가 넣은 행 정리** (🔴 2026-08-16 뒤집힘 — 아래 5-1)
- 판당 부하는 **동시 세션 ramp**로 포화점을 찾는다. 고정 부하로는 「천장」이 안 나온다.
  레벨은 1라운드 **20 · 40 · 60 · 80 · 120 · 160**(§5-2) → 2라운드 **+ 90 · 100**(§5-3)
- 🆕 **교대 단위는 «팔» 이 아니라 «레벨»**, 레벨 순서는 라운드마다 뒤집는다, 앵커 판을 넣는다
  (2라운드 — §5-3. [#251](https://github.com/Shadowfit/init/issues/251)·[#252](https://github.com/Shadowfit/init/issues/252))

📌 팔이 4개(A~D)면 12판 + 버림 4판이다. **D 를 빼면 9판 + 버림 3판**으로 준다.
1라운드는 팔 A·B·C 라 **9판 + 버림 3판 = 12판**(스윕 단위)이었고, 관측으로는 54 + 3 = **57행**이다.

### 5-1. 🔴 「스택 재기동」이 「AI 만 재기동」으로 뒤집혔다 ([#246](https://github.com/Shadowfit/init/issues/246))

초판은 *「판 사이 같은 초기 상태 — 스택 재기동 + `pose_data` 정리」* 였다. **rig 은 그렇게
안 한다** — `coresidency_sweep.sh` `reset_between()` 은 `shadowfit-ai` 만
`docker compose restart` 하고, `pose_data` 는 從 부하 시드 범위(`GHZ_SESS_LO~HI`)만 지운다.
탑승 전 [`../../loadtest/AWS-RIDE-ALONG.md`](../../loadtest/AWS-RIDE-ALONG.md) §4 대조에서 잡혔고,
**rig 쪽이 맞다고 판정해 설계를 고친다**(다른 3건은 반대로 rig 을 고쳤다 — [#245](https://github.com/Shadowfit/init/pull/245)).

| | 왜 |
|---|---|
| AI 는 **재기동해야 한다** | 세션 상태가 AI 프로세스 메모리에 있다(`ai-server/app/core/session_state.py:243`). 안 내리면 앞 판 세션이 검출기 풀 자리를 물어 `nolease` 가 **앞 판 탓으로** 난다 |
| MySQL·Spring 은 **안 내린다** | 매 판 내렸다 올리면 판당 비용이 크게 늘고, **버퍼풀·JIT 재워밍업 자체가 판마다 다른 조건**이 된다 |

⚠️ **대가 — 판을 건너 누적되는 상태가 남는다.** MySQL 버퍼풀·Spring JIT 이 그것이고,
**얼마나 누적되는지는 안 쟀다**(위 판단은 비용 논증이지 측정이 아니다). 라틴 방격 배열이
그 누적을 팔에 **균등하게 흩는 것**까지만 한다 — 없애지는 못한다. 결과 문서에 이 문장을 넣는다.

⚠️ 판 사이 `pose_data` 정리를 從 부하 시드 범위로 한정해도 되는 근거는 **«AI 경로는 합성
인체라 rep 이 0 이어서 적재가 없다»**(§2 실측)에 기대고 있다. rep 이 생기는 rig 으로 바뀌면
이 한정도 같이 깨진다.

### 5-2. 레벨을 4개에서 6개로 — 20 · 40 · 60 · 80 · 120 · 160

초판 격자 `20/40/80/160` 은 **단독 156 을 감싸도록** 잡은 것이다. 그런데 이 실험의 가설
H1 은 *「동거하면 내려간다」* 이므로, 내려간 천장이 **40~80 사이면 포화점을 낀 관측점이
위아래 한 개씩뿐**이라 천장을 구간으로밖에 못 적는다. **격자가 상단에 치우쳐 있었다.**

→ 40~80 구간에 60 을, 80~160 구간에 120 을 넣는다. 최고 레벨 160 은 그대로 둔다
(從 부하 19 req/s 의 유도가 «160세션 × 0.12» 라 최고 레벨이 바뀌면 그 값도 바뀐다).

⚠️ **6레벨도 «정한» 값이다** — 천장 위치를 모르는 상태에서 해상도를 올린 것이지 측정에서
나온 격자가 아니다. 결과의 조건 칸에 이 유도를 그대로 적는다.

| | 4레벨 | **6레벨** |
|---|---|---|
| 판당 | 4 × (DUR 90s + 앞뒤 pad 5s×2) ≈ 6.7분 | ≈ **10분** |
| 12판 | ≈ 100분 | ≈ **2시간** |
| 라운드 전체(부트스트랩·preflight·리허설 포함) | ≈ 3시간 | ≈ **3.5시간** |

`TIMEOUT_CORES=21600`(6시간, `../../loadtest/aws/run_all.sh:124`) 안에 들어간다.

> 대안 2개를 안 골랐다. **정찰판 후 확정**(팔 A 버림판으로 천장을 먼저 보고 격자를 정함)은
> 라운드가 2단이 되어 무인 1회의 이점을 잃고, **격자 유지**는 가장 싸지만 가장 그럴듯한
> 시나리오(천장이 40~80)에서 정확히 못 짚는다.

🔴 **그 걱정이 한 칸 위에서 그대로 재현됐다** — 천장이 실제로는 **80~120** 사이였고,
6레벨에도 그 구간에 점이 없어 1라운드는 「80 초과 120 미만」까지만 말한다. → §5-3 이 8레벨로 넓힌다.

### 5-3. 🆕 2라운드 판 설계 — 시간 축을 팔 축에서 떼고, 천장을 점으로 (2026-08-16 확정)

1라운드가 못 닫은 셋(H1 · 부하기 천장 · H3)의 **뿌리 둘이 판 설계에 있다.**

| 바꾸는 것 | 왜 | 시간 |
|---|---|---|
| **① 교대 단위 «팔» → «레벨»** — `A_20 → B_20 → C_20 → A_40 → …` | [#251](https://github.com/Shadowfit/init/issues/251). 지금은 한 팔이 6레벨을 다 돈 뒤 다음 팔로 가서 **같은 레벨의 팔 간 대조점이 13~27분** 떨어진다. 그 사이 처리량이 **분당 0.09~0.35%** 흐른다(팔 A: 12분 266.0 → 82분 282.9 → 108분 308.6 fps). 대조 간격이 **~4분**이 된다 | +4분 |
| **② 캡을 `docker update --cpus` 로 토글** | ①의 전제. 지금 팔 전환은 compose override 로 **컨테이너를 재생성**한다(`coresidency_sweep.sh:111-118`). 전환이 9 → 54회가 되면 **재생성 위치가 팔마다 달라져 새 교락**이 된다. `docker update` 는 재기동이 없다 | 전환 ~1초 |
| **③ 캡 적용 단언** ✅ 코드 반영 08-17 | [#253](https://github.com/Shadowfit/init/issues/253). 캡이 안 물리면 **팔 C ≡ 팔 B** 인데 표는 정상이다. 전환마다 `NanoCpus` 를 `.env` 기대값과 대조하고, 팔 A·B 에서는 **0 이어야 한다**는 반대 단언도 건다. 관측값은 `caps.tsv` 에 남긴다 — 캡은 **측정 조건**이다 | ~1초 |
| **④ 레벨 순서 라운드마다 «치환»** ✅ 코드 반영 08-17 | [#252](https://github.com/Shadowfit/init/issues/252). 아홉 블록 전부 오름차순이라 블록 안 추세가 **120·160**(= plateau 를 정의하는 자리)에 얹힌다. 🔴 **반전으로는 안 고쳐진다** — 오름/내림/오름이면 레벨↔평균위치 상관이 **여전히 1.000** 이다. 회전(shift 2)으로 **0.361** 까지 내린다 | 0 |
| **⑤ 앵커 판 4개** (라운드마다 + 끝, 동일 조건 B@80) ✅ 코드 반영 08-17 | ①이 팔 간 간격을 줄여도 **드리프트 자체는 남는다.** 그것을 보정 대상이 아니라 **측정값**으로 남긴다. 끝에 하나를 더 둬야 **전 구간을 감싼다**. 첫 앵커 앞에 그 팔의 버림판을 먼저 돌려 **워밍업이 계열 첫 점을 오염시키는 것**을 막는다 | +11분 |
| **⑥ 레벨 90 · 100 추가 → 8레벨** | 천장이 「80 초과 120 미만」 구간으로만 짚혔다. **최고 160 은 그대로 둔다** — `GHZ_RPS` 유도가 «160세션 × 0.12» 라 최고 레벨이 바뀌면 從 부하 값도 바뀐다 | +40분 |

| **⑩ 부하기 코어 팔(§T)** ✅ 코드 반영 08-17 | 🔴 라운드 간 +17.7% 의 원인이 «부하기 구성» 인지 «대상 박스 물리 호스트 개체차» 인지 **둘이 같이 바뀌어 안 갈린다**. 같은 세션·같은 호스트에서 `taskset -c 0-1`(2코어) ↔ 제한 없음을 **붙여서** 대조한다. 쌍의 순서는 반복마다 뒤집는다 | +26분 |

**판 수**: 본판 3팔 × 3라운드 × 8레벨 = 72 + 버림 3 + **앵커 4** + **§T 12** = **91판**.
판 주기 실측 128.5초(1라운드 57판 / 7,323초)로 **스윕 ≈ 3h20m**(팔 전환 포함),
리허설·preflight·collect 포함 **인스턴스 가동 ≈ 3h40m**. `TIMEOUT_CORES=21600`(6시간) 안이다.

⚠️ **④의 대가** — 내림차순 블록은 **160세션이 첫 판**이라 워밍업을 가장 큰 판이 뒤집어쓴다.
버림판 레벨을 그 블록의 첫 레벨에 맞춰야 한다(현재는 `$LEVELS` 첫 값 고정 — `:447`).

⚠️ **①이 §5-1 을 흔들지 않는다** — 판 사이 재기동 정책(AI 만)은 그대로다. ②가 없으면
①이 그 정책을 깨뜨린다(팔 전환마다 MySQL·Spring 재생성)는 것이 ②를 붙인 이유다.

⚠️ **`docker update --cpus` 가 이 구성에서 실제로 물리는지 아직 안 해봤다.** 로컬 확인이
2라운드의 선행 조건이고, 안 되면 ①을 «전환 45회 추가 × 재생성»(+23분)으로 되돌려야 한다.

---

## 6. 가설과 반증 조건

| # | 가설 | **무엇이 나오면 틀린 것인가** | 1라운드 판정 (08-16) | **2라운드 판정 (08-17)** |
|---|---|---|---|---|
| H1 | 동거하면 동시 세션이 **내려간다** | B 가 A 와 사실상 같다 (= 동거 비용이 무시할 수준) | 🔴 **판정 불가** — A 가 흐른다([#251](https://github.com/Shadowfit/init/issues/251)) | ❌ **반증** — 드리프트가 사라진 뒤 8레벨 × 3판 평균에서 **B−A = −0.43% ~ +0.03%**(160세션 347.9 → 346.4 fps). **동거 비용은 잡음 크기다.** ⚠️ 「옆이 일한다」가 **ghz 19 RPS** 였다는 조건이 붙는다([결과 §2-1](../../loadtest/results/coresidency-aws-2026-08-17/README.md)) |
| H2 | 뺏는 주체는 **AI 자신**이다 — 코어를 주는 대로 먹으므로 | 포화 시 AI cgroup CPU 가 지배적이지 않다 | ✅ **지지** — AI 869~893% vs 옆 둘 합 0.17 vCPU | ✅ **재확인** — 160세션 팔 B 3판에서 AI 평균 **644.5 / 673.2 / 695.4%**(피크 **944.6~947.0%**) vs 옆 둘 합 **평균 0.21~0.26 vCPU**(backend 14.6~19.5% · mysql 6.4~6.6%) |
| H3 | 캡을 걸면 **총 용량은 줄지만 다른 서비스가 산다** | C 에서 Spring p99·MySQL 지표가 B 와 차이 없다 (= 캡이 아무것도 안 지킴) | 🔴 **자료 없음** — 그 열이 생성되지 않았다([#254](https://github.com/Shadowfit/init/issues/254)) | ❌ **반증** — ⑨([#254](https://github.com/Shadowfit/init/issues/254))가 그 열을 처음 만들었고, 셋이 한꺼번에 읽혔다: ① 옆은 **애초에 안 아팠다**(AI 20→160세션 동안 옆 p50 8.2~8.8ms 평평 · fail 0) ② 캡을 걸어도 **안 좋아진다**(160세션 p99 10.7 → **12.7**) ③ 대가만 확실하다(AI 처리량 **−9.7%** · 100세션 AI p50 36.6 → **114.5ms = 3.1배**) — ❌ **從 R9(09-08) 재확인, 대가 폭 확대**: 120세션 이상에서 C·D 처리량이 245~250 rps 근방에 눌러앉음(A·B는 360~449 rps 유지) · 90세션에서 이미 p99 600ms대. 옆(Spring `process_cpu_usage` ≤8.3% · MySQL `Threads_running` 거의 항상 2)은 이번에도 안 아팠다. ⚠️ 캡 값이 08-17과 동일 조건인지는 미대조 — 절대값 비교엔 caveat, 방향은 동일. 상세: [`../../loadtest/results/coresidency-aws-2026-09-08/README.md`](../../loadtest/results/coresidency-aws-2026-09-08/README.md) §2·§4 |
| H4 | 검출률은 **처리량보다 먼저** 무너진다 | 포화까지 검출률이 평탄하다 | ❌ **반증** — 57행 전부 `detect_pct` 100.00, 대신 지연이 간다 | ❌ **반증 유지** — **91행 전부**(본 측정 72판 + 앵커 4 + 버림 3 + §T 12) `detect_pct` 100.00 |
| H5 | 관측 스택 비용은 **한 자릿수 %** | D 가 C 대비 두 자릿수 % 내려간다 | — 미측정(팔 D 제외) | 🔴 **여전히 미측정** — 팔 D 제외를 유지했다. 다만 08-18 세션분산 라운드가 **쓰기 경로 기준**으로 대체 관측했다(처리량 차 **−0.05%** · 스택 CPU 1% 미만). **AI 부하 기준 Q5 는 그대로 빈칸이다** — 두 부하는 병목이 다르다. 🟢 **2026-08-23: 팔 D 를 P6-b 라운드의 從 R9 로 등록했다**([`AWS-RIDE-ALONG.md` §1 從](../../loadtest/AWS-RIDE-ALONG.md)) — **등록이지 채택은 아니다**(+19판·+42분, 격자 확정 시 결정) — ✅ **從 R9(09-08) 첫 실측, AI 부하 기준 Q5 답 나옴**: C↔D rps 차이 ±3% 안, p99 차이 최대 ±7%(방향 세션마다 뒤집힘) — **가설대로 한 자릿수% 안, 두 자릿수%는 어디서도 안 나옴.** 단 3판이라 진짜 작은 비용과 반복 잡음을 완전히는 못 가른다. 상세: [`../../loadtest/results/coresidency-aws-2026-09-08/README.md`](../../loadtest/results/coresidency-aws-2026-09-08/README.md) §3 |

✅ **아래 두 문단은 1라운드 시점의 기록이다 — 2026-08-17 에 닫혔다.** ⑨([#254](https://github.com/Shadowfit/init/issues/254))가
그 열을 만들었고, 2라운드가 H3 을 **반증**했다. 「Q3 이 절반만」도 이제 아니다.

🔴 **H3 의 반증 조건이 「Spring p99·MySQL 지표」인데 rig 이 그 둘을 안 걷는다**(§4 표).
그래서 1라운드가 「캡 축은 닫혔다」고 적은 것은 정확히는 **「AI 처리량이 캡한 만큼 깎였다」**
(CPU 90.8% → 처리량 92.9%)이고, [#212](https://github.com/Shadowfit/init/issues/212) 가 실제로
물은 **«캡이 옆을 지키는가» 는 아직 안 답해졌다.** Q3 이 절반만 닫힌 것이 이 뜻이다.

**H3 이 반증되면 [#212](https://github.com/Shadowfit/init/issues/212) 의 전제가 흔들린다** —
캡이 지키는 게 없다면 캡 값을 정하는 일 자체가 다른 문제가 된다. 그것도 산출물이다.

→ ✅ **실제로 그렇게 됐다 (2026-08-17).** [#212](https://github.com/Shadowfit/init/issues/212) 는
**닫는 게 아니라 근거를 다시 세워야 하는** 항목이 됐다 — 「캡이 없어서 서로 코어를 뺏는다」가
**이 부하 수준에서는** 성립하지 않는다. ⚠️ 판정의 범위: 캡은 `AI=8 vCPU` **한 점**에서만 쟀고,
옆 부하는 **19 RPS** 였다. 「더 세게 때린 H3」은 안 해봤다.

> 임계값은 적지 않는다. 「동시 N세션 이상이면 합격」 같은 기준은 지금 근거가 없다
> ([[feedback_no_arbitrary_threshold_values]]). 이번 범위는 **값을 관측해 적는 것**까지다.

---

## 7. 로컬인가 EC2인가

🔴 **로컬로는 Q1 을 못 답한다.** 개발 장비는 i3-6100 2물리코어에 MySQL+백엔드가 이미
동거 중이라([[project_loadtest_env_constraint]]) **동거 실험의 대조군인 「단독」을 만들 수 없다.**
팔 A 가 성립하지 않으면 §3-1 의 분리가 통째로 무너진다.

그리고 이 실험이 답해야 하는 것은 **「c7i.4xlarge 급에서 156 이 얼마가 되나」** 다. 그 박스가
아니면 옮겨 쓸 수 없다.

→ **EC2 1대.** 08-14 와 같은 타입(c7i.4xlarge)이어야 156 과 비교가 성립한다.
부하기를 같은 박스에 두면 그것이 또 경합 변수가 되므로 **부하기는 별도 소형 인스턴스**다.

---

## 8. 알려진 함정 체크리스트 (착수 전)

- [ ] 🔴 **`AI_MEM_LIMIT` 없이 뜨면 첫 세션에서 죽는다** ([#214](https://github.com/Shadowfit/init/issues/214)).
      팔 B(캡 없음)를 「메모리 캡도 없음」으로 만들면 **팔이 아예 안 돈다.**
      → **팔 B 는 «CPU 캡 없음 + 메모리 캡 있음»** 으로 정의한다(현행 dev compose 와 같은 상태).
      이 한 줄을 안 적으면 무인 라운드가 통째로 날아간다
- [ ] **`AI_PUBLIC_TOKEN`** — 프레임 경로가 `InternalAuthMiddleware` 로 보호된다. 부하기가 이 토큰을 실어야 한다
- [ ] **합성 인체 이미지** — 빈 프레임을 넣으면 재는 것이 「추론」이 아니라 「탐지 실패」가 된다.
      `measure_ai_concurrency.py` 의 생성기를 재사용하고, **매 판 검출 성공을 단언**한다
- [ ] **세션이 실재해야 한다** — 프레임에 `session_id` 가 붙고 검출기 풀이 세션 단위로 점유한다.
      부하기가 세션을 실제로 열고 닫아야 하며, 안 그러면 **풀이 안 돌아 한도에 걸린다**
- [x] ~~**Spring·MySQL 이 실제로 일해야 한다** — rep 이 완성돼야…~~ → 🔴 **이 경로는 막혀 있다**
      (2026-08-15 실측). 합성 인체로는 rep 이 안 생긴다(§2 인용 블록). **ghz 로 따로 건다.**
      대신 새 함정이 생겼다 — **두 부하가 실제로 «동시에» 돌고 있는지**를 판마다 확인해야 한다.
      한쪽이 조용히 죽으면 «동거» 가 아니라 단독인데 표는 정상으로 보인다
- [ ] **`docker stats` 샘플러의 비용** — 관측이 관측 대상을 흔든다. 폴링 간격을 조건에 기록

---

## 9. 의식적으로 안 할 것

| 안 함 | 이유 |
|---|---|
| 수평확장 rig(N대 + 어피니티) | 이 문서는 **한 대의 값**을 만든다. 그 값 없이 N대를 재면 순서가 거꾸로다 |
| 캡 값 튜닝 스윕 | 「몇 %가 최적인가」는 배포 형태가 정해진 뒤. 여기선 **캡 유무**만 |
| 프레임 레이트 스윕 | 3fps 고정. 바꾸면 팔이 배로 는다 |
| 절대 RPS 를 다른 라운드와 비교 | rig·무대가 다르다. 비교는 **같은 라운드 안에서만** |

---

## 10. 결정 현황 — **6건 전부 닫혔다** (2026-08-16)

- [x] **착수 여부** — 🟢 **착수**(2026-08-16 사용자 confirm). EC2 2대 · 12판 · 라운드 3.5시간 안팎(§5-2)
- [x] **팔 D(관측 스택)를 넣을지** — **제외**(08-16). 판이 25% 는다. Q5 는 다음 라운드로
- [x] **인스턴스 타입** — **대상 c7i.4xlarge**(08-16). 08-14 의 156 이 나온 그 박스가 아니면 팔 A 가 기준선 구실을 못 한다
- [x] **부하기를 어디에 둘지** — **별도 c7i.large 1대**(08-16). 부하 생성 비용이 측정 대상 밖에 있어야 동거 값이 안 오염된다
- [x] **ramp 레벨** — **20/40/60/80/120/160**(2026-08-16 사용자 confirm). 격자가 상단에 치우쳐 있었다 — 유도·대가는 §5-2
- [x] **rig 을 새로 쓸지, `chain_*` 을 확장할지** — **새 rig**(08-15~16).
      [`coresidency_sweep.sh`](../../loadtest/results/coresidency-2026-08-15/README.md) 를 쓰고,
      부트스트랩은 `chain_*` 확장이 아니라 [`bootstrap.sh`](../../loadtest/aws/bootstrap.sh) 에
      `ROLE=db|p6-target|p6-loader` 갈래를 넣는 쪽으로 갔다

> 🔴 **결정이 닫힌 것과 실행이 검증된 것은 다르다.** [#222](https://github.com/Shadowfit/init/issues/222)·[#223](https://github.com/Shadowfit/init/issues/223)
> 의 수정, 2대 부트스트랩, ghz 배선 — **전부 코드 판독으로만 확인했고 EC2 에서 돌린 적이 없다.**
> 그래서 라운드의 첫 단계가 `coresidency_preflight` · `coresidency_rehearsal` 이고,
> **리허설이 실패하면 정판을 안 돈다**(`run_all.sh:722-735`).

---

## 11. 2라운드 — 「전부」로 간다 (2026-08-16 사용자 결정)

**닫을 것 셋**: H1(동거 비용) · 부하기 천장 배제 · H3(캡이 옆을 지키는가).
**얹는 것 여섯**: §5-3 의 ①~⑥ + [#250](https://github.com/Shadowfit/init/issues/250) 샘플러 보강 + §4-2 자기 프로브 + [#254](https://github.com/Shadowfit/init/issues/254) 옆 지표.

| | 항목 | 라운드 시간 | 근거 |
|---|---|---|---|
| ① | 레벨 단위 교대 ([#251](https://github.com/Shadowfit/init/issues/251)) | +4분 | §5-3 |
| ② | `docker update --cpus` 전환 | 포함 | §5-3 |
| ③ | 캡 단언 ([#253](https://github.com/Shadowfit/init/issues/253)) — ✅ **코드 반영 2026-08-17** | ~0 | §5-3 |
| ④ | 레벨 순서 **치환** ([#252](https://github.com/Shadowfit/init/issues/252)) — ✅ **코드 반영 2026-08-17** | 0 | §5-3 |
| ⑤ | 앵커 판 **4**(라운드마다 + 끝) — ✅ **코드 반영 2026-08-17** | +11분 | §5-3 |
| ⑥ | 레벨 90·100 (8레벨) — ✅ **코드 반영 2026-08-17** | +40분 | §5-3 |
| ⑦ | 부하기 샘플러 보강 ([#250](https://github.com/Shadowfit/init/issues/250)) — dt 일치 · `/proc/net/dev` · **리허설 게이트** · 음수 처리([#255](https://github.com/Shadowfit/init/issues/255)) · 판정선 정정 — ✅ **코드 반영 2026-08-17** | 0 | 아래 |
| ⑧ | 자기 프로브 — ✅ **코드 반영 2026-08-17** | 0 | §4-2 |
| ⑩ | **부하기 코어 팔(§T)** — `taskset` 2코어↔전체, 같은 호스트에서 쌍으로 — ✅ **코드 반영 2026-08-17** | +26분 | §5-3 |
| ⑨ | ghz 지연 백분위 + 옆 스냅샷 **3회**(pre·mid·post) ([#254](https://github.com/Shadowfit/init/issues/254)) — ✅ **코드 반영 2026-08-17** | +2분 | §4 표 |

**합계 — 91판 · 스윕 ≈ 3h20m · 인스턴스 가동 ≈ 3h40m** (1라운드 실측 2.29h 대비 1.6배).
**③④⑤⑥⑦⑧⑨⑩ 이 코드에 들어갔다**(2026-08-17). 남은 것은 **①②(교대 단위 — 강등)** 뿐이고,
그것은 드리프트가 다시 나타나면 되살린다(앵커 판이 그 감시를 맡는다).
🔴 **선행 조건이 하나 늘었다 — 부하기가 `c7i.xlarge` 이상이어야 한다.** 2 vCPU 박스에서는
«2코어 제한» 과 «전체» 가 같은 조건이라 §T 가 성립하지 않는다(preflight 가 막는다).

🔴 **⑦의 판정선이 200% 가 아니다.** 부하 생성은 **단일 파이썬 프로세스**이고
(`load_ai.py:37,209` — 스레드 160개 + 전역 `LOCK`), GIL 때문에 **1 vCPU 부근에서 먼저**
막힌다. 총 CPU 200% 가 아니라 **`load_ai_pct` 가 ~100%** 에 붙는지를 본다.

🔴 **⑦의 게이트가 핵심이다.** ~~`run_all.sh` 의 판정은 `cores_assert_usable`·`cores_assert_ghz`
둘뿐이고 **`loader_*.tsv` 를 안 본다.** 샘플러가 빈 파일을 남겨도 라운드는 성공으로 끝난다~~ —
「걷은 줄 알았는데 안 걷혔다」가 #250 의 실패 모드 그 자체다.

✅ **정정 (2026-08-20, [#262](https://github.com/Shadowfit/init/issues/262)).** 위 취소선은 **2026-08-17 이전**
서술이다. 지금 게이트는 **6종**이고 `loader_*.tsv` 를 보는 것이 그중 하나다:

| 게이트 | 자리 | 무엇을 막나 |
|---|---|---|
| `cores_assert_usable` | `run_all.sh:505` | 판이 성립했나(`setup_fail`) |
| `cores_assert_ghz` | `:538` | 從 부하가 실제로 갔나 |
| `cores_assert_probe` | `:570` | 두 시계(자기 프로브)가 있나 |
| `cores_assert_taskset` | `:598` | §T 가 돌았나 |
| **`cores_assert_loader`** | **`:626`** | **부하기 계측이 걷혔나 — 「없다」고 적었던 그 게이트** |
| `cores_assert_side` | `:655` | 옆(Spring·MySQL) 지표가 걷혔나 |

여섯 개 다 `phase_coresidency_rehearsal`(`:718~728`)과 `phase_coresidency`(`:764~`)에서 호출되고,
**2026-08-17 2차 리허설에서 6종 전부 실행·통과**했다. 판정은 「행이 있는가」가 아니라
「정상 · 전부 0 · 헤더만 · 파일 없음」 4가지로 대조한다(커밋 `ee9927b`).

### 🍚 점심때 할 일 (2026-08-17 기록 · **2차 리허설 후 갱신**)

**1차 EC2 리허설을 돌렸고 절반이 섰다** — 결과·실무 메모는
[`rehearsal-aws-2026-08-17/`](../../loadtest/results/coresidency-2026-08-15/rehearsal-aws-2026-08-17/README.md).
인스턴스는 둘 다 terminate 했다(인스턴스 0 · 볼륨 0 확인).

> ✅ **2차 리허설 완주 (2026-08-17)** — [`rehearsal2-aws-2026-08-17/`](../../loadtest/results/coresidency-2026-08-15/rehearsal2-aws-2026-08-17/README.md).
> 8판 + §T 2판 · 스윕 549초 · **`cores_assert_*` 6종 전부 통과**. ⑧ 자기 프로브와 ⑩ §T 가
> **처음으로 EC2 에서 실제로 걷혔다.** 인스턴스 둘 다 terminate(인스턴스 0 · 볼륨 0 확인).
> **rig 쪽으로 새로 만들 것은 남아 있지 않다** — 남은 것은 결함 수정뿐이다
> ([#259](https://github.com/Shadowfit/init/issues/259) · [#260](https://github.com/Shadowfit/init/issues/260) ·
> [#261](https://github.com/Shadowfit/init/issues/261) · [#262](https://github.com/Shadowfit/init/issues/262) ·
> [#263](https://github.com/Shadowfit/init/issues/263) · [#264](https://github.com/Shadowfit/init/issues/264) ·
> [#265](https://github.com/Shadowfit/init/issues/265)).
> 🔴 **정식 라운드를 막는 것은 이제 3번(S3) 하나다.**
>
> ⚠️ §T 의 `제한 30.2 ↔ 전체 30.1 fps (+0.3%)` 를 「부하기는 병목이 아니다」로 읽지 말 것 —
> 그 판에서 부하기 CPU 가 **5~17%** 라 애초에 놀고 있었다. 참인 것은 «§T 장치가 작동한다»
> 까지고, [#250](https://github.com/Shadowfit/init/issues/250) 의 답은 **레벨 120·160**
> 에서 나온다(`CORES_TASKSET_LEVELS` 기본값이 그것이다).

| # | 할 일 | 왜 지금 안 됐나 |
|---|---|---|
| 1 | ~~**2차 리허설** — 고친 스윕으로 ⑧(자기 프로브) · ⑩(§T) · `cores_assert_*` 4종 검증~~ → ✅ **완료** | ~~프로브는 `-n` 결함으로 꺼졌고, §T 는 격자 **뒤**에 붙는데 그 전에 중단했다~~ |
| 2 | ~~🔴 **`TARGET_SSH` 에서 `-n` 을 뺀다**~~ → ✅ **완료** (빼고 돌렸고 프로브가 8판 전부 걷혔다) | ~~붙이면 프로브 자산이 0바이트로 간다(rig 이 크기 대조로 잡아 프로브를 끈다)~~ |
| 3 | ~~🔴 **S3 를 풀 것**~~ → ✅ **풀렸다 (2026-08-17). 관리자도 IAM 수정도 필요 없었다** — 기동 명령에 **`--iam-instance-profile Name=shadowfit-measure`** 한 줄이면 된다. 실증: `t3.micro` 로 EC2→버킷 쓰기 성공(`put_ok.txt`). **`PHASES` 에 `coresidency_preflight`·`collect` 를 다시 넣는다** | 🔴 **1·2차의 「못 붙인다」가 오진이었다.** `iam:GetInstanceProfile` **거절 하나**를 보고 결론냈는데, **조회 권한과 사용 권한은 다른 것**이다. 판별법: `run-instances --dry-run` 을 **진짜 이름과 가짜 이름으로 대조**한다 — 진짜는 `DryRunOperation`(성공했을 요청), 가짜는 `InvalidParameterValue: Invalid IAM Instance Profile name`. 가짜가 거절되므로 AWS 가 존재를 **실제로 검증**한다는 뜻이고, 따라서 진짜 쪽 성공은 신뢰할 수 있다 |
| 4 | ~~부하기는 **`c7i.xlarge` 이상**으로 띄운다~~ → ✅ **완료** (2차도 `c7i.xlarge`, `ncpu=4`) | ~~2 vCPU 면 §T 가 성립하지 않는다(제한 = 전체)~~ |
| 5 | ✅ **완료** — CRLF 는 `sed` 가 답이 아니었다: repo 블롭은 LF 이고 바꾸는 건 작업 트리의 `core.autocrlf=true` 다. `git show HEAD:<path>` 로 뽑으면 애초에 안 생긴다. ~~대상 박스 **root SSH 열기** · scp 후 **CRLF 제거** | AL2023 은 root 로그인 차단이 기본 · Windows 작업 트리는 CRLF 라 `$''` 로 죽는다 |

**2차 리허설 명령** (부하기에서, `-n` 없이) — 아래는 **2차에서 실제로 태운 형태로 고쳐 둔 것**이다([#260](https://github.com/Shadowfit/init/issues/260) · [#261](https://github.com/Shadowfit/init/issues/261)). 물렸던 자리 둘을 남겨 둔다. ⑴ `CORES_ARMS` 에 **`B` 가 없으면 §T 가 안 돈다** — `TASKSET_ARM=B` 가 `ARMS` 에 없으면 실패가 아니라 `note` 경고 한 줄로 조용히 건너뛴다(#260). ⑵ `GHZ_TOKEN` 이 받는 것은 **`INTERNAL_API_TOKEN`** 이고, `AI_PUBLIC_TOKEN` 과 **같은 값을 넣으면 AI 가 아예 안 뜬다**(#230 단언 · #261).

```bash
cd /root/init && sudo env   S3_BASE=s3://shadowfit-measure-055447613012/shadowfit TARGET_HOST=<대상 사설 IP>   TARGET_SSH="ssh -i /root/.ssh/measure.pem -o StrictHostKeyChecking=no root@<대상 사설 IP>"   AI_PUBLIC_TOKEN=<대상 .env 의 AI_PUBLIC_TOKEN> GHZ_TOKEN=<대상 .env 의 INTERNAL_API_TOKEN>   GHZ_RPS=19 GHZ_DATA=/root/batch_multi.json GHZ_BIN=/usr/local/bin/ghz   CORES_ARMS="C A B" CORES_REH_LEVELS="5" CORES_ANCHOR=0   PHASES=coresidency_rehearsal nohup bash loadtest/aws/run_all.sh > /root/run_all.log 2>&1 &
```

팔에 `C A` 가 들어가는 이유는 **캡이 걸린 뒤 풀리는 전이**를 보기 위해서고(`STALE` 판정 경로),
`B` 는 ⑩(§T)가 돌게 하려고 붙인다([#260](https://github.com/Shadowfit/init/issues/260)).
⚠️ 이 명령은 **S3 없이** 도는 형태다 — 3번을 풀면 `PHASES` 에 `coresidency_preflight` 와
`collect` 를 다시 넣는다.

🔴 **terminate 전에 R6·R7 을 태운다 (2026-08-17 추가)** — [`AWS-RIDE-ALONG.md`](../../loadtest/AWS-RIDE-ALONG.md) §1 從.
**P6 대상 박스(`c7i.4xlarge` = 물리 8코어)가 R6 의 유일한 무대다** — GIL 이냐 캐시냐는
로컬 물리 2코어로는 **구조적으로 못 가른다**(4워커에서 스레드와 프로세스가 같은 천장에 붙는다).
**R6 ~10분 + R7 ~5분** · 도커·MySQL·시딩 **불필요**(venv + `frames.json` 만) · 스윕과 독립.
판정선: 프로세스 2개에서 처리량 **1.7~2배** + AI CPU **8.7→14~16** 이면 GIL, 아니면 캐시·HT.
⚠️ **2026-08-17 2차 리허설에서 이 박스를 45분 띄웠다가 그냥 껐다.** 같은 실수를 반복하지 말 것.

**선행 조건 (탑승 전, 로컬)**

- [ ] `docker update --cpus` 가 실제로 물리는지 확인 (안 되면 ①이 +23분으로 되돌아간다)
- [ ] 대상 박스에서 actuator(8080)·`SHOW GLOBAL STATUS` 가 부하기 쪽에서 닿는지
- [x] ~~자기 프로브 자산을 `ROLE=p6-target` 부트스트랩에 넣기~~ → **스윕이 `$SSH` 로 직접 민다**(부트스트랩 변경 불필요). 대신 **대상 박스에 python 이 있어야** 하고, preflight 가 그걸 묻는다
- [x] ~~[#249](https://github.com/Shadowfit/init/issues/249) `GHZ_BIN` 기본값 ≠ 설치 경로~~ → ✅ **고쳤다 (2026-08-17)** — 기본값이 `/usr/local/bin/ghz` 다. `run_all.sh` 와 `coresidency_sweep.sh` **두 곳**에 있었다
- [ ] `docker update --cpus` 해제는 `--cpus 0` 이 아니라 `--cpu-quota -1` ([#263](https://github.com/Shadowfit/init/issues/263)) — ② 를 되살릴 때만 해당
- [ ] 축소 리허설 통과 — **1라운드는 여기서 세 번 막혔다.** 실제 리드타임은 라운드 3시간이 아니라 **리허설 재시도 횟수**가 정한다

**이번에도 안 하는 것**

| 안 함 | 이유 |
|---|---|
| 팔 D(관측 스택 · Q5) | 판이 19개(+42분) 는다. 못 닫은 셋이 먼저다. 🔴 **정정 2026-08-23** — 못 닫은 셋은 그 뒤 **전부 닫혔다**(§6 «2라운드 판정» 열 · 상태 헤더). 그래서 「먼저」의 대상이 사라졌고, 팔 D 는 **從 R9 로 등록**됐다([`AWS-RIDE-ALONG.md` §1](../../loadtest/AWS-RIDE-ALONG.md)). **넣을지는 P6-b 격자 확정 때 정한다** — «판이 19개 는다» 는 사실은 그대로다 |
| 부하기 스케일업 대조(c7i.large → xlarge, [#250](https://github.com/Shadowfit/init/issues/250) 2안) | **⑦·⑧의 결과를 보고 정한다.** 「부하기가 붙었나」가 답해지면 안 해도 된다. 필요하면 축소 격자 별도 라운드(20판 ≈ 1시간) |
| 드리프트 **원인** 규명 | ①~⑤는 «분리» 지 «설명» 이 아니다. 앵커 판이 원인 가설의 재료를 만든다 |

---

## 12. 🔴 그 사이에 2라운드가 이미 돌아 있었다 — 그리고 절대값이 안 재현된다 (2026-08-16b)

§11 을 쓰는 사이에 발견했다. **완주한 라운드가 회수되지 않은 채** 디스크에 있었다
(08-16 08:55~11:08 UTC · 본 측정 7,387초 · 커밋 `6b8a5ae`).
**회수·분석 완료**: [`../../loadtest/results/coresidency-aws-b-2026-08-16/`](../../loadtest/results/coresidency-aws-b-2026-08-16/README.md).
1라운드와 다른 것은 둘 — 부하기 `c7i.large`(2 vCPU) → **`c7i.xlarge`(4 vCPU)**, 그리고
[#250](https://github.com/Shadowfit/init/issues/250) 샘플러. 대상·격자·팔 배열은 같다.

| 팔 | 1라운드 (부하기 2 vCPU) | **2라운드 (부하기 4 vCPU)** | 차 |
|---|---:|---:|---:|
| A | 285.8 fps (95.3세션) | **316.5 (105.5세션)** | +10.7% |
| B | 267.6 (89.2) | **315.0 (105.0)** | **+17.7%** |
| C | 248.6 (82.9) | **291.2 (97.1)** | +17.1% |

**팔 대조는 닫혔다.**

- ⭐ **H1 이 답을 얻었다** — A 316.5 vs B 315.0 = **−0.5%**(sd 1.4~1.7). 19 req/s 從 부하
  조건에서 **동거 비용은 잡음 크기**다. 1라운드의 −0.8/−6.2/−13.3% 는 팔 효과가 아니었다.
- ⭐ **드리프트가 사라졌다** — 팔 A r1 315.3 → r2 316.5 → r3 **317.7 fps = +0.8%**
  (1라운드 +16.0%). [#251](https://github.com/Shadowfit/init/issues/251) 의 교락은 실재하지만
  **이 무대에서는 크기가 무너졌다.**
- **캡 효과 재현** — C 는 B 대비 −7.6%(1라운드 −7.1%), 3판 전부 같은 방향.
- 품질: 54 관측 전부 `detect_pct` 100.00 · 결함 카운터 0 · 從 부하 실측 18.9~19.0 req/s
  (ok 72,162 / fail 1) — «옆이 논 판» 이 아니다.

### 12-1. 🔴 그런데 「부하기가 천장이었다」는 실측이 지지하지 않는다

가장 먼저 떠오르는 설명 — *「2 vCPU 부하기가 프레임을 덜 먹여서 1라운드가 낮았다」* — 은
**배제된다.** 그랬다면 1라운드의 **AI CPU 가 낮아야** 하는데:

| | 1라운드 | 2라운드 |
|---|---:|---:|
| 팔 B AI CPU | **869.3%** | **869.0%** |
| 팔 B 처리량 | 267.6 fps | **315.0 fps** |
| fps / AI vCPU | 30.78 | **36.25** |

**AI 는 같은 CPU 를 쓰고 프레임을 17.7% 덜 처리했다** — 그 CPU 를 «일이 아닌 데» 썼다는 뜻이다.
그리고 부하기 CPU 는 4 vCPU 중 **0.68 만**(포화선 400%, 관측 최대 67.6%) 썼다.

남은 후보는 둘이고 **같이 바뀌어서 안 갈린다** — ① 부하기 2→4 vCPU 의 **송신 패턴**,
② 라운드 사이 정지→시작으로 인한 **대상 박스의 물리 호스트 변경**. 상세·약한 갈림은
[2라운드 결과 §5](../../loadtest/results/coresidency-aws-b-2026-08-16/README.md).

📌 **이때까지 [#250](https://github.com/Shadowfit/init/issues/250) 은 «계측 축» 만 닫혀 있었다** —
부하기 CPU 는 천장이 아니었다는 것까지가 사실이고, **무엇이 +17.7% 를 만들었는지는 열려 있었다.**

✅ **갱신 (2026-08-17 · §T) — 귀속 축도 닫혔다.** 부하기 코어를 **2 ↔ 전체(4)** 로 흔든 12판에서
처리량 차가 **0.3~0.4%** 이고(120세션 345.4~346.1 ↔ 346.2~347.0), 지연도 같다(160세션 p50
456.8 ↔ 457.1). 부하기 자신의 CPU 는 2코어 팔에서 평균 **47.0%**(상한 200%) — 포화 근처도
아니다. → **346 RPS 는 대상 박스(서버)의 천장이고, 08-16 의 「부하기가 천장」 판정은 뒤집혔다**
([08-17 결과 §2-3](../../loadtest/results/coresidency-aws-2026-08-17/README.md)).

🔴 **남은 몫은 하나다 — §T 와 08-16 스케일업의 폭이 안 맞는다.** 같은 「부하기를 흔든다」인데
§T(`taskset` 으로 코어만 조임)는 **0.3~0.4%**, 08-16(박스 자체를 `c7i.large`→`xlarge`)은
**+17.7%** 였다. 두 자릿수 차이라 위 후보 ①② 가 그대로 남는다.

⭐ **그리고 이것이 §4-2 를 강화한다.** 부하기 CPU 가 «안 붙었다» 는 관측만으로는
**「부하기 구성이 결과를 안 움직인다」를 못 말한다** — 실제로 안 붙었는데 움직였다.
대상 쪽 독립 시계(**자기 프로브**)가 없으면 이 축은 계속 안 갈린다.

### 12-2. 이 발견이 §11 을 어떻게 바꾸나

| §11 항목 | 바뀐 우선순위 |
|---|---|
| ⑨ H3 판정 열 ([#254](https://github.com/Shadowfit/init/issues/254)) | 🔴 **최우선.** 2라운드도 이 열이 없어 H3 는 **세 라운드째 못 닫혔다** |
| ⑥ 레벨 90·100 | 🔴 **최우선.** 천장이 여전히 80(100%)~120(88%) 사이다 |
| 🆕 **라운드 간 절대값 비재현** | 🔴 **새 최우선 — ⑩(§T)이 그 절반을 가른다.** 같은 코드·격자·인스턴스 타입에서 천장 환산이 89.2 → 105.0세션으로 움직였다. **앵커 판(⑤)의 값이 여기서 올라간다** — 라운드 내부뿐 아니라 **라운드 간** 기준점이 된다 |
| ⑧ 자기 프로브 | 🟡 **유지** — 12-1 이 그 필요를 강화했다 |
| ③ 캡 단언 ([#253](https://github.com/Shadowfit/init/issues/253)) | 🟡 유지. 두 라운드 재현됐으나 비용이 ~0 |
| ④ 레벨 순서 반전 | 🟢 유지(비용 0) |
| ①② 레벨 단위 교대 + `docker update` | 🟢 **강등.** 드리프트가 +0.8% 로 줄어 **가장 비싼 변경의 근거가 약해졌다** |
| ⑦ 샘플러 보강 | 🟡 유지 + [#255](https://github.com/Shadowfit/init/issues/255)(종료 샘플 음수) 반영 |
| 무대 | **부하기 `c7i.xlarge` 를 기본으로.** 단 12-1 때문에 «2 vCPU 가 나빴다» 가 아니라 «조건을 고정한다» 가 이유다 |

🔴 **1라운드 헤드라인(「동거 천장 89.2세션」)은 인용하지 않는다** — 절대값은 라운드를 건너
재현되지 않는다. 팔 간 비교와 메모리 식은 재현된다.

---

## 결정 로그

- 2026-08-15: 설계 초안. 「동접 100 이면 수평확장인가」 논의에서 출발했는데, **답할 수 없는
  질문**임이 드러나 그 앞의 빈 칸을 문서로 세웠다 — 156 은 단독 값이고 동거 값이 없다.
  - **기존 rig 을 못 쓴다는 것이 이 설계의 첫 발견**이다(§2). `measure_ai_concurrency.py` 는
    검출기를 in-process 로 부르므로 컨테이너·동거를 통째로 못 본다. 「rig 이 있으니 돌리면 된다」로
    시작했으면 **다른 것을 재고 동거 값이라 불렀을 것**이다.
  - 팔 A(단독 재현)를 넣은 이유도 같다 — 없으면 «동거 탓» 과 «rig 탓» 이 안 갈린다(§3-1).
  - [#214](https://github.com/Shadowfit/init/issues/214) 가 팔 B 의 정의를 바꿨다(§8 첫 항목).
    «캡 없음» 을 곧이곧대로 만들면 팔이 안 돈다.
  - **실행 미착수**, §10 미결정 6건.

- **2026-08-15: rig 작성 — 그리고 설계 전제 하나가 실측으로 반증됐다.**
  rig: [`../../loadtest/results/coresidency-2026-08-15/`](../../loadtest/results/coresidency-2026-08-15/README.md)
  (`gen_frames.py` · `synthetic_body.py` · `load_ai.py` · `probe.sh` · `coresidency_sweep.sh`).
  - 🔴 **«rep 이 완성돼 Spring·MySQL 이 따라온다» 가 틀렸다.** 합성 인체의 무릎 각도가
    **86.95° ~ 124.30°** 로 «서있음»(≥155°) 문턱에 30° 이상 모자란다. **rep 이 영원히 0** 이다.
    검출은 **30/30** 이라 기존 rig 의 단언(`assert_detectable`)은 통과한다 — **«검출» 과 «rep» 은
    다른 조건인데 후자를 아무도 확인한 적이 없었다.**
  - 이 발견은 **rig 을 짜지 않았으면 EC2 위에서 났을 것**이다. 그랬으면 «동거를 쟀다» 는 표에
    Spring·MySQL 이 논 판이 담겼을 것이고, #196 이 남긴 함정(HTTP 200 인데 전 필드 0)과 같은 형태다.
  - → **부하를 두 갈래로 분리**(사용자 결정, §2). rep 에 안 기대므로 #196·#192 에 안 막힌다.
  - ⭐ **게이트가 자기 자신을 한 번 잡았다.** `gen_frames.py` 초판이 «검출 실패» 와 «각도 계산
    실패» 를 같은 `None` 으로 뭉개 *「합성 인체는 검출이 아예 안 된다」* 는 오진을 냈다.
    사유를 갈라 적게 고치니 실제로는 검출 30/30 이었다. 이 rig 이 막으려는 것이 정확히 그런
    종류라 rig 자신이 그러면 안 된다 — 그 교훈을 함수 docstring 에 박았다.
  - **G0(사본 드리프트 대조)를 새로 넣었다.** 합성 인체를 사본으로 둘 수밖에 없었는데
    (원본은 import 만 해도 측정이 돈다), 사본은 갈라진다. 원본에서 같은 구간을 떼어 **글자
    단위로 대조**하고 다르면 게이트에서 멈춘다 — 로컬에서 실제로 통과시켜 봤다.

- **2026-08-16: 무인 러너에 P6 를 붙이다 결함 2건이 나왔고, 팔 A 가 (a)안으로 재정의됐다.**
  `loadtest/aws/run_all.sh` 에 `coresidency_preflight`·`coresidency_rehearsal`·`coresidency`
  단계를 붙였다. **이 라운드만 러너의 자리가 다르다** — 다른 단계는 «러너 = 측정 대상» 인데
  P6 는 rig 이 «부하기에서 대상 박스를 SSH 로 몬다» 를 전제해서, `collect` 가 대상 박스의
  조건을 따로 걷지 않으면 매니페스트가 **부하기 스펙**을 측정 조건으로 박제한다.
  - 🔴 [#222](https://github.com/Shadowfit/init/issues/222) **팔 A 는 성립할 수 없었다**
    — 팔 전환이 Spring·MySQL 을 내리는데 부하기는 세션을 Spring 으로 연다.
    → **(a)안 채택**(사용자 결정): «B 와 같은 구성 + ghz 부하 없음». 상세·대가는 §3-0
  - 🔴 [#223](https://github.com/Shadowfit/init/issues/223) **ghz 부하가 스윕에 없었다** —
    §2 가 «두 갈래» 라고 적은 것 중 Spring·MySQL 쪽이 배선되지 않았다. (a)안이 이것을
    **선행 조건으로 승격**시켰다.
    → **같은 날 배선했다 (요청/초 고정, 사용자 결정).** 부하기 동시성이 아니라 **rate** 를
    묶는다 — 세션 수를 흔드는 실험에서 옆의 부하까지 흔들리면 조작 변수가 둘이 된다.
    `GHZ_RPS` 에 **기본값을 두지 않았다**([[feedback_no_arbitrary_threshold_values]]):
    «정한» 값이므로 근거가 조건 칸에 같이 가야 한다. 그리고 **목표 rps 와 실측 rps 를 따로**
    `ghz.tsv` 에 남긴다 — 못 따라간 rate 는 다른 조건이지 같은 조건의 오차가 아니다.
    부수로 두 가지를 더 막았다: ① ghz 페이로드의 세션이 대상 DB 에 없으면 **전 요청이 FK 로
    실패하는데 지연·요청 수는 정상으로 찍히므로** 시작 전에 시드를 대조한다,
    ② 從 부하가 판마다 행을 쌓아 «판 순서» 가 «세션 수» 에 겹치는 것을 판 사이 정리로 끊는다
  - 📌 **둘 다 표에 흔적을 안 남기는 부류다.** #222 는 `setup_fail` 로 드러나기라도 하지만
    #223 은 «B 가 A 와 비슷하다 = 동거 비용이 작다» 로 **정상처럼 읽힌다.** 그래서 방어를
    사후 주의가 아니라 코드에 넣었다 — 리허설 판정을 종료 코드가 아닌 **결과 표**(`setup_fail`·
    `req`)로 하고, A≡B 는 **시작 전에** 막는다
  - **실행 검증은 아직 없다.** 두 결함 모두 코드 판독으로 찾았고 EC2 에서 돌린 적이 없다

- **2026-08-16: 첫 P6 라운드의 조건이 확정됐다** (사용자 결정 4건). §10 미결정이 그만큼 닫힌다.

  | 항목 | 값 | 근거 |
  |---|---|---|
  | 대상 인스턴스 | **c7i.4xlarge** | 08-14 의 156 이 나온 바로 그 박스(물리 8코어). 다르면 팔 A 가 기준선 구실을 못 한다 |
  | 부하기 | **별도 소형 1대**(c7i.large) | 설계 §10 의 «권장». 부하 생성 비용이 측정 대상 밖에 있어야 동거 값이 안 오염된다 |
  | 從 부하 | **19 req/s 고정** | 유도: 클라 3fps · 배치 25프레임 → 세션당 3/25 = 0.12 req/s. 최고 레벨 160세션 × 0.12 ≈ 19. **«합성이라 rep 이 안 생겨 못 나오는 적재» 를 그만큼 대신 건다** |
  | 팔 C 캡 | **AI 8 / MySQL 4 / Spring 4 vCPU** | c7i.4xlarge 는 **16 vCPU**(물리 8 × HT)이고 docker `cpus:` 는 vCPU 단위다. 4:2:2 비율로 **박스 전체**를 나눈다 |
  | 팔 D | **제외** | 판이 25% 는다. Q5(관측 스택 비용)는 다음으로 |

  🔴 **팔 C 캡에서 함정을 하나 피했다.** 초안은 «8코어를 4:2:2» 로 읽어 4/2/2 를 넣을 뻔했는데,
  그러면 합이 8 vCPU 라 **16 중 절반이 노는 조건**이 되고 팔 B↔C 차이에 «캡 효과» 와
  «절반 효과» 가 섞인다. 물리 코어와 vCPU 를 같은 단위로 쓴 데서 오는 오류다.
  ⚠️ 비율 4:2:2 자체는 여전히 «정한 값» 이다 — [#212](https://github.com/Shadowfit/init/issues/212)
  가 «근거 없음» 으로 열어둔 그 자리이고, 이번 대조가 그 값에 처음 근거를 붙이려는 시도다.

  **부트스트랩이 P6 를 못 세운다는 것도 이때 드러났다** — `bootstrap.sh` 는 MySQL 만 띄운다
  (백엔드는 *"측정에 필요한 건 스키마지 애플리케이션이 아니"* 라는 이유로 의도적 제외).
  P6 는 반대로 **세 컨테이너의 동거가 측정 대상**이라, `ROLE=db|p6-target|p6-loader` 로 갈래를
  넣었다. `db` 는 기존 라운드와 동작이 같다.

- **2026-08-16: 착수가 확정됐고, 남은 미결정 2건이 닫혔다** (사용자 confirm).
  §10 이 6건으로 적혀 있었으나 **실제로 열려 있던 것은 2건**이었다 — 나머지 4건은 같은 날
  위 항목에서 이미 닫혔는데 **체크박스가 안 따라간 것**이다([[project_doc_drift_pattern]] 의 그 형태).

  | 항목 | 결정 | 근거 |
  |---|---|---|
  | 착수 | 🟢 **한다** | 從 4건(R1·R2·R3·**R5**)이 추가 비용 없이 같이 실린다. 특히 R5([#229](https://github.com/Shadowfit/init/issues/229))는 **P6 라운드 전용**이라 놓치면 EC2 를 다시 띄워야 한다 |
  | ramp 레벨 | **20/40/60/80/120/160** | 초판 격자가 «단독 156» 기준이라 상단에 치우쳐 있었다. 유도·대가·안 고른 대안 2개는 §5-2 |

  - **§5 의 「판 사이 스택 재기동」이 「AI 만 재기동」으로 뒤집혔다**([#246](https://github.com/Shadowfit/init/issues/246), §5-1).
    설계와 rig 이 갈렸을 때 **이번엔 rig 이 맞았다** — 세션 상태가 AI 프로세스 메모리에 있어
    AI 는 내려야 하고, MySQL·Spring 까지 내리면 워밍업이 판마다 다른 조건이 된다.
    지우지 않고 뒤집힘으로 남긴다. **남는 한계(판 간 누적)를 §5-1 에 같이 적었다.**
  - 레벨을 **코드 기본값 두 곳**(`coresidency_sweep.sh:47`·`run_all.sh:106`)에 함께 박았다.
    한쪽만 고치면 «러너로 돌린 값» 과 «손으로 돌린 값» 이 갈린다 — [#245](https://github.com/Shadowfit/init/pull/245)
    가 잡은 것이 정확히 그 부류다.
  - 🔴 **여전히 실행 검증은 0 이다.** 이 커밋이 바꾼 것은 조건과 문서지 «돌아간다는 사실» 이 아니다.

- **2026-08-16: 예비 라운드가 이미 있었다 — 회수해 기록했다.**
  EC2 를 다시 켜보니 **08-15 밤에 완주한 라운드**가 박스와 S3 에 남아 있었다(17:41~19:03 UTC,
  본판 4,925초). **회수·기록이 안 된 채 끊겨** 이 문서가 「측정 라운드 미실시」로 남아 있었다.
  결과: [`../../loadtest/results/coresidency-aws-2026-08-15/`](../../loadtest/results/coresidency-aws-2026-08-15/README.md)
  - **Q1 이 처음 숫자를 얻었다 — 동거 천장 ≈ 97세션**(291 fps). 단독 156 대비 크게 내려가고,
    **80까지는 목표를 100% 내다가 160 에서 60% 로 꺾인다.** 천장이 그 사이에 있는데
    4레벨 격자엔 그 구간에 점이 없다 — **6레벨 결정(§5-2)이 사후에 근거를 얻었다**
  - **H2 지지** — 포화 시 AI 939%(≈9.4 vCPU) vs Spring 4.1% · MySQL 8.0%. 옆이 뺏은 게 아니다
  - **H4 반증** — 검출률은 100% 를 유지하고 **지연이 21배**(p50 26ms → 546ms)로 나타난다.
    「품질이 처리량보다 먼저 무너진다」는 이 무대에선 안 일어났다
  - ⭐ **從 R5([#229](https://github.com/Shadowfit/init/issues/229))가 닫혔다.** AI 컨테이너 RSS 실측이
    `708 + 106.74×N` MiB(잔차 ±62)로 적합되어, 공식(`95.8 + 94.1×N`)보다 **고정 +612 MiB ·
    세션당 +12.6 MiB** 크다. 20,000 MiB 한도에서 **공식은 212세션을 허용하는데 실측은 181에서
    한도에 닿는다** — 「풀이 거절하기 전에 OOM」이 **31세션(17%) 폭으로 실재**한다
  - 🔴 **못 읽는 것도 같이 박았다** — ① 라틴 방격이 없어 팔 C 의 처리량 하락은 캡↔순서 교락
    (단 「캡이 800% 에 정확히 붙었다」는 기전 관측이라 별개), ② `stats`↔`req` 시간축 미정렬로
    포화 순간의 귀속은 근사, ③ 🔴 **부하기(c7i.large 2 vCPU) 천장 가능성을 못 배제했다** —
    대상 박스는 16 vCPU 중 9.4 만 쓰고 있었고 부하기 CPU 는 안 걷혔다. **이게 안 갈리면
    「97세션」이 통째로 흔들린다.** 정식 라운드에서 부하기 지표를 같이 걷어 닫아야 한다
  - **네 번째 시도에서 완주했다** — 앞선 3회는 전부 리허설에서 멈췄고(표가 헤더만), 그래서
    6시간짜리 정판이 헛돌지 않았다. **리허설 게이트가 세 번 값을 했다**

- **2026-08-16: 정식 라운드를 완주하고 회수했다 — 두 축이 닫혔고 한 축이 열렸다.**
  6레벨(20·40·60·80·120·160) × 팔 A·B·C × 본판 3 = **본 측정 54 관측** + 버림 3 = 57행
  (설계 §5 의 「12판」은 스윕 단위 = 팔 3 × (본 3 + 버림 1)). 본 측정 7,408초.
  라틴 방격이 성립했다(**r1 = A→B→C · r2 = B→C→A · r3 = C→A→B**). 결함 0 — 전 판
  `detect_pct` 100.00, `nolease`·`nopose`·`setup_fail` 전부 0.
  결과: [`../../loadtest/results/coresidency-aws-2026-08-16/`](../../loadtest/results/coresidency-aws-2026-08-16/README.md)
  - ⭐ **Q4 가 닫혔다 — 팔 C 교락이 풀렸다.** 캡(8 vCPU)에서 AI 가 789.0% 로 평평하고,
    CPU 비 `789/869 = 90.8%` 에 처리량 비 `248.6/267.6 = 92.9%` 가 따라붙는다. **캡한 만큼
    그대로 깎인다.** B↔C 는 부하기 조건이 같아 차이가 전부 대상 쪽이고, 라운드별 방향도
    일정하다(−8.3% · −7.4% · −5.7%). 예비가 열어둔 「캡↔순서 교락」은 여기서 닫힌다
  - **Q2 재확인 + 새 사실.** 포화 시 AI 869~893% vs Spring 4.5% · MySQL 7.8% — 동거인 둘을
    합쳐도 **0.17 vCPU 남짓**이다. 그리고 박스가 **16 vCPU 인데 천장이 ~8.9 vCPU** 다 —
    이 워크로드에서 **하이퍼스레딩이 사실상 아무것도 안 준다**. 천장은 vCPU 가 아니라
    **물리 코어 8** 이고, 08-14 의 「물리 코어당 16.4세션」과 정합한다
  - ⭐ **從 R5 가 재현됐다.** 예비가 세운 실측식 `708 + 106.74×N` MiB 를 6레벨에 대보면
    오차 **−0.7% ~ +0.6%**(20→160세션). 다른 격자·다른 코드에서 재현됐으니 **한 라운드의
    우연이 아니다.** 그리고 이 조건에서 **메모리는 병목이 아니다** — 20,000 MiB 는 식으로
    176세션을 허용하는데 처리량 천장이 그 절반도 안 되는 80~120 에서 먼저 온다
  - **Q1 은 구간까지만.** 80 은 목표를 100% 내고 120 은 69~80% 로 꺾인다. 그 사이에 관측점이
    없어 **「80 초과 120 미만」** 이 한계다. §5-2 가 4→6레벨로 넓힌 이유가 「천장이 40~80 이면
    구간으로밖에 못 적는다」였는데, **천장이 실제로는 한 칸 위에 있어 같은 문제가 반복됐다**
  - 🔴 **H1(동거 비용)은 지지도 반증도 안 됐다.** 팔 A 의 plateau 가 **r1 266.0 → r2 282.9 →
    r3 308.6 fps (+16.0%)** 로 단조 상승한다. 팔 A 의 판 순서는 1→3→2 인데 값은 **벽시계
    순서로** 올랐다 — 위치 효과가 아니라 **시간 추세**이고, **라틴 방격은 위치는 상쇄해도
    시간 추세는 상쇄하지 못한다.** 그래서 「A−B」가 라운드마다 **−0.8% / −6.2% / −13.3%** 로
    16배 벌어진다. §0 표의 −6.4% 는 **평균일 뿐 대표하는 모집단이 없다**
  - 🔴 **부하기 천장은 이번에도 못 배제했다** ([#250](https://github.com/Shadowfit/init/issues/250)). `stats_*.tsv` 는 전부 대상 박스의 `docker stats`
    이고 **부하기(c7i.large 2 vCPU) 지표는 이번 라운드도 안 걷혔다.** 팔 B↔C 는 이 문제에서
    자유롭지만(부하기 조건 동일), **팔 A↔B 는 ghz 프로세스가 부하기에서 도는 만큼 교락**이다.
    위 H1 판정 불가와 **같은 뿌리일 수 있다** — 다음 라운드는 부하기 샘플러가 선행 조건이다.
    ✅ **그 선행 조건은 충족됐다** — `coresidency_sweep.sh:660 start_loader_stats()` 가 부하기
    자신을 걷고 `run_all.sh:626 cores_assert_loader()` 가 「걷혔나」를 판정한다. 08-17 라운드에서
    실제로 걷혔고, 그 값이 위 §12-1 의 귀속 판정을 만들었다
  - **예비값과 8% 다르다.** 예비 97세션(291 fps) vs 정식 팔 B 89.2세션(267.6 fps). 격자·코드·
    반복 수가 전부 달라 **원인을 못 가른다.** 정식 격자 값을 쓰되 차이는 남긴다

- **2026-08-16: 2라운드 설계 — 판 설계에서 결함 4건이 더 나왔다** (사용자 결정: **「전부」로 간다**).
  1라운드가 못 닫은 셋을 놓고 rig 을 다시 대조했더니, **둘의 뿌리가 판 설계 자체**에 있었다.
  - 🔴 [#251](https://github.com/Shadowfit/init/issues/251) **팔 대조와 시간 추세가 안 갈린다.**
    블록의 단위가 «팔» 이라 같은 레벨의 팔 간 대조점이 **13~27분**(r1 +14 · r2 −27 · r3 +12)
    떨어져 있고, 그 사이 처리량이 **분당 0.09~0.35%** 흐른다. 기울기가 구간마다 4배 달라
    **선형 보정으로도 못 뺀다** — 그래서 §5-3 이 교대 단위를 **레벨**로 내린다.
    📌 §4-1(H1 판정 불가)이 결과 해석의 문제가 아니라 **판 배열의 문제**였다는 것이 이번 발견이다
  - 🔴 [#252](https://github.com/Shadowfit/init/issues/252) **레벨 순서가 아홉 블록 전부 오름차순.**
    라틴 방격을 팔에만 걸고 레벨엔 안 걸었다. 블록 안 12분 추세가 **120·160**, 즉 plateau 를
    정의하는 자리에 얹힌다. 고치는 비용 0
  - 🔴 [#253](https://github.com/Shadowfit/init/issues/253) **캡이 물렸는지 단언이 없다.**
    안 물리면 팔 C ≡ 팔 B 인데 결함 카운터는 0 이다 — #223 과 같은 부류를
    **캡 축에서만 안 막고 있었다.** 1라운드의 「물렸다」는 사후 관측(789%)이지 게이트가 아니다
  - 🔴 [#254](https://github.com/Shadowfit/init/issues/254) **H3 은 판정할 열이 없다.**
    설계 §4 가 요구한 세 소스 중 `docker stats` 하나만 걷고, mysqld_exporter·actuator 는
    스크레이프 코드가 없으며 `ghz.tsv` 에는 지연 백분위조차 없다.
    **「캡 축이 닫혔다」는 AI 쪽 이야기였고 «옆이 사는가» 는 그대로 열려 있다** — Q3 절반
  - 📌 **그리고 CPU 샘플러만으로는 #250 이 안 닫힌다** — plateau 가 **세션수 ÷ p50** 으로
    오차 2% 안에 재현된다(§4-2). 페이싱 rig 이라 **서버가 느린 것과 부하기가 느린 것이 같은
    모양**을 낸다. 대상 박스 **자기 프로브**를 넣어 RTT 를 두 시계로 재는 것이 그 답이다
  - **결정: 아홉 항목을 한 라운드에 전부 얹는다**(§11). 78판 · 인스턴스 ≈ 3h20m.
    대상 인스턴스를 두 번 띄우는 것이 판 18개보다 비싸다는 판단이다
  - ⚠️ **이 커밋도 문서와 이슈만 바꾼다.** rig 코드는 아직 안 고쳤고,
    `docker update --cpus` 검증도 안 했다. **실행 검증은 여전히 0 이다**

- **2026-08-16: 그 설계를 쓰는 사이에 2라운드가 이미 돌아 있었다** (§12).
  `coresidency-aws-b-2026-08-16/` 이 회수되지 않은 채 디스크에 있었다 — **돌았는데 repo 가
  모르는 일이 P6 에서 두 번째**다(08-15 예비도 같았다). 회수·분석은
  [결과 문서](../../loadtest/results/coresidency-aws-b-2026-08-16/README.md)에 있다.
  - ⭐ **H1 이 답을 얻었다** — A 316.5 vs B 315.0 = **−0.5%**(sd 1.4~1.7). 19 req/s 조건에서
    동거 비용은 **잡음 크기**다. 그리고 **드리프트가 사라졌다**(팔 A +16.0% → **+0.8%**) —
    [#251](https://github.com/Shadowfit/init/issues/251) 의 교락은 실재하지만 크기가 무너져
    §11 의 ①② 가 강등된다. **가장 비싼 변경의 근거가 반나절 만에 약해졌다** — 「설계 먼저,
    코드 나중」이 값을 한 사례다
  - 🔴 **그런데 「부하기가 천장이었다」는 아니다.** 팔 B 의 AI CPU 가 **869.3% → 869.0%** 로
    같은데 처리량만 **+17.7%** 다. 부하기 기아였다면 AI CPU 가 낮았어야 한다.
    남은 후보 둘(부하기 송신 패턴 · 대상 박스 물리 호스트 교체)이 **같이 바뀌어 안 갈린다**
  - 🔴 **새로 열린 것 — 라운드 간 절대값이 안 재현된다.** 같은 코드·격자·인스턴스 타입에서
    천장 환산이 **89.2 → 105.0세션**으로 움직였다. 「절대 세션 수를 인용하지 말라」가
    이제 **경고가 아니라 측정**이다. 앵커 판(§11 ⑤)의 값이 여기서 올라간다
  - ⚠️ 초안은 이 라운드를 「부하기가 천장이었다」로 읽었다. **AI CPU 대조가 그것을 반증했고,
    그 정정을 지우지 않고 여기 남긴다**

- **2026-08-17: rig 코드에 ⑥·⑨ 를 넣었다** (§11 의 남은 최우선 둘).
  - **⑥ 레벨 8개** — `20 40 60 80 90 100 120 160`. 천장이 두 라운드 연속 「80 초과 120 미만」
    구간으로만 짚혔다. **두 곳(`coresidency_sweep.sh` LEVELS · `run_all.sh` CORES_LEVELS)에
    같이 박았다** — 한쪽만 고치면 «러너로 돈 값» 과 «손으로 돈 값» 이 갈린다(#245 가 잡은 부류)
  - **⑨ H3 의 판정 열** — ① `ghz.tsv` 에 `p50_ms·p95_ms·p99_ms`(ghz JSON 의
    `latencyDistribution`. **이미 그 파일을 파싱하고 있었다 — 꺼내 적기만 하면 됐다**),
    ② 판마다 **pre·mid·post 3회** 옆 스냅샷 → `side.tsv`(long format).
    🔴 **2회가 아니라 3회인 이유**: 카운터는 post−pre 로 그 판의 일을 내지만, **게이지
    (`Threads_running`·Hikari `pending`)는 부하 중에만 뜻이 있다.** pre·post 두 점은 둘 다
    유휴 순간이라 「포화 때 옆이 어땠나」가 통째로 안 남는다 — H3 이 묻는 것이 정확히 그것이다
  - 🔴 **actuator 는 9090 이고 compose 가 127.0.0.1 에만 연다**(`application.yml` [6]).
    그래서 부하기에서 직접 못 긁고 **대상 박스 안에서** curl 한다 — SG 를 안 건드린다
  - **게이트를 같이 넣었다** — `cores_assert_side` 가 리허설에서 «spring/mysql 행이 있는가 ·
    전부 FAIL 이 아닌가 · **mid 게이지가 실제로 찍혔는가**» 를 보고, 아니면 본판을 안 돈다.
    preflight 도 actuator 도달을 미리 묻는다. **«걷는 코드가 있다» 와 «걷혔다» 는 다르다**
    — #250 이 그 자리에서 두 번 막혔다
  - 새 열은 **끝에만** 붙였다(`cores_assert_ghz` 가 $1~$8 을 위치로 읽는다). 못 걷은 값은
    빈칸이 아니라 **`FAIL`·`-`** 로 남긴다 — 빈칸은 «0» 으로도 «안 걸었다» 로도 읽힌다
  - 부수: `start_loader_stats` 주석의 «200% 가 판정선» 을 지웠다. 2라운드가 그것을 반증했다(§12-1)
  - **검증**: `bash -n` 통과 · ghz 파서를 **원본 코드를 떼어내** 합성 JSON 3종(정상·백분위
    없음·읽기 실패)으로 대조(4.1/18.9/42.0ms · `-` · FAIL, 전부 11열) · `snap_side` 를 SSH 스텁으로
    돌려 프로메테우스 필터·라벨 보존·FAIL 경로 확인(전 행 8열) · `cores_assert_side` 를 정상 1종·
    파손 4종으로 대조(전부 기대대로 통과/차단).
    🔴 **EC2 실행 검증은 0** — 리허설이 판정한다
  - **판 수 75 · 스윕 ≈ 2h41m** (앵커 판 ⑤ 는 아직 코드에 없다 — 들어가면 78판 ≈ 3h01m)

- **2026-08-17: ④·⑤ 도 넣었다 — 그리고 「반전」이 안 통한다는 것을 계산으로 확인했다.**
  - **④ 레벨 순서** — 설계는 *「라운드마다 반전」* 이라고 적었는데, **그걸로는 안 고쳐진다.**
    오름/내림/오름(3판)이면 레벨의 «블록 안 평균 위치» 가 레벨에 **여전히 완전 비례**한다
    (상관 **r = 1.000** — 고치기 전과 같다). 그래서 `rotate_arms` 와 같은 **치환**으로 갔다:

    | 배열 | 반전 | shift1 | **shift2** | shift3 | shift4 | shift6 |
    |---|---:|---:|---:|---:|---:|---:|
    | 레벨 ↔ 평균위치 상관 | **1.000** | 0.580 | **0.361** | 0.411 | 0.907 | 0.316 |

    `LEVEL_SHIFT=2` 를 골랐다(6 이 0.316 으로 사실상 같다 — 8 기준 거울상).
    ⚠️ **완전 균형이 아니다.** 3판으로 8자리를 못 고르며, 이 값은 «8레벨 × 3판 × 그 레벨 값»
    에 딸린 값이라 격자를 바꾸면 다시 계산해야 한다. 상관을 **없앤 것이 아니라 줄였다.**
  - **⑤ 앵커 판** — 설계의 «3개» 를 **4개**로 늘렸다(라운드마다 + **끝에 하나**). 끝 점이
    없으면 계열이 라운드 전 구간을 안 감싸서, 정작 1라운드에서 드리프트가 가장 컸던
    «마지막 구간» 이 밖에 남는다. 그리고 🔴 **첫 앵커 앞에 그 팔의 버림판을 먼저 돌린다** —
    앵커 계열의 첫 점이 워밍업을 뒤집어쓰면 «시간에 따라 올라간다» 가 그 한 점 때문에 생긴다.
    앵커는 `round=anc1..4` 로 본 격자와 구분되고, `warm_lo` 로 시간축에 그대로 올라간다.
  - **판 수 79**(본판 72 + 버림 3 + 앵커 4) · 스윕 ≈ 2h52m · 인스턴스 ≈ 3h15m.
  - **검증**: `bash -n` · 루프를 스텁으로 **드라이런**해서 ① 판 구성 79개(버림 3 = A@20·B@80·C@20,
    앵커 4) ② 라운드별 레벨 순서가 `20…160 / 60…40 / 90…80` 로 치환 ③ 레벨↔평균위치 상관이
    **0.361**(오름차순 고정이면 1.000) ④ 리허설 격자(5·10 · REPEATS 1)에서 11판으로 성립
    ⑤ `ARMS="A"` 처럼 앵커 팔이 없으면 **경고 후 건너뛴다**(죽지 않는다) 를 확인했다.
    🔴 **EC2 실행 검증은 여전히 0.**

- **2026-08-17: ③·⑦ 을 넣었다 — 남은 것은 ①②(강등)와 ⑧(자기 프로브)뿐이다.**
  - **③ 캡 단언** ([#253](https://github.com/Shadowfit/init/issues/253)) — `apply_arm` 이 끝나면
    `docker inspect` 로 세 컨테이너의 `NanoCpus` 를 읽어 판정한다. 팔 C·D 는 **`.env` 기대값과
    일치**해야 하고, **팔 A·B 는 0이어야 한다** — override 를 지웠는데 컨테이너가 재생성되지
    않으면 앞 팔의 캡이 그대로 남기 때문이다(반대 방향 단언이 없으면 그게 안 보인다).
    판정은 6가지로 갈라 적는다: `OK` · `MISSING` · `MISMATCH` · `STALE` · `SET_UNVERIFIED`
    (캡은 있는데 기대값을 못 읽음) · `UNREADABLE`(못 물어봤다 — «캡이 없다» 와 구분).
    관측값은 **`caps.tsv`** 에 남긴다. 캡은 측정 조건이라 «단언했다» 로 끝내면 결과 문서에
    적을 근거가 없다.
  - **⑦ 부하기 샘플러 보강** ([#250](https://github.com/Shadowfit/init/issues/250)) — 넷이다.
    ① **프로세스 %도 실측 dt 로** 나눈다(초판은 총 CPU 만 jiffies, 프로세스는 `STATS_SEC`
    가정이라 루프가 밀리면 프로세스만 과소), ② **`/proc/net/dev`** 로 `rx_mbps`·`tx_mbps` —
    CPU 가 안 붙었는데 천장이면 다음 용의자가 대역인데 자료가 없었다,
    ③ **음수는 `-1`**([#255](https://github.com/Shadowfit/init/issues/255)) — 프로세스가 창 안에서
    끝나면 차분이 음수가 되고, 그대로 평균 내면 부하기 CPU 가 «작게» 나온다,
    ④ **리허설 게이트** `cores_assert_loader` — 판정을 «행이 있는가» 가 아니라
    **«`load_ai` 가 한 표본이라도 보였는가»** 로 한다. `pgrep` 패턴이 안 맞으면 전부 0 인 표가
    조용히 생기고 「부하기는 안 붙었다」로 **잘못** 읽히는 것이 이 축의 실패 모드다.
    그리고 「200% 가 판정선」 주석을 지웠다 — §12-1 이 반증했다.
  - **검증**: `bash -n` · 계산 awk 를 **원본에서 떼어내** 알려진 입력으로 대조
    (n=2·hz=100·5초에서 총 60.0% · 프로세스 50.0% · 음수 → `-1` · rx 10.00 Mbps, 9열 = 헤더 9열) ·
    `/proc/net/dev` 파서를 합성 표(`lo` 포함)로 대조 · `assert_caps` 를 **7가지 상황**으로
    (정상 C · 정상 B · AI 캡 빠짐 · 값 불일치 · 앞 팔 캡 잔존 · `.env` 못 읽음 · inspect 실패)
    전부 기대대로 통과/차단 · `cores_assert_loader` 를 4가지(정상 · 전부 0 · 헤더만 · 파일 없음)로 대조.
    🔴 **EC2 실행 검증은 여전히 0.**

- **2026-08-17: ⑧ 자기 프로브까지 넣었다 — 2라운드 항목 아홉 중 일곱이 코드에 있다.**
  대상 박스에서 `load_ai.py --sessions 1 --dur 0` 을 돌려 **서버 쪽 시계**를 따로 잰다.
  구현하며 정해진 것 셋은 §4-2 표에 있다(부하기 재사용 · SIGTERM 종료 · warm 창 슬라이스).
  - 🔴 **부하기 코드를 그대로 쓴다.** 프로브를 따로 짜면 세션 개설 경로가 두 벌이 되고,
    이 rig 은 이미 그 함정(온보딩 `preferredUrl` 없으면 세션 생성 400)을 한 번 밟았다.
    자산은 `$SSH "cat > …"` 로 밀고 **크기로 대조**한다 — 반쯤 간 파일은 `ls` 에 «있다» 로 보인다.
  - 🔴 **`--dur 0`(SIGTERM 까지)이 필요한 이유**: 프로브는 판이 끝날 때 같이 끝나야 하는데,
    부하기의 계정 준비가 세션 수에 비례해 길어져(160세션이면 20초 초과) **측정창 시작이
    판마다 다르다.** 고정 dur 로는 못 맞춘다. `kill -9` 로 잡으면 그 판의 서버 시계가
    통째로 사라진다 — `load_ai.py` 는 요약을 **끝에 한 번** 쓴다.
  - 🔴 **부하기의 warm 창으로 잘라서** 요약한다. 프로브는 판보다 먼저 시작해 부하 없는
    구간을 같이 담기 때문이다. `window` 칸에 `warm`·`full`·`no_overlap`·`empty` 를 갈라 적고,
    **겹치지 않으면 백분위를 안 적는다** — 부하 없는 p50 은 «서버가 빠르다» 로 읽히고,
    그게 이 프로브를 넣은 이유를 통째로 무너뜨린다.
  - **게이트**: `cores_assert_probe` 가 «행이 있는가» 가 아니라 **«두 시계를 같은 창에서
    읽었는가»**(`window=warm` 이 한 판이라도 있는가)로 판정한다. `CORES_PROBE=0` 으로 끄면
    «서버 시계 없이 도는 라운드» 라고 로그에 남는다.
  - **검증**: 스텁 HTTP 서버(signup·login·onboarding·session·pose·end)를 세워 **실제로
    돌렸다** — ① `--user-prefix probe` 로 계정이 `probe0@shadowfit.local` 로 갈리고,
    ② `--dur 20` 정상 경로가 그대로 서며(요약 `dur`=20 · warm 30요청),
    ③ `--dur 0` 경로에서 STOP 이 서면 **`dur_eff`=실제로 돈 14초**로 warm 창이 잡히고
    요약·원본이 남는다. 슬라이서는 4가지 상황(warm·full·no_overlap·empty)으로 대조했고,
    게이트는 5가지로 대조했다.
    ⚠️ **OS 의 SIGTERM 전달 자체는 로컬에서 확인 못 했다** — Windows 는 파이썬 SIGTERM
    핸들러를 안 돌리고(하드 킬, 종료코드 143), WSL 에 배포판이 없다. **리허설이 판정한다.**
  - **남은 것은 ①②(교대 단위)뿐이고 강등 상태다.** 드리프트가 다시 나타나면 되살린다 —
    그 감시는 앵커 판이 맡는다.

- **2026-08-17: ⑩ 부하기 코어 팔(§T)을 넣었다 — 「부하기 탓」과 「호스트 탓」을 처음으로 가른다** (사용자 결정).
  §12-1 이 남긴 후보 둘이 **같이 바뀌어서** 안 갈렸다. §T 는 그 둘을 뗀다 — 대상 박스를
  재기동하지 않고 **같은 세션 안에서 부하기 코어 수만** `taskset` 으로 흔든다.
  - **쌍을 붙여서 돈다** (`lc2_t1@120` → `lcF_t1@120` → `lc2_t1@160` → …). 그래야 시간 추세가
    쌍 안에서 상쇄된다. 쌍의 순서는 반복마다 뒤집는다(홀수 2→전체 · 짝수 전체→2)
  - **부하기의 두 프로세스를 다 묶는다.** `load_ai.py` 만 묶으면 ghz 가 남은 코어로 도망가
    «2코어 박스» 가 아니라 «2코어 + 별도 코어» 가 된다
  - 🔴 **볼 것은 처리량 차이지 «기아» 가 아니다.** 기아 가설은 이미 기각됐다 — 두 라운드 모두
    AI 가 **869% (물리 8코어 천장)** 에 붙어 있었고, 굶었다면 `267.6 × 27.6ms = 739%` 여야 했다.
    남은 질문은 «도착 리듬이 프레임당 비용을 올리나» 이고, 그래서 판정 열은
    **`rps` 와 `AI CPU ÷ rps`** 다
  - ⚠️ **완전한 재현이 아니다.** `taskset` 은 코어 수만 흉내 낸다 — 실제 c7i.large 는 네트워크·
    EBS 대역도 다르고, 샘플러(ssh·docker stats)는 제한 밖에서 돈다(실제 2 vCPU 박스에서는
    그것들도 같은 두 코어를 나눠 썼다). **「c7i.large 라운드를 재현했다」고 쓰면 안 된다.**
  - 🔴 **부하기가 `c7i.xlarge` 이상이어야 성립한다.** 2 vCPU 면 «제한» 과 «전체» 가 같은
    조건이라 대조가 없다 — preflight 가 막고, 스윕도 스스로 건너뛴다
  - **판 12개 · +26분.** 라운드는 91판 · 인스턴스 ≈ 3h40m 이 된다
  - **검증**: `bash -n` · 루프 드라이런으로 ① §T 12판이 **쌍으로 붙어** 나오고 순서가 반복마다
    뒤집히는 것 ② `taskset -c 0-1` 이 `lc2` 판에만 붙는 것 ③ 본 격자 72판의 `loader_cpus` 가
    전부 `full` 인 것 ④ 건너뛰는 세 경우(taskset 없음 · 부하기 2코어 · 팔이 ARMS 에 없음)가
    **죽지 않고 경고로 지나가는** 것을 확인. 게이트 `cores_assert_taskset` 은 4가지로 대조했고,
    쌍이 한쪽만 남으면 차단한다. 🔴 **EC2 실행 검증은 0.**

- **2026-08-17: EC2 리허설 1차 — 절반이 섰고, 두 결함을 잡았다** (§11 「점심때 할 일」).
  대상 c7i.4xlarge + 부하기 **c7i.xlarge** 를 띄워 축소 리허설을 돌렸다. 13판 중 8판에서
  사람이 끊었고 **인스턴스는 둘 다 terminate**(볼륨 0 확인). 기록:
  [`rehearsal-aws-2026-08-17/`](../../loadtest/results/coresidency-2026-08-15/rehearsal-aws-2026-08-17/README.md).
  - ✅ **EC2 에서 실제로 선 것**: ③ 캡 단언(팔 C 가 `8/4/4 vCPU` 로 물렸고 `.env` 기대값과 일치,
    팔 A·B 는 0 → `STALE` 없음) · ④ 레벨 치환과 새 열 `loader_cpus` · ⑤ 앵커 판(앞에 버림판이
    먼저 돈다) · ⑦ 부하기 샘플러 · ⑨ **ghz 지연 백분위(11.0~13.7ms)와 `side.tsv` 4,639행** —
    `grpc_server_*`·`hikaricp_*`·`Threads_running` 이 걷힌다. **H3 판정 열이 처음으로 존재한다**
  - ⭐ **부수 확인**: `STALE` 이 한 번도 안 났다 = compose 가 override 를 지우면 컨테이너를
    실제로 재생성한다. 지금까지 «아마 그럴 것» 이던 전제가 사실이 됐다
  - 🔴 **결함 ①**: `$SSH` 에 `-n` 이 있으면 `setup_probe` 의 자산 push(`cat > … < 파일`)가
    **0바이트**로 간다. ⭐ **rig 이 스스로 잡았다** — 크기 대조가 `0/418135 B` 를 보고 프로브를
    끄고 사유를 남겼다. 경고문에 «-n 이면 이것» 을 박았다
  - 🔴 **결함 ②**: 같은 함정이 `assert_caps` 에 잠복해 있었다 — `while read … <<EOF` 안에서
    `$SSH` 를 부르면 그 ssh 가 heredoc 을 먹어 **루프가 첫 줄만 돈다**(= 컨테이너 하나만 확인).
    이번엔 `-n` 덕에 우연히 안 터졌다. 기대값을 루프 앞에서 한 번에 읽도록 고쳤다
  - 🔴 **S3 가 막혀 있다**: 이 IAM 사용자는 인스턴스 프로파일을 못 붙인다 → `preflight_s3` 가
    하드 실패라 **정식 라운드가 아예 안 돈다.** 1차는 `PHASES=coresidency_rehearsal` 만 돌리고
    scp 로 회수했다. **탑승 전 반드시 풀어야 하는 항목**이다
  - **아직 미검증**: ⑧ 자기 프로브 · ⑩ §T · `cores_assert_*` 게이트 4종 · preflight 단계

- **2026-08-22: §6 에 «2라운드 판정» 열을 붙였다 — 라운드는 08-17 에 돌았는데 이 표만 닷새 늦었다.**
  정식 2라운드([`coresidency-aws-2026-08-17/`](../../loadtest/results/coresidency-aws-2026-08-17/README.md))의
  판정이 §11·§12 와 결과 디렉터리·[`experiment-inventory.md`](./experiment-inventory.md) 에는 반영돼 있었는데,
  **가설표에는 1라운드 열만 있어 이 문서만 보면 H1「판정 불가」· H3「자료 없음」으로 읽혔다.**
  전형적인 문서 드리프트다([[project_doc_drift_pattern]]) — 기능·경위는 갱신되고 **판정이 안 따라왔다.**
  - **새 열의 숫자는 결과 원본에서 다시 계산했다**(README 인용이 아니다):
    H1 은 `coresidency/coresidency.tsv` 의 r1~r3 평균(8레벨 × 3판), H2 는 `stats_B_r*_160.tsv` 의
    컨테이너별 집계, H4 는 같은 tsv 의 `detect_pct` 91행 전수. H3 은 README §2 의 지연표를 인용했다
  - **상태 헤더도 같이 고쳤다** — 「1라운드 회수 완료 · H1 은 열려 있다」가 새 열과 정면으로 어긋난다.
    08-16 의 «부하기가 천장이었다» 가 §T 로 반증된 것도 헤더에 박았다(§12-1 은 이미 갱신돼 있었다)
  - 🔴 **남은 빈칸은 H5 하나다**(팔 D — AI 부하 기준 Q5). 08-18 세션분산 라운드의 −0.05% 는
    **쓰기 경로** 값이라 여기 그대로 못 옮긴다 — 두 부하는 병목이 다르다

- **2026-08-23: H5(팔 D · Q5)를 P6-b 라운드의 從 R9 로 등록했다.**
  이 문서는 세 곳에서 «미측정» 이라고만 적었고 **어느 라운드에 얹히는지는 아무 데도 없었다** —
  그래서 라운드마다 「길어지니 뺀다」로 밀리기만 했다(1라운드 §5-3 · 2라운드 §11).
  탑승 목록에 **자리를 준 것**이 이번 변경이다([`AWS-RIDE-ALONG.md` §1 從 R9](../../loadtest/AWS-RIDE-ALONG.md)).
  - **P4 에는 못 얹는다.** 그 무대는 MySQL 2대뿐이고 AI 컨테이너·부하기·`frames.json` 이 없다.
    팔 D 는 «팔 C + 관측 스택» 이라 **AI 동거 무대 자체가 전제**다 — P6-b 가 유일한 자리다.
  - ⚠️ **등록 ≠ 채택.** +19판(+42분)이라 從 의 «추가 비용 거의 0» 에서 벗어난다.
    넣을지는 격자 확정 시점의 결정으로 남긴다.
  - §11 「이번에도 안 하는 것」의 팔 D 줄도 같이 고쳤다 — 제외 사유가 「못 닫은 셋이 먼저」였는데
    **그 셋은 2라운드에서 전부 닫혔다.** 사유가 유효한 채로 남아 있으면 다음 라운드도 같은 문장으로 밀린다.
